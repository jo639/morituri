# MORITURI — [0] 구현 로드맵
**작성:** 2026-06-12 (기획시안·설계서[1][3][4][5]·README 전수 검토 후 수립)
**원칙:** 각 단계는 *측정 가능한 합격 기준*을 가진다. 그래픽보다 숫자 먼저 (문서[1] M2→M3 순서의 존재 이유).
> **이 문서는 결정 로그(히스토리 보존).** 지금 코드의 현재 상태 단일 진입점은 [`MORITURI_현황.md`](MORITURI_현황.md).

---

## 현재 위치

```
Phase 1  [M1 수식 ✅] [M2 막대기 전투* ✅] [M3 헤드리스 ✅] [M4 ◐ M4-a+B 완료, M4-b 웹 프레젠테이션 1R]
Phase 2  특성/감정/관계 — 인터페이스만 예약됨 (EmotionMult, T09~T11)
Phase 3  리그/역사 — 미착수 (문서[2] ERD부터 필요)
Phase 4  혈통/감독 — 미착수
```
\* M2의 "Unity 캡슐 재생"은 텍스트 리플레이 뷰어(`replay` 모드)로 대체 검증됨.

**달성:** 테스트 56개 통과. M3 = 상성 매트릭스(A)·무기 밸런스+하이퍼아머(A2)·도발 역전 재설계(B)·
거울전 편향 수용·문서화(C)·직렬화(M3.5). M4-a = 디버그 2D 뷰어(`viewer.html` + 위치 프레임 트랙).
**남은 것:** M3-D(치명타 상한, 백로그 — Phase 2와 병합). M4-c(문서[7] UI/UX 바인딩, M4-a로 도구 준비됨).

---

## M3 마무리 — "상성이 진짜 가위바위보인가" (Phase 1의 본질 검증)

기획시안 16장: *"전투 상성 시스템을 잘 구현해야함"* — 지금까지 5×5 상성 매트릭스(문서[4] 11장)의
**1칸**(난전vs카운터)만 검증했다. Phase 1 합격 판정은 매트릭스 전체로 한다.

### M3-A. 상성 매트릭스 배치 + CSV 리포트 ✅ (결론: 가설 폐기, 프레임 전환)
- 인프라 완성: `matrix [N] [무기]`, `weaponprobe`, `sigmatrix`, `sweep` (Parallel). CSV 리포트 출력.
- **검증 결과(문서[4] 11장 개정):** 동일 무기 전술 RPS는 **구조적으로 성립 불가** — 전술이
  공격성↔규율 1차원 축으로 붕괴. 81구성×3무기 스윕에서 최적해도 10칸 중 5칸 퇴화. 상수 조정 무력.
  차별화 축은 무기(사거리·속도·Poise·GuardCrush)가 공급. 난전형 검 23%→쌍검 99.7%.
- **난전형 결론:** 무기 의존 전술로 재정의 확정. 검 약세는 버그 아님. 클린치 등 신규 메커니즘 불필요.
- **후속 분기(M3-A2):** ① 무기 밸런스 ✅ 해결 ② 행동 다양성 → 스킬·계급(문서[6]).

### M3-A2. 무기 밸런스 + 하이퍼아머 ✅
- 중량 무기도 빌드 결합 판명(도끼·균형 24%→난전 54%). **성공 지표를 균형형 스프레드에서
  무기×빌드 매트릭스로 전환**(`buildmatrix`): 목표 반격부재 0 + 파워 38~62.
- **하이퍼아머**(라니스타 설계): 강공 선딜 중 약공 관철·강공 끊김 → 중량 무기 정체성 부활
  (버서커 도끼 33.6:66.4 → 58.5:41.5). T01.HyperArmor/HeavyBias + 아머 트레이드 AI + 적중 프레임 우위.
- **무기 데미지 자동 스윕**(`weaponsweep`, 좌표하강): 검 42→33·방패 36→40·쌍검 26×2→18×2.
  빌드 스프레드 79%p→23%p, 반격부재 0/8. 망치는 재설계 불필요(제 빌드서 건강) 확인.
- 잔여(미세, 나중 일괄): 검·균형 61.8 상단, 창/방패 39 하단. 버서커 매치업 도끼 우세(b 수용).

### M3-B. 오만함 도발 역전 재설계 (이슈 #3) ✅
- 문제: 옛 트리거(상대 HP≤10%)는 처벌자가 이미 빈사 → 역전패 **0%** (죽은 메커니즘).
- 재설계: 트리거를 `OppStaggeredWhileAhead`(상대 스태거 + HP 리드 ≥5.5%p)로 이동 — 상대가
  살아서 스태거(0.8s) 회복 후 도발(1.5s) 잔여 창에 2배 처벌 가능. 다운(1.5s)이 아니라 스태거를 쓴
  이유: Down=Taunt=1.5s라 상대가 일어날 때 도발이 이미 끝나 처벌 창이 0 (역전 0% 재발).
- **핵심 진단:** 도발의 조건부 역전패는 처벌 *크기*가 아니라 *선별*(어떤 경기 상태가 도발 자격을 얻나)이
  결정한다. 처벌 배수(2.0→1.75)·도발 창(1.5→1.3)·쿨다운(8→15)·확률(0.7→0.5) **모두 무효과**였고,
  유일하게 움직인 레버는 트리거의 HP 리드 폭(선별 기준)이었다. 리드 5.5%p가 5~10% 밴드를 조인다.
- **합격:** near-mirror(압박+검, 오만 vs 냉철, 6000경기)에서 도발 발생 20.0%, **도발자 패배 8.5%**
  (목표 5~10% 정중앙). 전술 불균형 매치업은 상성이 도발 효과를 삼켜 역전이 수학적으로 불가능해
  측정 도구로 부적합 → 도발만이 유일 변수인 near-mirror로 측정.

### M3-C. 거울전 선공 편향 완화 (이슈 #4) ✅ (수용·문서화 — 전제 반증)
- 재측정: 로드맵의 7.6%p는 옛 값. 선행 밸런스 작업(스태미나·판정 개편)으로 **현재 ~4.8%p**(A 52.4%)
  — 제안 밴드 [0.42, 0.58]에 이미 진입.
- **전제 반증:** "갱신 순서 교대로 편향 감소" 가설은 데이터로 깨졌다. 시도한 국소 수정 4종이 모두
  편향을 상쇄가 아니라 *재매핑*했다 — 결정순서교대(A 59%)·선수별 독립RNG(A 53.9%)·+FsmAdvance교대
  (A 66.3%)·ResolutionPhase resolve-both(B 57.2%, 부호 반전). 180초 판정 거울전은 결정론적이지만
  카오스적 초민감이라, 미세 비대칭이 평균으로 상쇄되지 않고 다수 경기를 한쪽으로 기울인다.
- **부수 발견(미수정·백로그):** ResolutionPhase는 선수 0의 적중이 선수 1을 Stagger로 바꿔 *이미 커밋된*
  동시 스윙을 취소시키는 진짜 버그가 있다(인덱스 0이 동시 트레이드 독식). 단 고치면 편향이 B로 이동하고
  M3-A2 밸런스 수치를 전부 재튜닝해야 해 보류. 동시성 전면 재작성과 함께 다룰 후보.
- **결론:** 밸런스 데이터 무편향은 배치 도구의 시드별 코너 교대가 이미 담당. 편향 4.8%p 수용,
  회귀 테스트만 [0.42, 0.58] @ n=1000으로 강화(상한까지 ~3.5σ 여유). ±3%p 산문 목표는
  동시성 전면 재작성 필요 → 저ROI로 백로그.
- **합격:** `MirrorMatch_NoFirstMoverDominance` [0.42, 0.58] @ n=1000 통과.

### M3-D. 치명타 상한 도달 불가 (이슈 #5) — *우선순위 낮음, 백로그*
- `Crit% = 5+(ATK−DEF)×0.05, clamp 2~20` — 스탯 범위(1~150)상 상한 20%는 ATK−DEF=300 필요. 도달 불가.
- Phase 2 감정 보정(TriggerProbMod)이 치명타에 기여하기 시작할 때 같이 재설계.

---

## M3.5 직렬화 — M4와 Phase 3의 공용 기반 (문서[1] 6장 의무) ✅

- **`MatchSerializer`** (Sim/Serialization): `MatchRecord{schemaVer, seed, result, events}` ↔ JSON.
  SimEvent 9종 폴리모피즘은 도메인 속성 오염 없이 System.Text.Json 계약 수정자로 격리(타입 판별자 "type",
  enum은 문자열). `schemaVer`는 처음부터 포함 — 포맷 진화해도 과거 기록을 읽는다.
- **CLI**: `export [매치업] [시드] [경로]` = 경기 1판 JSON(뷰어·역사 DB 입력, 매치업 어휘는 replay와 공유).
- **명경기 자동 태깅**: `highlights [N] [경로]` → `highlights.json`. comeback(승자 HP≤10% 사선 역전) +
  taunt_reversal(도발자 패배, M3-B 산물). 30%는 공격적 매치업서 90%+가 걸려 선별력이 없어 사선 기준으로 큐레이션.
  → 영속 세계의 "역사 보존"이 시드 목록 하나로 가벼워진다 (원칙 B의 보상).
- **합격:** `SerializationTests` 4종 — 같은 시드 = 바이트 동일 JSON(결정론 회귀), 라운드트립 재직렬화 동일,
  schemaVer 포함, 폴리모픽 이벤트 필드(float 라운드트립) 보존. 전체 테스트 54개 통과.

---

## M4 프레젠테이션 — "10분 봐도 재밌는가" (기획시안 최종 목표의 1차 판정)

### M4-0. 기술 결정 (착수 전 결정 포인트 — 라니스타 결재 필요)
두 갈래가 있고, **순서를 나눠 둘 다 취하는 것을 추천:**
1. **✅ M4-a 디버그 2D 뷰어 — 완료.** `viewer.html`(단일 파일, 의존성 0) + `viewer` CLI 모드.
   - **위치 트랙 발견·해결:** SimEvent엔 좌표가 없다(이산 "무슨 일"). 막대기의 전진/후퇴(전술 정체성의
     핵심)를 그리려면 위치가 필요해, 연속 상태 프레임 트랙 `ReplayFrame`(15Hz 샘플)을 **SimEvent와 분리**해
     추가. MatchRecord(schemaVer=1, 바이트 동일 테스트)는 안 건드림 — 격투게임 리플레이의 표준(이산 이벤트
     + 연속 상태) 모델. 뷰어 봉투 `ViewerExport`{meta, frames, events, result}는 별도 프레젠테이션 투영.
   - **뷰어 기능:** 위치 보간 막대기 2개(자세별 포즈 — 가드/경직/다운/도발/공격), HP·스태미나 바,
     사거리 위스커(간격 읽기), 타격·자세붕괴·다운 팝업, 🧠 Decision 핀, 재생/스크럽/배속(0.25~4×).
     스크럽해도 t로부터 전 상태 재계산(무상태) → 어느 지점이든 정확. `dotnet run -- viewer [매치업] [시드]`.
   - **검증:** 신규 테스트 2개(프레임 단조·아레나 내 / 프레임 수집이 결정론 불변). 전체 56개 통과.
     육안: 버서커(도끼·난전·충동) vs 전술가(창·카운터·냉철)에서 사거리·간격·판단이 시각적으로 구분됨.
   - **육안 baseline 판정(1D 현 상태, 2026-06-13):** 문서[3] 9장 4항목 대비 —
     ① 압박+잔혹 vs 압박+겁쟁이: 잔혹 63% 승(행동 차이 O), 그러나 양쪽 99.8% 사선 난타전 = 질감 동일 △.
     ② 버서커 vs 전술가: 창 카운터 "그림"은 나옴(카운터 9건, 적중 18:10 우세)이나 **평균 간격 0.92m**
        (창 사거리 2.6m), 벽에 몰려 패배(전술가 37%, 목표 55~60% 역전) ✗.
     ③ 도발 역전 KO: 18.5%×7.6% = 1.4회/100 (목표 2~5) △.
     ④ 변화감: 공격형=사선 난타전 / 수동형=180초 무KO 대치, 양극단·중간 부재 ✗.
   - **결론 = B 정당화:** 3계층 AI는 승률 차이를 만들지만(결정 수준 O), **1D가 공간 전술을 붕괴**.
     기하학적 근본: *같은 이동속도+직선 = 카이팅 불가*(후퇴는 간격 유지, 공격은 추격 허용) → 16m
     아레나서 평균 0.92m로 뭉침, 창의 사거리는 죽은 무게. zoner/counter 정체성이 실행 불가 = 밋밋함의
     원인. B(2D 선회)가 튜닝 아닌 구조로 푸는 지점. baseline 확보로 B 전후 비교 기준 마련.
   - 별개 백로그: 사선 난타전·도발 절대빈도는 1D 무관한 밸런스 튜닝(공격/방어). B 후 별도 패스.
2. **M4-b 본 프레젠테이션:** 검증된 연출을 게임 엔진으로. 기획시안 14장(2.5D, 시각 70%)과
   추후.odt의 AI 스프라이트 파이프라인(나노바나나 시트 → 배경 제거 → 좌표 추출)이 입력.
   엔진(Unity 2D vs 웹 확장)은 M4-a 결과를 보고 결정 — 문서[1]의 Unity 전제는 M4-b 시점에 재확인.
- **합격 (M4 전체):** 문서[3] 9장 4항목 육안 통과 — 특히 "같은 전술, 다른 성격이 다른 선수로 보이는가".

### M4-b-bal. disc 점착 밸런스 재설계 (→ `09_disc밸런스재설계_설계서.md`) — ✅ **종결 (2026-06-17)**
- **결론(doc[9] §7):** 5레버 통합 패스는 bistability(셀이 절벽 — 국소 튜닝으로 수렴 불가)로 **착수 없이 폐기.**
  이동 레버(2026-06-16)·추격 반응 지연(2026-06-17) 전부 반증 — 동속 락스텝은 시뮬로 못 고침(연출 백로그).
- **★ 돌파 — 인내심(Patience):** 거울전(사거리 우위 무기끼리)의 **영원 대치**(reachAdvantage 부재 → 무승부
  100%, baseline부터 있던 기존 이슈·sigmatrix 미검출) 발견 → 무교전 시 인내심 소모→공격 충동(Aggression 연동
  =전술·성격·특성 자동)으로 해소. **매트릭스 스프레드 53→29폭 축소**(견제 32.9→48.2↑) — 절벽을 구조적
  메커니즘이 일부 수렴시킨 첫 사례. + 비대칭 disc 충돌(가만히 선 쪽 안 밀림). 커밋 `9422c40`.
- **백로그:** 난전vs카운터 11→98 극단 튜닝(`PatienceImpulseScale`/`damp`), 5레버 미련.
- **발견:** M4-a가 진단한 "동속+직선=카이팅 불가"를 **B(2D disc)가 구조로 풀 것으로 기대했으나, 실측 결과 못 풂.**
  disc는 붕괴 위치만 이동(통과·교환 → 0.8m 점착 고정). 동속 점착은 2D에서도 잔존.
- **현 균형은 취약 평형:** `strafe baseline 0.10`이 러셔의 망설임을 인위 주입해 지탱(3회 실측 — strafe 빈도를
  낮추면 매트릭스 전 붕괴, 쌍검 100%). 유저 보고 "strafe 과빈도"는 이 crutch의 증상이라 strafe 단독 너프 불가.
- **재설계 = 5레버 통합 패스:** ①카이터 속도차등 ②후퇴 벽회피 ③점착 교환비용(whiff 우회 보전) ④벽 처리
  ⑤템포 강건성→게임 슬로다운. 개별 튜닝은 편향을 재매핑할 뿐(M3-C 교훈) → 묶어서 매트릭스 동시 검증.
  합격: 각 전술 40~60·반격부재 0.
- **레버 5(라니스타 가독성):** "게임이 빠르고 정신없다" → 템포를 늦추고 행동 수를 줄이려 했으나, `DecisionTickSec`을
  0.3만 늦춰도 반응형(카운터·견제)이 죽고 쌍검 100%(M4-b 실측). 빠른 템포는 반응형 AI+disc 스페이싱과 한 몸 →
  레버 1~4로 밸런스가 템포에 강건해진 뒤에야 슬로다운 가능. viewer 배속 꼼수는 반증(연출 아닌 sim이어야).
- 별개였던 "사선 난타·도발 빈도" 백로그(M4-a 116행)와 통합 — 둘 다 disc 후 밸런스 패스 대상.

### M4-c. 문서[7] UI/UX 바인딩 정의서 작성
- 문서[1] 4.1 이벤트 표가 입력 명세. T08_ReasonTags(아이콘/오라/대사 풀) 확정.
- M4-a에서 실제로 그려보며 작성하는 게 탁상 설계보다 빠르다 — M4-a와 병행.

---

## Phase 2 — 특성/감정/관계 ("억까가 아니라 서사로 느껴지는가")

설계 검증 질문(문서[1] 5장): *"이 기능을 넣을 때 Sim 기존 파일을 고쳐야 하는가?"* — 답이 No여야 함.
인터페이스는 이미 예약돼 있다 (EmotionMult 자리, OppIsNemesis enum, T09~T11 스키마).

1. **T10 감정:** ✅ **엔진 구현 (2026-06-26, `EmotionData.cs`).** 경기 결과 → 감정 8종 생성(승/패/KO패 × 성격 해석:
   자신감/자만/부담감·열등감/동기부여/좌절·트라우마/원한). **★ 의사선택만 영향**(라니스타 확정 — 데미지·스탯 배율 무관;
   당초 "EmotionMult 주입"은 폐기, 트리거 확률+Directive 결정 가중치 경로로). `emotionprobe`로 검증, 테스트 +5. → 현황 §7.5.
   **잔여:** 영속 이월(Phase 3)·성격 변화 입력(Phase 4)·수치 밸런스 패스.
2. **T09 특성:** ✅ 구현 (14종, `TraitData.cs`/`TraitGen`). *AgingMod/GrowthMod(천재·저속노화)는 나이 시스템 도입 시.*
3. **T11 관계:** ✅ **구현 (2026-06-29, `RelationData.cs`).** 7종 전부 + 누적 원장(`RelationLedger`).
   감정과 차별 = **메타 영속 그래프 + 특정 상대 전용 트리거 게이트**(OppIsNemesis/Rival/Feared) + 경기 외적 메타(복수전·라이벌리). decision-only.
   `relations` 데모로 관계 그래프 창발 실증, 테스트 +5. **잔여:** 영속 저장·자동 매치메이킹·명성(Phase 3)·성격 변화(Phase 4).
4. **기억 최소판:** 경기 결과 로그 → 감정/관계 갱신의 입력. (Phase 3 역사 DB의 전신)
5. **스킬·계급 시스템** (라니스타 발의, → `06_스킬계급시스템_설계초안.md`): 행동 다양성(#2)의 대안/보강.
   액티브=무기·패시브=성격 결합, 슬롯 경제로 "계급≠비례파워" 보장. **특성(T09)과 엔진 통합**이 선결.
   엔진은 Phase 2, 획득(계급)은 Phase 3 리그 연동. 예약 스키마 T12_Skills/T13_RankTiers.
- **합격:** 동일 매치업에서 감정/관계 유무로 승률·행동 분포가 *측정 가능하게* 갈리고,
  배치 리포트에 그 차이가 잡힌다. + 도발 역전·복수전 같은 "서사 이벤트" 발생률이 목표 범위.

## Phase 3 — 리그/역사 (창발 서사의 무대)

1. **문서[2] 역사 DB ERD 작성** (착수 조건 — 현재 문서 없음). MatchResult JSON(M3.5)이 적재 입력.
2. 리그 스케줄러 + 랭킹/전적 + 시즌 진행 (시뮬레이션은 Headless 코어 재사용 — 문서[1] "백그라운드 리그"가 공짜).
3. **매치메이킹 권한** (기획시안 핵심 디벨롭 포인트 2): 플레이어가 대진표를 짜는 "간접 개입"의 1호 기능.
4. 인기 시스템 기본형 (연승/이변/도발이 인기 증감 → 이벤트 매치 선정 가중).

## Phase 4 — 혈통/감독 (기획시안 13장)
성장 곡선(유년→신인→전성기→노년), 노화의 기계적 의미(RCT 하락 → 같은 카운터 프로파일이 늦는다, 문서[3] 6.3),
성격 변화 3대 트리거(세월/트라우마/영광 — 기획시안), 은퇴/명예의 전당, 혈통 계승, 감독 모드.

---

## 문서 거버넌스

| 문서 | 상태 | 조치 |
|---|---|---|
| **[현황] MORITURI_현황.md** | **신규 (2026-06-26)** | 코드와 항상 동기화 — 현재 상태 단일 진입점 |
| [0] 로드맵 (이 문서) | 결정 로그 | 마일스톤 종료마다 갱신 |
| [1] 아키텍처 | 최신 | M4-b에서 Unity 전제만 재확인 |
| [2] 역사 DB ERD | **없음** | Phase 3 착수 조건으로 작성 |
| [3] AI 행동트리 | **2026-06-26 동기화** (성격 10종·전술수치·지터·인내심) | — |
| [4] 전투 공식 | **2026-06-26 동기화** (무기표·출혈·패링·카이팅·2D) | — |
| [5] 데이터 테이블 | **2026-06-26 동기화** (T01 신규컬럼·T04 enum·T06 상수군·T09 구현) | CSV 임포터는 백로그 (아래) |
| [6] 스킬·계급 | 설계초안 0.1 | Phase 2 착수 시 확정 |
| [7] 스킬 카탈로그 | 특성만 구현, 스킬 Phase 2 | M4-c UI/UX 바인딩 문서는 별도(없음) |
| [8] 게임방향·비전 | 설계노트 (B 결정 로그) | Fight Pits/TFM 비전 + B 완료. Q2 결정 대기 |
| [9] disc 밸런스 재설계 | **종결 (2026-06-17, §7)** | 5레버 폐기(bistability), 인내심(Patience)으로 영원 대치 해소·부분 수렴 |
| [10] 관중 시스템 | 인매치 일부 구현 (2026-06-22) | Phase 3 Fame 훅 예약 |
| [11] 천부·잠재력 | StatGen 구현 | 계급·성장은 Phase 2~3 |
| 기획시안 | 유효 | 변경 없음 — 모든 단계의 북극성 |

## 백로그 (당장 안 함, 잊지도 않음)
- CSV 데이터 파이프라인 (문서[5] 1장): 현재 C# 정적 테이블로 충분. 시트 튜닝이 실제로 필요해지는
  시점(상성 매트릭스 25칸 동시 튜닝이 고통스러워질 때)에 착수.
- 치명타 상한 재설계 (M3-D → Phase 2와 병합).
- NUnit 전환 (README 절차 있음): CI 붙일 때.
- 관중/미디어 시스템 (기획시안 "The Juice"): Phase 3 인기 시스템의 확장으로.
- 부상/트라우마 (기획시안 제안): Phase 2 기억 시스템의 확장으로.

## 권장 다음 작업
**M3-A (상성 매트릭스)** — 이유: ① Phase 1의 존재 이유("상성 수싸움")를 처음으로 전면 검증
② 도발(M3-B)·편향(M3-C) 수정의 효과 측정 인프라(CSV 리포트)를 겸함 ③ 실패 칸이 나와도
리플레이 뷰어로 즉시 원인 추적 가능 (M3 밸런스 작업으로 방법론 확립됨).
