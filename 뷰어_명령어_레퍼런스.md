# MORITURI 뷰어 명령어 레퍼런스

> 경기를 viewer.json으로 내보내고 브라우저로 재생하는 명령어 모음.
> **이 문서는 무기/전술/성격/특성 종류나 명령 문법이 바뀌면 항상 갱신할 것.** (최종 동기화: 2026-06-26 — 전술 선호거리 결단/도박/방어 교정)

---

## 1. 기본 명령

```
cd C:\Users\user\Desktop\morituri\Morituri\Morituri.Headless
dotnet run -c Release -- viewer [매치업] [경기시드] [천부시드A] [천부시드B]
```

- 실행하면 viewer.json 생성 + 로컬 서버 기동 → 브라우저에서 **http://localhost:5173/viewer.html**
- `[경기시드]` 생략 시 1. `[천부시드A/B]` 생략 시 천부·스탯 패널 없음(천부시드 주면 그 시드로 스탯 추첨해 표시, B 생략 시 A시드 재사용).
- 이미 서버가 떠 있으면 exe 잠금 에러 → 기존 프로세스 종료 후 재실행:
  ```
  taskkill /IM Morituri.Headless.exe /F 2>nul
  ```

---

## 2. 매치업 지정 — `b:` 문법 (주력)

```
b:무기/전술/성격[/특성+특성][/감정+감정][/관계]:...
```

- `:` 로 **A·B 두 선수** 구분. **B 생략 시 A의 거울전.**
- 각 선수 = `무기/전술/성격` (슬래시 구분), 4번째=`특성`(`+`), 5번째=`감정`(`+`), 6번째=`관계`(상대를 향한 관계 1종).
- **빈 필드는 기본값:** 무기 `SWORD` / 전술 `BALANCED` / 성격 `CALM`. (특성·감정·관계 생략 시 없음.)
- 대소문자 무관(자동 대문자화). 잘못된 특성 ID는 무시됨.
- 따옴표로 감싸기(`"b:..."`) — `/`·`+` 때문에.

**예시:**
```
dotnet run -c Release -- viewer "b:SWORD/BALANCED/CALM:SWORD/BALANCED/CALM" 7   # 균형형 검 거울
dotnet run -c Release -- viewer "b:SHIELD/DEFENDER/CALM:SWORD/PRESSURE/CALM" 7  # 방패 방어 vs 검 압박
dotnet run -c Release -- viewer "b:WHIP/ZONER/CALM:AXE/BRAWLER/CRUEL" 3         # 채찍 견제 vs 도끼 난전
dotnet run -c Release -- viewer "b:GREATSWORD/PRESSURE/CALM/GIANT:GREATSWORD/PRESSURE/CALM" 7  # 거인 대검 vs 일반 대검
dotnet run -c Release -- viewer "b:SHIELD/DEFENDER/CALM/STONEWALL+TOUGHHIDE:AXE/BRAWLER/CRUEL" 1  # 봉쇄자+질긴가죽 방패 (특성 2개)
dotnet run -c Release -- viewer "b:/PRESSURE/ARROGANT" 5                        # 빈 무기=검, 압박·오만 vs 거울
dotnet run -c Release -- viewer "b:SPEAR/COUNTER/CALM:AXE/BRAWLER/CRUEL" 7 1001 2002  # + 천부시드
```

---

## 3. 무기 (8종) — `WPN_` 접두 자동, 아래 짧은 이름만 입력

| 입력값 | 이름 | 사거리 | 정체성 |
|---|---|---|---|
| `SWORD` | 검 | 2.4 | 균형형 |
| `SPEAR` | 창 | 3.9 | 거리유지·카운터(카이터) |
| `AXE` | 도끼 | 2.1 | 출혈·버스트(하이퍼아머 없음) |
| `GREATSWORD` | 대검 | 3.0 | 강력한 일격·느림(하이퍼아머) |
| `DUALBLADES` | 쌍검 | 1.65 | 근접 난전(2타·빠름) |
| `HAMMER` | 망치 | 1.95 | 높은 경직(하이퍼아머) |
| `WHIP` | 채찍 | 4.5 | 중거리 견제·가드우회(카이터) |
| `SHIELD` | 방패 | 2.25 | 방어·CC·패링(가드보너스) |

*(사거리는 현재 ×1.5 스케일 적용값. CollisionRadius 0.6 → 최소 간격 1.2m.)*

## 4. 전술 (10종) — `TAC_` 접두 자동

| 입력값 | 이름 | 선호거리 | 비고 |
|---|---|---|---|
| `BALANCED` | 균형 | 2.4 | 범용 (기본값) |
| `PRESSURE` | 압박 | 1.5 | 근접 공격 압박 |
| `COUNTER` | 카운터 | 3.0 | 거리 유지·반격(창 시그니처) |
| `ZONER` | 견제 | 4.2 | 최장거리 순수 견제(채찍 시그니처) |
| `BRAWLER` | 난전 | 1.05 | 근접 난전(쌍검·도끼 시그니처) |
| `DEFENDER` | 방어 | 2.1 | 가드 중심(방패 시그니처) |
| `DECISION` | 결단 | 3.3 | — |
| `HUNTER` | 사냥꾼 | 2.4 | 상처 입은 사냥감 추적 |
| `GAMBLER` | 도박 | 1.8 | 고위험 |
| `EVADER` | 회피 | 3.75 | 회피 중심 |

*(선호거리도 ×1.5 스케일 적용값.)*

## 5. 성격 (10종) — `PER_` 접두 자동

| 입력값 | 이름 | 핵심 |
|---|---|---|
| `CALM` | 냉철 | 도발 면역적·트리거 확률↓ (기본값) |
| `RECKLESS` | 충동 | 피격 시 분노·도발 |
| `ARROGANT` | 오만 | 우세 시 도발(역전 위험) |
| `HONORABLE` | 고결 | 다운 상대 추가타 자제 |
| `COWARD` | 겁쟁이 | 강공 윈드업 회피·위축 |
| `SHOWMAN` | 쇼맨 | 관중 호응 |
| `OPPORTUNIST` | 기회주의자 | 허점 포착 |
| `CRUEL` | 잔혹 | 취약 상대 공격성↑·도발 |
| `BOLD` | 대담 | 열세·시간부족 시 분발 |
| `WARY` | 신중 | 방어 편향·신중 |

## 6. 특성 (14종) — `TRT_` 접두 자동, 4번째 필드에 `+`로 여러 개

| 입력값 | 이름 | 효과 |
|---|---|---|
| `TOUGHHIDE` | 질긴가죽 | 받피 −12%·이속 −8% |
| `SPRING` | 마르지않는샘 | 스태미나max +25%·회복 +20% |
| `FLEET` | 민첩 | 이속 +12%·회피 +0.1s·HP −10% |
| `STAND` | 불굴 | 포이즈 +30% |
| `STONEWALL` | 봉쇄자 | 가드 시 받피 ×0.5 |
| `GIANT` | 거인 | HP +25%·사거리 ×1.25·크기 ×1.3 |
| `BRITTLE` | 유리몸 | 받피 +%(순수 페널티) |
| `FRAIL` | 허약체질 | 스태미나max·회복 −%(페널티) |
| `DULL` | 둔감 | 받피 −%·인지지연 페널티(트레이드오프) |
| `GLASSJAW` | 유리턱 | 포이즈 −40%(기절 잘 걸림) |
| `ZOMBIE` | 좀비 | 이속 −15%, HP≤30% 시 디버프 면역 |
| `CATCHBREATH` | 숨고르기 | 스태미나 ≥80% 시 모든 공격 치명타 |
| `LIGHTFOOT` | 초상비 | 대시 ST소모 −50%·대시 후 1s 이속↑ |
| `FIRSTBLOOD` | 선취점 | 첫 클린 히트 ×1.25 + 흡수 쉴드 |

**상반 배타(동시 부여 불가):** 유리몸 ⊗ 질긴가죽 · 유리몸 ⊗ 둔감 · 허약체질 ⊗ 마르지않는샘.
*(실제 게임에선 생성 시 자동 부여 — 1개/20%→2개/5%→3개. b: 빌더에선 수동 지정 가능.)*

### 6.5 패시브 스킬 (T12 MVP) — `SKL_` 접두를 **직접 붙여** 4번째 필드에 (특성과 혼용 가능)

스킬은 장착형 특성(같은 엔진). b:에선 A/B 검증용으로 수동 주입: `b:SWORD/BALANCED/CALM/SKL_READ2:...`
(접두 없는 토큰은 `TRT_` 자동 — 스킬은 반드시 `SKL_` 명시.)

| 입력값 | 이름 | 성격 게이트 | 효과 |
|---|---|---|---|
| `SKL_READ` | 간파 | 냉철 | 인지지연 −0.04s |
| `SKL_RUSH` | 저돌 | 충동 | 이속 +6% |
| `SKL_LEISURE` | 여유 | 오만 | 스태미나 회복 +15% |
| `SKL_AEGIS` | 수호자 | 고결 | 가드 받피 −20% |
| `SKL_SURVIVE` | 생존술 | 겁쟁이 | 받피 −4%·회피 소모 −15% |
| `SKL_VIGOR` | 살육의 활력 | 잔혹 | 스태미나 최대 +10% |
| `SKL_NERVE` | 배짱 | 대담 | 포이즈 +12% |
| `SKL_ECONOMY` | 절제 | 신중 | 회피 소모 −30% |
| `SKL_FLAIR` | 무대 장악 | 쇼맨 | 이속 +4%·간격 +0.08 |
| `SKL_ANGLE` | 노림수 | 기회주의 | 인지 −0.03s·이속 +3% |
| `SKL_READ2` | 전장 분석 Ⅱ | 냉철·집정관+ | 인지지연 −0.08s |
| `SKL_BULWARK` | 불괴 Ⅱ | 고결·집정관+ | 가드 받피 −35%·이속 −4% |

*(인게임 습득: 루두스 카드 「📖 수련」 — 훈련 3pt, 슬롯 = 챔피언+ 2 / 그외 1, 교체 가능. 미장착 시 매트릭스 무영향.)*

## 6.5 감정 (7종) — `EMO_` 접두 자동, 5번째 필드에 `+`로 여러 개

> 감정은 **일시적 심리 상태**로 **의사선택만** 바꾼다(데미지·스탯 배율 무관). 실제 게임에선 경기 결과로 자동 생성, b:에선 수동 주입.
> **원한(GRUDGE)은 감정이 아니라 관계다** — §6.6 `NEMESIS`(원수) 참조. KO패한 공격형은 무감정이고, 대신 그 상대에게 원한을 품는다.

| 입력값 | 이름 | 생성(성격) | 효과(의사선택) |
|---|---|---|---|
| `CONFIDENT` | 자신감 | 승리(일반) | 공격성↑·결단↑ |
| `HUBRIS` | 자만 | 승리(오만/쇼맨 압승) | 방심(가드↓·도발↑·커밋↑) |
| `PRESSURE` | 부담감 | 승리(겁쟁이/신중) | 위축(공격성↓·가드↑) |
| `INFERIOR` | 열등감 | 패배(겁쟁이/신중) | 위축(공격성↓·거리↑·가드↑) |
| `MOTIVATED` | 동기부여 | 패배(대담/고결/기회) | 분발(결단↑·반격창↑) |
| `FRUSTRATED` | 좌절 | 패배(충동/잔혹/오만) | 산만(커밋↓·가드↓·도발↑) |
| `TRAUMA` | 트라우마 | KO패(겁쟁이 등 내면화형) | 강한 공포(공격성↓↓·거리↑↑·가드↑) |

예: `dotnet run -c Release -- viewer "b:SWORD/PRESSURE/RECKLESS//TRAUMA:SWORD/PRESSURE/CALM" 7` (A=트라우마 주입; 빈 4번째=특성 없음, 5번째=감정)

## 6.6 관계 (7종) — 6번째 필드, 상대를 향한 관계 1종 (`NEMESIS` 등 영문)

> 관계는 **특정 상대를 향한 누적 유대/적대**. 실제론 RelationLedger가 경기 누적으로 형성, b:에선 수동 주입. 데미지 무관(의사선택·트리거 게이트만).

| 입력값 | 이름 | 효과(인매치 게이트) |
|---|---|---|
| `NEMESIS` | 원수(=원한) | 복수심(공격성·리스크↑) + 복수 도발(VENGE). KO패한 공격형이 그 상대에게 품는 원한이 여기로 발화 |
| `FEAR` | 공포(천적) | 위축·회피·거리↑ + 움찔 회피(DREAD) |
| `ENVY` | 질투 | 과공격·강공 |
| `OBSESSION` | 집착 | 추격·반복공격 |
| `RIVAL` | 라이벌 | 집중·결단(방심 면역 경향) |
| `RESPECT` | 존경 | 정정당당(가벼움) |
| `FRIEND` | 친구 | 봐주기(공격성↓) |

예: `dotnet run -c Release -- viewer "b:SWORD/PRESSURE/WARY///NEMESIS:SWORD/PRESSURE/WARY" 3` (A가 B를 원수로 — 빈 4·5번째, 6번째=NEMESIS)

배치 데모: `dotnet run -c Release -- relations 20` (라운드로빈 누적 관계 그래프·복수전 후보)

---

## 7. 기타 매치업 프리픽스 (진단용)

| 프리픽스 | 형식 | 용도 |
|---|---|---|
| `b:` | `b:무기/전술/성격[/특성]:...` | **빌드 직접 지정 (주력)** |
| `t:` | `t:전술:전술` | 전술 매트릭스(검+냉철 고정) |
| `w:` | `w:무기:무기` | 무기 진단(균형형+냉철 고정) |
| `s:` | `s:무기:무기` | 시그니처 빌드(각 무기 제 전술) |
| (이름) | `mirror`·`cruel`·`arrogant`·`cruelanvil` | 사전 정의 매치 |
| (없음) | (기본) | 버서커 vs 전술가 |

예: `dotnet run -c Release -- viewer s:WHIP:AXE 3` (채찍 시그니처 vs 도끼 시그니처)

---

## 8. 분석용 배치 명령 (참고)

```
dotnet run -c Release -- 300              # 배치(거울·버서커 등 핵심 매치 통계)
dotnet run -c Release -- sigmatrix 300    # 5전술 시그니처 매트릭스
dotnet run -c Release -- buildmatrix 300  # 8무기 시그니처 8×8 승률
dotnet run -c Release -- weaponbalance 300 # 8무기 균형전술 8×8
dotnet run -c Release -- spacingprobe 20  # 무기별 평균 간격
dotnet run -c Release -- parryprobe 200   # 방패 패링 성공률
dotnet run -c Release -- replay "b:..." 7  # 텍스트 중계(브라우저 X)
dotnet run -c Release -- emotiongen 40     # 감정(T10) 발생률 점검
dotnet run -c Release -- relations 20      # 관계(T11) 누적 관계 그래프
dotnet run -c Release -- season 6          # Phase 3 시즌: 순위·복수극·챔피언 창발 (텍스트)
dotnet run -c Release -- season 6 1 serve  # + season.json 내보내고 대시보드 서버 → localhost:5173/league.html
```
*(league.html = 시즌 대시보드: 순위·명성 리더보드·관계 그래프(SVG)·이벤트 매치·서사. season.json을 읽음.)*
