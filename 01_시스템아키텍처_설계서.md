# MORITURI — [1] 시스템 아키텍처 설계서
**버전:** 0.1
**범위:** 전체 프로젝트의 모듈 구조와 의존 규칙. Phase 1 구현 대상은 ★ 표시, 나머지는 인터페이스만 예약.

---

## 1. 최상위 원칙 3가지

### 원칙 A — 시뮬레이션 코어와 렌더링의 완전 분리 ★
전투 로직(문서[3]+[4])은 **UnityEngine에 의존하지 않는 순수 C# 라이브러리**로 작성한다.

이유:
1. **헤드리스 배치 시뮬레이터**(문서[4] 12장)가 같은 코드를 렌더링 없이 1,000배속으로 돌려야 한다. 로직이 MonoBehaviour에 붙어 있으면 이게 불가능하다.
2. 밸런스 튜닝 = 코어만 다시 돌리기. 그래픽 작업과 밸런스 작업이 서로를 막지 않는다.
3. 추후 "백그라운드 리그 진행"(다른 경기가 화면 밖에서 동시 진행)이 공짜로 따라온다 — Phase 3 리그 운영의 기술적 기반.

### 원칙 B — 결정론적 시뮬레이션 ★
같은 (시드, 선수 데이터, 무기 데이터) 입력이면 항상 같은 경기 결과가 나와야 한다.

- 고정 Tick: 시뮬레이션 60Hz 고정 스텝. `Time.deltaTime` 사용 금지.
- 단일 RNG: 경기당 시드 하나에서 파생된 `SimRandom` 인스턴스만 사용. `UnityEngine.Random` 금지.
- 효과: 버그 재현(시드만 기록하면 됨), 리플레이 시스템이 "시드 + 입력 데이터" 저장만으로 구현됨, 배치 테스트 재현성 확보.
- **리플레이가 곧 하이라이트 시스템의 기반**이다. 명경기를 통째로 저장하는 게 아니라 시드만 저장하면 된다 — 영속 세계의 "역사 보존"이 가벼워진다.

### 원칙 C — 데이터 주도 설계 ★
전술/성격/무기/특성의 수치와 규칙은 코드가 아닌 데이터 테이블(문서[5])에 있다. 코드는 "테이블을 해석하는 엔진"만 담당한다. 신규 무기 추가 = 행 추가, 컴파일 불필요.

---

## 2. 모듈(어셈블리) 구조

```
MORITURI.sln
│
├── Morituri.Sim            ★ 순수 C#, UnityEngine 참조 금지
│   ├── Core/               Tick 루프, SimRandom, SimClock
│   ├── Combat/             CombatMath(문서[4]), 히트박스, 상태이상
│   ├── Agent/              Blackboard, 전략층, Utility, FSM(문서[3])
│   ├── Data/               POD 구조체 (WeaponDef, TacticsDef, ...)
│   └── Events/             SimEvent 정의 + EventBuffer
│
├── Morituri.Sim.Tests      ★ 단위 테스트 (수식 검증)
│
├── Morituri.Headless       ★ CLI 배치 러너 (콘솔 앱)
│   └── 매치업 × N경기 실행 → matchup_report.csv
│
├── Morituri.Unity          (Unity 프로젝트)
│   ├── Bridge/          ★  Sim 구동 + 이벤트 → 프레젠테이션 변환
│   ├── Presentation/    ★  캐릭터 뷰, 이펙트, 카메라, 관중
│   ├── UI/                 HUD, 아이콘, 말풍선 (문서[7] 대상)
│   ├── DataAuthoring/   ★  ScriptableObject ↔ Sim POD 변환기
│   └── Meta/               리그, 역사DB, 세대교체 (Phase 3~4)
│
└── Tools/                  데이터 임포터 (CSV → SO), 밸런스 리포트 뷰어
```

**의존 방향 (절대 규칙):** `Unity → Sim` 단방향. Sim은 Unity의 존재를 모른다. 어셈블리 정의 파일(asmdef)로 컴파일러 수준에서 강제한다.

---

## 3. ECS 채택 여부에 대한 결정

**결론: Phase 1~2에서 Unity DOTS/ECS를 쓰지 않는다.** 솔직히 말하면 기획 문서에 언급된 ECS는 이 게임의 병목과 맞지 않아.

- ECS가 이기는 상황은 "동일한 로직을 수천 개 엔티티에 적용"할 때다. MORITURI의 전투는 1v1, 화면 위 에이전트 2명이다. 복잡한 건 *개체 수*가 아니라 *개체당 의사결정 로직*이고, 이건 ECS가 오히려 작성하기 고통스러운 영역이다.
- 순수 C# Sim 코어(원칙 A)가 이미 ECS의 실익(테스트 용이성, 렌더링 분리, 멀티스레드 배치 실행)을 다 제공한다. Headless 러너에서 경기들을 `Parallel.For`로 돌리면 끝.
- **예외 후보:** 관중 시뮬레이션(수천 명 군중 애니메이션)과 Phase 3의 "백그라운드 리그 동시 진행"은 ECS 또는 Job System이 어울린다. 그때 Presentation 안에서만 부분 도입하면 되고, Sim 코어는 영향받지 않는다.

---

## 4. 런타임 데이터 흐름 ★

```
        [DataAuthoring: SO 테이블]
                  │ Bake (POD 변환)
                  ▼
┌─────────────  Morituri.Sim  ─────────────┐
│  MatchConfig(시드, 선수A/B, 무기, 경기장)    │
│        │                                  │
│        ▼   60Hz 고정 스텝                  │
│  SimLoop.Step()                           │
│   ├ 전략층 평가 (1~3s 주기)                 │
│   ├ 전술층 Utility (0.2s 주기)             │
│   ├ 실행 FSM + 이동/판정                    │
│   └ EventBuffer에 SimEvent 기록            │
│        │                                  │
│        ▼                                  │
│  SimEvent 스트림 + MatchResult             │
└────────────────┬──────────────────────────┘
                 │
     ┌───────────┴───────────────┐
     ▼ (Unity Bridge)            ▼ (Headless)
  프레젠테이션 재생             통계 집계 → CSV
  - 보간/애니메이션
  - DecisionEvent → UI 아이콘
  - 카메라 연출
```

**핵심 계약:** Sim은 매 Tick "무슨 일이 있었는지"를 이벤트로만 말한다. 그림을 어떻게 그릴지는 전적으로 Bridge의 일.

### 4.1 SimEvent 타입 (Phase 1 최소셋) ★

| 이벤트 | 페이로드 | 소비처 |
|---|---|---|
| `StateChanged` | fighterId, fromState, toState | 애니메이션 전환 |
| `AttackSwung` | fighterId, weaponMotionId | 모션 재생, 휙 소리 |
| `HitLanded` | attacker, defender, damage, isCrit, isCounter, isGuarded | 데미지 숫자, 히트 이펙트, HP바 |
| `PoiseBroken` / `GuardBroken` | fighterId | Stagger 연출, 관중 함성 |
| `Decision` | fighterId, reasonTag, layer, duration | **성격/감정 아이콘 (문서[3] 8장)** |
| `StaminaExhausted` | fighterId | 헐떡임 연출 |
| `MatchEnded` | 승자, 종료 사유, 판정 점수 | 결과 화면 |

이 표가 그대로 **UI/UX 바인딩 정의서[7]의 입력 명세**가 된다.

### 4.2 시간 동기화

- Sim Tick(60Hz 고정) ≠ 렌더 프레임. Bridge는 Sim을 0.5~2 Tick 앞서 돌리고 프레젠테이션은 보간 재생.
- 배속 관전(1x/2x/4x), 리플레이 되감기, "결과만 스킵"이 전부 이 구조에서 공짜: Sim을 더 빨리/미리 돌리면 된다.

---

## 5. Phase별 확장 지점 (지금은 인터페이스만)

| Phase | 추가 모듈 | 기존 코드 변경 범위 |
|---|---|---|
| 2 감정/관계/특성 | `Sim.Agent.Emotion`, 트리거 입력 확장 | 문서[4] 9장 변수에 값 주입만. 전투 코드 무변경 |
| 3 리그/역사 | `Unity.Meta.League`, 역사 DB(문서[2] ERD) | MatchResult를 구독하는 소비자 추가. Sim 무변경 |
| 4 혈통/감독 | `Meta.Lineage`, 선수 생성기 확장 | 선수 데이터 생성부만 |

설계 검증 질문: **"이 기능을 넣을 때 Morituri.Sim의 기존 파일을 고쳐야 하는가?"** 답이 Yes인 기능이 나오면 그때 아키텍처를 다시 본다.

---

## 6. 저장 전략 (Phase 1 범위)

- Phase 1은 영속 세계가 없다 — 저장 대상은 **데이터 테이블 + 배치 리포트 + 명경기 시드 목록**뿐.
- 단, MatchResult 직렬화 포맷(JSON)은 지금 확정한다. Phase 3 역사 DB(문서[2])가 이 포맷을 적재 입력으로 쓰기 때문. 스키마 버전 필드(`schemaVer`)를 처음부터 포함할 것.

---

## 7. Phase 1 구현 순서 제안 (마일스톤)

1. **M1 — 수식 검증:** `Morituri.Sim.Combat` + 단위 테스트. 무기 8종 데미지/경직/가드 수식이 문서[4]대로 동작.
2. **M2 — 막대기 전투:** Sim 코어 완성, Unity에서 캡슐 2개로 재생. 애니메이션 없이 FSM 상태를 색깔로 표시. ← **여기서 전술×성격 가독성 1차 판정**
3. **M3 — 헤드리스 러너:** 매치업 배치 실행 + CSV 리포트. 상성 매트릭스(문서[4] 11장) 1차 튜닝.
4. **M4 — 프레젠테이션:** 실제 모션/이펙트/Decision 아이콘. 문서[3] 9장 체크리스트 최종 판정.

M2와 M3 순서에 주의 — **그래픽을 입히기 전에 숫자로 먼저 재미(역전율, 행동 다양성)를 확인**하는 것이 이 아키텍처의 존재 이유다.
