# MORITURI — [12] 배포·클라이언트 로드맵 (v0.1)
**작성:** 2026-07-02. **상태:** 경로 결정됨(하이브리드). 착수 대기.
**한 줄:** 실제 게임 프로그램으로 배포한다. **웹 래핑으로 개발(빠른 반복·즉시 배포) → 최종은 Godot 4(C#)로 완성(폴리시·Steam).** `Morituri.Sim`(순수 C#)은 두 단계 모두 그대로 재사용.
**전제:** [1] 아키텍처(Sim/렌더 분리), [8] 비전(TFM형 2.5D 관전), 현황(구현 상태).

---

## 0. 결정 (라니스타 2026-07-02)

- **경로 = 하이브리드 2트랙:**
  1. **웹 래핑 개발 트랙(당분간):** 현재 C#(Sim + 시즌/월드 + 로컬 서버) + HTML/Canvas UI를 **데스크톱 앱으로 래핑**(Photino/Tauri) → 더블클릭 실행되는 프로그램으로 개발·배포하며 빠르게 반복.
  2. **Godot 4 완성 트랙(최종):** 릴리스 폴리시 단계에서 프레젠테이션을 **Godot 4(C#)** 로 이식해 완성. Sim은 그대로, 웹 뷰어는 개발/밸런싱 도구로 잔존.
- **불변 원칙:** `Morituri.Sim`은 클라이언트와 무관(원칙 A). 통합은 `SimEvent`/`ReplayFrame`/`MatchResult`/`world.json`/`season.json` 계약으로만. → 밸런싱은 계속 `Headless`로 헤드리스 수행.

**왜 하이브리드:** 이미 검증된 웹 프레젠테이션을 안 버리고, 며칠 만에 "실제 프로그램"을 확보(데모·피드백). 웹 캔버스 질감 상한에 부딪히거나 릴리스 폴리시가 필요할 때 Godot로 이주 — 그때도 Sim은 무변경이라 이주 비용이 렌더러/UI에 한정.

---

## 1. 통합 이음새 (이미 존재 — 재설계 불필요)

```
[Morituri.Sim]  결정론 엔진
   │ MatchSim.Run() → ReplayFrame(위치 15Hz) · SimEvent(사건) · MatchResult
   │ Season/RelationLedger → world.json(영속) · season.json(대시보드)
   ▼
[클라이언트]  웹(현재) → 래핑(.exe) → Godot(최종)
```
- 웹 뷰어(viewer.html)·대시보드(league.html)가 이미 이 계약을 소비 → **엔진 렌더러의 설계도**. Godot 이식 = "검증된 것 재구현".

---

## 2. 트랙 A — 웹 래핑 개발 (당분간, 게임 본체)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **W0 셸 스파이크** ✅ | Photino(C# 네이티브 웹뷰) 앱 1개 = Sim in-process + 로컬 UI 표시. 더블클릭 `.exe` | **완료 (2026-07-02, `Morituri.Client` → `Morituri.exe`).** WinExe(콘솔 없음)가 시즌을 in-process 실행→season.json→내장 서버→창에 league.html. 빌드·기동 확인. |
| **W1 화면 승격** ✅ | viewer.html/league.html을 *디버그 도구→게임 화면*으로: 메인메뉴·로스터·리그·경기 관전 | **완료 (2026-07-02).** `index.html` 게임 셸(메뉴 오버레이+리그/로스터/관전 탭) + 로컬 API(`/api/state·next·newcareer`) + `Game.cs` 상태 기계(구 Season 흡수 — 한 경기씩 진행). [다음 경기 ▶]→경기 실행→관전 자동 전환→리그 결과 반영, 브라우저 E2E 검증. |
| **W2 게임 루프** | 커리어 시작 → 시즌 진행 → (자동)매치메이킹 뷰 → 세이브/로드(world.json)·설정 | 처음~끝 한 바퀴 플레이 |
| **W3 웹 내 폴리시** | 캔버스 스프라이트·연출·오디오(WebAudio)·툴팁·로컬라이즈 | "봐줄 만한" 데모 질감 |
| **W4 배포(래핑)** | Photino 게시(win/mac/linux) + itch.io 데모 | 남이 설치해 실행 |

- **Photino 선택 이유:** 순수 .NET — Sim·시즌·서버를 **한 프로세스**에 담고 네이티브 웹뷰로 UI. 별도 백엔드/Electron 무게 없음. (Tauri는 Rust 셸+웹, 더 가볍지만 C# 백엔드와 IPC 필요.)
- 현재 `ViewerServer`(HttpListener)·`season.json`/`world.json`이 그대로 W1~W2의 데이터 공급.

---

## 3. 트랙 B — Godot 4 완성 (최종, 릴리스)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **G0 Sim 통합 스파이크** | Godot 4(C#) 프로젝트에서 `Morituri.Sim` 참조 → 경기 한 판 실행·로그 | 엔진 안에서 Sim 결정론대로 |
| **G1 전투 렌더러** | 웹 뷰어 패리티: 위치보간·자세·HP/스태미나·데시전 핀·관중 미터 | 웹과 동등 재생 |
| **G2 비주얼 업** | 스프라이트(9포즈, sprites/README)·2.5D 깊이·이펙트·사운드·카메라 | 비주얼 70% 목표 |
| **G3 메타 UI** | 리그·관계 그래프·명성·매치메이킹·세이브 이식 | 웹 대시보드 대체 |
| **G4 셸+배포** | 메뉴·설정·온보딩 + Steam(GodotSteam)·인스톨러·데모(Next Fest) | Steam 출시 가능 |

- 이주 시 **웹 뷰어는 유지**(개발·디버그·밸런싱 관전 — 엔진 렌더 버그를 웹과 대조).

---

## 4. 배포 현실

- **itch.io:** 무료·즉시 — 래핑 데모 1순위(트랙 A W4).
- **Steam:** 앱 등록 $100(1회) + 심사 + 스토어. SDK 연동(업적·클라우드세이브). 데모로 Next Fest — 주로 트랙 B에서.
- **코드사인(선택):** Windows SmartScreen 완화용 인증서(연 $100~400). 초기 생략 가능.
- **자동 업데이트:** Steam이 해결 / 독립 배포는 Velopack(.NET) 등.
- **크로스플랫폼:** Photino·Godot 모두 win/mac/linux. Sim은 순수 C#이라 무관.

---

## 5. 리스크 · 원칙

- **최대 리스크 = 스코프(메타 UI + 비주얼).** Sim은 끝났으나 W1~W3/G1~G3이 실제 시간의 대부분. 데모 스코프(1리그·1시즌·핵심 화면)로 먼저 자를 것.
- **계약 고정:** Sim↔클라를 `SimEvent`/`ReplayFrame`/`world.json`로만 — 그래야 밸런싱을 헤드리스로 계속(생산성 이점, 원칙 A).
- **웹→Godot 이주 비용은 렌더러/UI 한정** — Sim·밸런스·세이브 포맷은 재사용. 이주 시점은 "웹 질감 상한" 또는 "릴리스 폴리시 필요" 시.
- **문서·밸런싱 규율 유지:** 코드 바뀌면 [현황] 우선 갱신, `sigmatrix`/`season`으로 회귀 점검.

---

## 6. 다음 액션
1. ~~W0 Photino 셸 스파이크~~ ✅ — `Morituri.Client`(`Morituri.exe`, WinExe). (Photino.NET 복원 위해 `NuGet.config`에 nuget.org 추가 — Sim/Headless/Tests는 외부 패키지 0.)
2. ~~W1 화면 승격~~ ✅ **완료 (2026-07-02)** — 구조:
   - **`Game.cs`(Headless):** 구 Season.Run을 흡수한 상태 기계. `PlayNext()` 1경기 진행(감정/관계/명성 동일 로직) → viewer.json/season.json 갱신, 정규 소진→이벤트 빅매치→시즌 종료(world 저장)→다음 시즌 자동 개막. CLI `season`도 이걸 사용(동작 보존 확인).
   - **로컬 API(ViewerServer):** `/api/state`(상태) · `/api/next`(다음 경기) · `/api/newcareer`(초기화). http.sys 특성상 POST에 Content-Length 필수(빈 body 명시).
   - **`index.html`(게임 셸):** 메인메뉴 오버레이(계속하기/새 커리어) + 리그(league.html iframe)/로스터(선수 카드+관계)/관전(viewer.html iframe) 탭. [다음 경기 ▶] → 실행 → 관전 자동 전환(승자 비스포일) → 리그 복귀 시 결과 반영.
   - 시즌 페이스: 정규 1라운드로빈(28) + 이벤트 4 = 32경기/시즌.
   - **알려진 이슈(→W3):** viewer HUD가 로드 즉시 승자 표시(스포일러) — 재생 완료 전 숨김 처리 필요.
3. **W2 게임 루프(다음):** 시즌 넘김 연출·시즌 요약 화면, 미드시즌 세이브(스케줄 커서·감정 대기열 영속 — 현재 앱 재시작 시 시즌 처음부터), 설정. "처음~끝 한 바퀴" 완결.
4. Godot(G0)는 웹 트랙 성숙/폴리시 단계 진입 시.
> 현 상태에선 Phase 2/3 게임플레이·밸런스를 계속 확장해도 무방(클라와 독립). 배포 트랙은 이 문서 순서로 언제든 착수.
