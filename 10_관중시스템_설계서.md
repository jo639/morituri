# MORITURI — [10] 관중 시스템 · 군중게이지 (설계 v0.1)

**상태:** 라니스타 4대 결정 확정(2026-06-22) → Claude 세부 설계. **수치 초안(매트릭스 미검증).**
**한 줄:** 콜로세움 관중이 더 흥분시킨 쪽으로 기우는 **공유 줄다리기 게이지(−100~+100)**. 기운 쪽이 **기세 버프**(사기)를 받는 강한 밸런스 레버. 인매치 자원 + Phase 3 인기·명성 훅.

> **라니스타 확정(이 설계의 전제):**
> 1. 전투 영향 = **전 선수 사기 효과(강한 레버)** — 스킬 한정 아님, 유리한 쪽 능력에 직접 작용.
> 2. 구조 = **공유 줄다리기(단일 게이지)**.
> 3. 페이오프 = **기세 버프(유리한 쪽 강화)**.
> 4. 범위 = **인매치 게이지 완결 + Phase 3 메타 인기 '연결 지점'만 예약.**

---

## 1. 개요 — 2층 구조

| 층 | 이름 | 성격 | Phase |
|---|---|---|---|
| ① 인매치 | **군중게이지(CrowdGauge)** | 경기 중 실시간 줄다리기 → 기세 버프 (Sim, 결정론) | 2 |
| ② 메타 | **인기·명성(Fame)** | 경기 결과·관중 장악도 → 영속 평판(네메시스·매니지먼트) | 3 (훅만) |

이번 설계는 ①을 완결하고, ②는 "경기 끝에 무슨 값을 내보내는가"(export 인터페이스)만 정의한다.

---

## 2. 군중게이지 정의

- `CrowdGauge ∈ [−100, +100]`, 시작 0. **부호 규약: + = A(선수0) 편, − = B(선수1) 편.** |값|이 클수록 관중이 그쪽에 열광.
- **Sim 레이어 값**(결정론) — `ResolutionPhase`/이벤트에서 갱신, `StrategyTick`마다 기세 버프 재계산.
- 의미: *누적 점수*가 아니라 **최근 기세(momentum)** — 빠르게 차고 빠르게 식는다(§4 감쇠). "지금 관중이 누구에게 열광하나."

---

## 3. 적립 (Fill) — 관중은 무엇에 환호하나

행동의 **주체(acting fighter)** 쪽으로 게이지가 기운다. A의 멋진 행동 → `+`, B → `−`. 관중의 가치 = **스펙터클 + 유혈 + 역전**(기존 `highlights` 태그 comeback·taunt_reversal와 정합).

| 이벤트 | 델타(주체 쪽) | 메모 |
|---|---|---|
| 클린 히트 (`HitLanded` clean) | ±2 | 기본 호응(약함 — 단순 딜은 저평가) |
| 크리티컬 / 카운터 | ±6 | 기술·한방 |
| 가드 브레이크 (`GuardBroken`) | ±8 | 함성 이벤트(기존 훅) |
| 자세 붕괴 / 스태거 (`PoiseBroken`) | ±5 | 함성 이벤트(기존 훅) |
| 넉다운 (`KnockedDown`) | ±15 | 큰 환호 |
| 도발 성공 (`DoTaunt`) | ±7 | 쇼맨·오만 테마 |
| 결정타 / KO 마무리 | ±20 | 피날레 |

**보너스/특수:**
- **역전 호응(언더독):** 가격자 HP < 30% 일 때 위 델타 **×2** (저HP에서 때리면 관중이 미친다 → 스노볼이 아닌 *역전* 유인).
- **명예 분기(고결 HoldOff/추가타 거부):** 지금은 중립 **+3**("명예에 호응"). Phase 3에서 군중 성향(피에 굶주림 ↔ 명예존중)으로 분기 예약.
- **소극 야유:** 무교전(Idle/이동만) 3s↑ 지속 시 → 감쇠 ×2 가속 + **양쪽 동시 −2/s**(게이지 0으로) + Patience 연계(§6).

> 적립은 **스펙터클·역전에 가중**, 단순 누적딜은 저평가 → 일방적 압살보다 *드라마*에 게이지가 쏠린다.

---

## 4. 감쇠 (Decay) & 거울전 대칭

- **상시 감쇠:** 이벤트 없으면 `CrowdGauge → 0` 방향으로 **6/s**. (+50이면 새 스펙터클 없을 시 ~8s에 소멸 → "최근 기세"만 반영.)
- **소극 시 ×2**(12/s) — 비비기/대치는 관중이 식는다.
- **캡 ±100**, 초과분 버림.
- **거울전 대칭 보존(필수 점검):** 동일 선수끼리는 이벤트가 대칭 발생 → 게이지 ≈ 0 유지 → 데드존(§5) 안 → 버프 0 → **거울 50/50·KO율 보존.** `_tick&1` 처리순서 교대가 결정론·대칭을 지킴. (구현 후 거울 KO율 반드시 재측정.)

---

## 5. 페이오프 — 기세 버프 (유리한 쪽 강화)

**데드존 + 선형 스케일:**
- `norm = clamp((|CrowdGauge| − 30) / 70, 0, 1)` — |게이지| 30 이하는 버프 0(관중이 결정적으로 한쪽 편든 게 아님), 30→100 구간을 0→1로.
- **유리한 쪽**(`sign(gauge)`)에만 버프(불리한 쪽 페널티 없음 = "유리한 쪽 강화"):

| 파라미터 | 최대(norm=1) 버프 | 비고 |
|---|---|---|
| Aggression | +0.30 | 더 적극적(결단↑) |
| 공격력 | ×(1 + 0.12) | 화력 |
| 스태미나 회복 | ×(1 + 0.30) | 지구력(장기전 기세) |
| 이동속도 | ×(1 + 0.08) | 압박/기동 |

- 매 `StrategyTick`(1s)에 현재 게이지로 재계산해 유리한 쪽에 `ActiveOverride`(reasonTag `MOMENTUM`) 적용. 게이지가 반대로 넘어가면 즉시 반대편으로.
- **드라마 곡선:** 역전 호응(저HP 가격 ×2)으로 *지고 있던 쪽*이 관중을 뺏어오면 → 기세 버프 → 실제 역전 발판. 일방 압살은 단순딜 저평가 + 상대 역전호응으로 게이지가 잘 안 쏠려 **순수 스노볼을 억제**. (단 강한 레버라 스노볼 위험 상존 → §9 검증.)

---

## 6. 기존 시스템 통합

- **이벤트 훅:** `HitLanded·GuardBroken·PoiseBroken·KnockedDown·DoTaunt·MatchEnded`에서 게이지 갱신. 새 엔진 불필요(원칙 C).
- **명예중시 트리거 재정의:** AI 문서[3]의 "관중 호응 高 → Aggression+0.1"이 **곧 이 게이지** — 자기 편 게이지 높을 때 발동하도록 연결(기존 트리거가 게이지를 읽음).
- **Patience(인내심) 시너지:** 소극 야유(−2/s)와 Patience 소진(무교전 시 공격충동↑)이 같은 방향 → **관중이 교착을 깨도록 압박.** 야유 지속 시 양쪽에 약한 Aggression 너지(+0.05).
- **쇼맨/오만 스킬 재정합(문서[7]):** 군중게이지 = 그 스킬들의 *조건·자원*.
  - 쇼맨 '관중몰이' = 자기 편으로 게이지 적립 **×1.5**(스펙터클 특화).
  - 쇼맨 '쇼타임' = 자기 편 게이지 ≥ 70에서 발동(스택 대신 게이지 임계).
  - 오만 '황제의 위압' = 도발 적립 보너스 + 위압.
  - **주의:** 라니스타 페이오프 결정은 "기세 버프(보편)"라, 게이지의 *기본 효과*는 누구에게나 적용되는 기세 버프다. 크라우드형 스킬은 그 위에 *추가로* 게이지를 더 잘 쌓거나 임계에서 터지는 식으로 *얹힌다*(별개 효과).

---

## 7. 가시성 (뷰어 — 관전 게임 원칙)

- **게이지 미터:** 화면 상단 중앙 양방향 바(중앙 0, 좌 B파랑 / 우 A빨강). 기우는 방향·세기 실시간.
- **함성/야유:** 적립 이벤트 시 해당 쪽 함성 파티클·볼륨↑, 소극 시 야유. (기존 `PoiseBroken/GuardBroken → 관중 함성` 확장.)
- **기세 발동:** `MOMENTUM` reasonTag → 유리한 쪽 오라/사기 컷인. 데드존 진입/탈출 시 시각 전환.
- 모두 Presentation 레이어 — Sim 게이지 값을 읽어 그림. (Sim은 렌더 무의존, 문서[1].)

---

## 8. Phase 3 메타 인기·명성 훅 (인터페이스만)

경기 종료(`MatchEnded`) 시 **선수별 export 값**(영속 Fame 입력):
- **관중 장악도** = 경기 내내 *자기 편 게이지*의 시간적분(∫ max(0, ±gauge) dt) / 경기시간. (꾸준히 관중을 쥐었나.)
- **스펙터클 점수** = 적립 이벤트 가중합(크리·역전·KO 등).
- **승패·연출 보정** = 역전승·처형·도발승 등 highlights 태그.
- → Phase 3에서 `Fame += f(관중장악도, 스펙터클, 승패)`로 팬·드래프트가치·네메시스 라이벌 강도에 반영. **본 설계는 값 산출까지만**, 영속 저장·소비는 Phase 3.

---

## 9. 밸런스 검증 (★ 강한 레버 — 필수)

라니스타가 "강한 레버"를 택했으므로 **매트릭스 영향 큼**. 메모리 원칙([9]) 강제:
- 추가/튜닝 시마다 **`sigmatrix 500` + 거울전 KO율** 재측정. 특히 **거울 게이지 ≈0 유지**(대칭 깨지면 버그) 확인.
- **스노볼 가드:** 버프가 과하면 "먼저 앞선 쪽이 게이지·버프 독식 → 압살". 역전 호응(×2)·단순딜 저평가가 완충이나 부족하면 → 버프 상한↓ / 데드존↑ / 역전 보너스↑로 조정.
- **카이터 영향:** 게이지 버프(이속+8%·회복+30%)는 카이터·러셔 매치업을 흔들 수 있음 → 견제/난전 행 집중 점검.
- 권장 튜닝 순서: 데드존 → 최대버프(공12%·회복30%·이속8%·Agg0.3) → 적립 가중 → 감쇠율 순으로 셀 보며.

### 9.1 구현·검증 (2026-06-22, 커밋 38d6017)
구현 튜닝(초안→엔게이지 조정): **감쇠 4/s**(소극 ×2)·**데드존 15**·**적립 ×2 스케일**(클린4·크리/카운터12·가드붕괴16·스태거10·넉다운30·KO+40·도발14, 빈사 ×2). 데드존 30·감쇠 6/s 초안은 적립이 못 쌓여 게이지가 데드존을 못 넘어 **무효과**였음 → 조정 후 작동.
- **거울 51.1/48.9·KO 76.5% 보존**(게이지≈0). **sigmatrix 무관중 대비 ±1pt**(무기 매트릭스 안정 — 결정적 매치업 일방 압살은 데드존 못 넘음).
- 드라마는 **접전·성격 매치업에만**: 잔혹vs겁 66.5→55.5(언더독 ×2 역전호응), 오만vs도전 43.9→34.2(오만 위축 0.8).
- **남은 튜닝 여지:** 오만 −10은 다소 강함(CrowdSensitivity 0.8↓ 검토). 버프 상한·적립 스케일은 더 강하게/약하게 자유.
- 뷰어: 상단 중앙 양방향 미터(+A파랑/−B빨강·데드존 음영) 구현(ReplayFrame.Crowd 직렬화).

---

## 10. 구현 스키마 / 맵핑

- **Sim 상태:** `MatchSim.CrowdGauge`(float, −100~100). `_now` 기반 결정론.
- **갱신 지점:** 이벤트 핸들러(HitLanded 등) + `PassiveUpdate` 감쇠 + `StrategyTick` 버프 재적용.
- **버프:** 유리한 쪽 `FighterRuntime.Overrides`에 `MOMENTUM` ActiveOverride(매 틱 갱신, ParamMod Add/Mult).
- **이벤트:** `Emit(CrowdShift(time, delta, towardFighter, gaugeNow))` 신규 SimEvent(뷰어 미터·함성용). `MOMENTUM` Decision 이벤트(버프 가시화).
- **데이터:** T14_CrowdTuning(이벤트별 적립 델타·감쇠율·데드존·최대버프계수) — 원칙 C, 코드 아닌 데이터.
- **Phase3 예약:** `MatchResult`에 `CrowdControlA/B`, `SpectacleA/B` 필드 추가(export). T15_Fame(영속).

---

## 11. 미해결 질문 (다음 결정)

1. **버프 상한 구체값** — 공12%·회복30%·이속8%·Agg0.3은 초안. sigmatrix 보며 확정.
2. **불리한 쪽 페널티 여부** — 현재 "유리한 쪽만 강화"(라니스타 결정)지만, 약한 위축(−)을 줄지(드라마↑ vs 스노볼↑).
3. **소극 야유의 Aggression 너지(+0.05)** 세기 — Patience와 중복 과할 수 있음.
4. **명예 분기**(군중 성향: 피갈망 ↔ 명예존중) — Phase 3 군중 개성 시스템으로 갈지, 인매치 고정 +3로 둘지.
5. **데드존 30 / 최대 100 스케일** — 관중이 너무 쉽게/어렵게 쏠리지 않게 적립 총량과 균형.
6. **메타 Fame 공식**(관중장악도·스펙터클·승패 가중) — Phase 3 본설계에서.
