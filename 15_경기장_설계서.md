# MORITURI — [15] 경기장 설계서 (v0.1 초안)

**작성:** 2026-07-31 (M4-b 프레젠테이션 브랜치)
**상태:** **§0~§9 확정·A1~A5 구현 완료 (2026-07-31~08-01)** — 조명 층 종결. **§10 배경 아트 방향은 초안(§10.9 결재 대기).**
**한 줄 더:** A1~A5는 *조명*을 만들었다. §10은 그 조명이 비출 *대상*을 다시 만드는 이야기다 — 절차 생성 도형을 3D 베이크 배경으로 교체.
**한 줄:** 경기장을 **충돌(Sim) / 투영(카메라) / 재질·조명(렌더)** 세 층으로 갈라 정의하고, 노멀맵 조명을 **런타임 비용 0**으로 얹을 수 있게 만든다.
**전제 문서:** [8] 비전(B: 2D-lite), [9] disc 밸런스(충돌 지오메트리 성역), [12] 배포 로드맵(Godot 4 최종), [14] 세계 이벤트(조도·날씨 변조), [10] 관중 시스템

---

## 0. 설계 원칙 5

1. **충돌은 성역.** 경기장의 *모습*은 마음껏 바꾸되 *충돌 지오메트리*(원 r=12m, 장애물 0)는 건드리지 않는다. 건드리는 순간 무기·전술 매트릭스 재검증이다(→ §1, [[disc-strafe-balance-spine]]).
2. **투영은 단일 출처.** 배경·바닥·선수·그림자가 **같은 타원 파라미터 하나**를 읽는다. 지금은 안 그렇다(→ §2.3 발견).
3. **광원은 먼저 고정하고 굽는다.** 노멀맵은 광원이 정해져야 의미가 있다. 조명 리그를 §3에서 **수치로 확정**하고, 모든 그림자·하이라이트를 거기서 유도한다. 손으로 그린 "그럴듯한 음영"과 계산된 음영이 섞이면 둘 다 가짜로 보인다.
4. **높이는 그리는 김에 나온다.** 바닥·벽 질감은 이미 코드가 절차적으로 생성한다. 같은 패스에서 **높이 버퍼**를 같이 쌓으면 노멀맵 아트 에셋이 0장이다(→ §4).
5. **에셋은 남고 코드는 버려진다.** 최종 목표는 Godot 4다. 웹의 조명 *코드*는 이식 때 버려지고, **노멀맵과 광원 리그 수치**는 1:1로 넘어간다. 그러니 웹에는 WebGL 파이프라인을 새로 짜지 않는다(→ §5).

---

## 1. 층 ①: 충돌 지오메트리 (불변 계약)

렌더러가 **가정해도 되는 것**과 **가정하면 안 되는 것**의 경계. 출처는 `Morituri.Sim/Data/BalanceConstants.cs`.

| 항목 | 값 | 의미 |
|---|---|---|
| `ArenaRadius` | **12 m** (지름 24 m) | 중심 (0,0)의 **순수 원**. 선수 위치는 `Pos.Length ≤ 12` 보장 |
| `CornerZone` | **1.5 m** | 경계 1.5 m 이내 = 가장자리(판정 패널티). 시각적으로 **표시할 수 있는 유일한 내부 경계** |
| 장애물 | **없음** | 기둥·함정문·단차 전무. `MatchSim`은 원 클램프와 벽 접선 미끄러짐만 안다 |
| 높이(Z) | **없음** | Sim은 완전 2D. 선수 키·무기 높이는 전부 렌더 연출 |

**계약:**
- 렌더러는 원 안 어디든 선수가 설 수 있다고 가정해야 한다. 바닥 그림에 "못 밟는 영역"을 만들면 안 된다.
- 시각적 소품(철문·시체 끄는 갈고리·모래 무더기)은 **원 바깥 또는 벽면에만** 놓는다. 원 안에 놓이면 선수가 통과해서 지나간다.
- **장애물 도입은 기각됐다(§9 결정 3).** 넣으려면 Sim 변경 + `matrix` CLI 전 무기 A/B 재검증이 따르고, 벽-disc 핀 교착이 살아 있는 상태에서 장애물 추가는 새 교착을 만들 확률이 높다([[wall-disc-pin-deadlock]]). 이 문서는 **Sim 무접촉**을 전제로 쓰였다.

---

## 2. 층 ②: 투영 (2.5D 카메라)

### 2.1 현재 파라미터 (`viewer.html`)

```
논리 좌표계  W = 940, H = 440, PAD = 70          (불변 — 모든 그리기 기준)
카메라 노브  CamHeight = 0.50
             CamTilt   = 0.34(기본) ↔ 0.26(줌 추적)   ※ 모드별 · 줌 배율에 연동 보간 (§2.4)
투영 타원    CX = W/2 = 470,  CY = H·CamHeight = 220
             RX = W/2 − PAD = 400  (불변)
             RY = RX·CamTilt = 136(기본) ↔ 104(줌)
m→px         mPx = RX / ArenaRadius = 400/12 ≈ 33.3 px/m (가로)
             세로는 ×(RY/RX) = ×CamTilt
```

즉 **월드 (x, y) → 화면 (CX + x·mPx, CY + y·mPx·0.34)**. `CamTilt = 0.34`는 카메라 부각 ≈ 20°에 해당한다(sin⁻¹ 0.34). 낮게 눕힌 관전석 시점 — 이게 이 게임의 룩이다.

### 2.2 이 문서가 고정하는 것

- ~~**부각 20° 유지.**~~ → **§2.4에서 개정: 기본 20°, 줌 추적 15°.** 아래 노멀맵 논리는 그대로 유효하되 **더 낮은 쪽(15°)을 기준으로** 설계해야 한다. 부각이 낮을수록 바닥 노멀의 효과가 약해지고(바닥을 비스듬히 봄) 벽면 노멀의 효과가 강해진다. 15~20°는 **벽·소품 중심의 조명 설계**를 요구한다 — 바닥은 갈퀴 자국 같은 **방향성 있는 굵은 요철**만 살아남는다(미세 모래알은 이 각도에서 안 보인다). §4의 강도 배분이 여기서 나온다. 부각이 구간으로 바뀌었으므로 바닥 노멀은 **20°에서 검증하고 15°에서 재확인**한다(낮은 쪽이 더 불리).
- **선수 키** — 초안의 78 px는 폐기. 현재 `FIGH = 120 px`(줌 1배 기준)이고 줌 추적 1.6배에서 화면 **192 px**다(§2.4). 바닥의 세로 축척과 **다른 것은 그대로 정상** — 사람은 세워서 그리고 바닥은 눕혀서 그리는 2.5D의 본질. 그림자만 바닥 축척을 따른다.

### 2.3 ⚠ 발견 → **A1 수정 완료 (2026-07-31)** — 배경과 플레이 타원이 35 px 어긋나 있었다

`buildArenaBg()`(viewer.html:190)는 중심을 **하드코딩**한다:

```js
const _CX=940/2, _CY=440*0.58, _RX=940/2-70, _RY=(940/2-70)*0.34;
                      ^^^^ 0.58
```

런타임 투영은 `CamHeight = 0.50`을 쓴다(viewer.html:759). 배경은 `drawImage(bgcv, 0, 0, W, H)`로 **오프셋 없이** 그려진다(viewer.html:1236).

| | 중심 Y | 위쪽 끝 | 아래쪽 끝 |
|---|---|---|---|
| 그려진 모래바닥 | 255.2 | 119.2 | 391.2 |
| 실제 플레이 타원 | 220.0 | **84.0** | 356.0 |

→ 원 북단(화면 위쪽)에 선 선수는 **모래가 아니라 관중석 위에 서 있고**, 남단에는 아무도 못 밟는 모래가 35 px 남는다. `CamHeight`가 나중에 0.58→0.50으로 조정되면서 배경의 하드코딩이 따라오지 않은 것으로 보인다.

**처방(원칙 2) — 적용됨:** 카메라 상수 블록(`CamHeight`/`CamTilt`/`CX`/`CY`/`RX`/`RY`)을 논리 좌표계 선언 **바로 아래**(W/H/PAD 다음)로 올리고, `buildArenaBg()`는 자체 하드코딩 대신 이 값을 참조한다. 상수가 배경 빌더보다 뒤에 있으면 `const` TDZ로 스크립트 전체가 죽으므로 **순서가 곧 계약**이다 — 코드에 주석으로 못 박았다.

**검증 (2026-07-31):** 원 경계(r=11.5 m) 8방위에 발을 놓고 배경 픽셀의 재질을 판정.

| | 모래 위 | 관중석 위(실패) |
|---|---|---|
| 수정 전 | 5/8 | **3/8** — 225°·270°·315°(화면 북쪽) |
| 수정 후 | **8/8** | 0 |

바닥 아래쪽 끝도 353.5 px로 이동(플레이 타원 하단 356)해 예측한 35.2 px만큼 정확히 따라왔다. 콘솔 오류 0, designlint 0.

> 계측 메모: 위쪽 끝의 잔여 오차(측정 105.5 vs 기하 84)는 어긋남이 아니라 **북쪽 벽 그림자 그라디언트**가 모래를 어둡게 덮어 밝기 임계값에 안 걸린 것이다. 그 그림자 자체는 §3.2에 따라 어차피 폐기·재계산 대상.

### 2.4 프레젠테이션 파이프라인 — **확정 (라니스타 결재 2026-07-31)**

M4-b 스프라이트가 3D 베이크(`Morituri/tools/spritebake/`)로 바뀌면서 투영·카메라 계약이 함께 확정됐다. **이 절의 값들은 서로 묶여 있다 — 하나만 바꾸면 어긋난다.**

#### 2.4.1 확정값

| 축 | 값 | 묶인 상대 |
|---|---|---|
| 스프라이트 원본 높이 | **96 px** | `PX`·줌과 1:1 관계 |
| 저해상 월드 배율 `PX` | **2** | 화면 픽셀이 전부 2×2로 균일 |
| 화면상 검투사 키 `FIGH` | **120 px** (줌 1배) | 줌 1.6배에서 192 px |
| 줌 추적 배율 `ZoomFollow` | **1.6** | `(FIGH/PX)×줌 = 96` → 원본과 1:1 |
| 상단 여백 `ZoomLiftPx` | **46 px** | 머리 위 여백 28→74 px |
| 부각 `CamTilt` | **0.34(기본) ↔ 0.26(줌)** | 베이크 `--elevation` |
| 베이크 각도 | **20° 한 벌** | `asin(0.34)` |
| 무대 세로 `H` | **440 → 560** (개정 2026-08-01, 결정 ⓒ) | 아레나 폭·부각 불변, **위로만 열린다** |

> **`H` 440 → 560 개정 근거(§10.12):** 세로 440에서는 프레임 세로가 13.2 m뿐이라 24 m 아레나를 담고 나면 위로 2.5 m밖에 안 남고, 포디움 벽 하나가 그 예산을 다 먹어 **관중석이 들어갈 자리가 0**이었다. 560이면 16.8 m가 되어 관중석 사발이 들어온다. 부각을 올리는 대안(ⓑ)은 스프라이트 전량 재베이크라 더 비싸다. **뷰어 적용은 B4에서** — 지금 `viewer.html`의 `H`를 바꾸면 곧 삭제될 `buildArenaBg`의 하드코딩(940/440)까지 손봐야 한다.

**1:1 매핑 식:** `저해상 공간 스프라이트 크기 = (FIGH / PX) × 줌 = (120/2) × 1.6 = 96 px`.
줌을 1.6에서 벗어나게 하면 이 등식이 깨져 픽셀이 미세하게 불균일해진다. 구도가 답답하면 **줌이 아니라 `lift`로 푼다.**

#### 2.4.2 왜 저해상 캔버스인가 (2층 렌더)

깊이 원근(`sc = 0.8~1.2`) 때문에 스프라이트 배율은 **항상 비정수**다. 스무딩만 끄면 어떤 소스 픽셀은 2칸, 어떤 것은 1칸이 되어 격자가 들쭉날쭉해진다. 월드를 `1/PX` 캔버스에 그린 뒤 정수배로 확대하면 리샘플링이 저해상 공간 안에서만 일어나고 **화면 픽셀은 전부 `PX×PX`로 균일**해진다.

- **월드층**(저해상): 아레나 배경 · 그림자 · 스프라이트 · 혈흔
- **UI층**(풀해상): 이름표 · 상태 스트립 · 간격선 라벨 · 데미지 숫자 · 컷인 — 글자는 저해상으로 내리면 못 읽는다

검증(인접 픽셀 동일률, 짝수/홀수 경계): `PX=1` → 0.732 / 0.737(차이 없음 = 격자 없음) · `PX=2` → **0.992 / 0.634**(짝수 경계에서만 일치 = 정렬된 2×2 격자).

#### 2.4.3 부각은 모드별, 스프라이트는 한 벌

기본 화면 20°, 줌 추적 15°. **줌 배율에 연동해 연속 보간**하므로 전환 순간 바닥이 튀지 않는다.

```
zoomFrac = clamp((camZoom − 1) / (ZoomFollow − 1), 0, 1)
CamTilt  = TiltBasic + (TiltZoom − TiltBasic) · zoomFrac
```

패자 줌인(2.9배)은 `zoomFrac`이 1로 클램프되어 가장 낮은 시점이 된다. 배경 프리렌더는 **부각별로 두 벌** 굽고 `zoomFrac`으로 알파 합성한다(매 프레임 재생성은 too expensive, 배경은 `drawImage` 1장이라 2장도 싸다).

**스프라이트는 20° 한 벌만 쓴다.** 검토했고 기각한 안이다:

- **측정:** 96 px에서 20°와 15° 스프라이트의 차이는 **전부 윤곽선 위 1 px 이동**으로만 나타난다. 뷰어 정렬(하단=지면·가로 중앙)로 맞춘 뒤 픽셀 차이율 45.7%가 나오지만, 차이 지도를 보면 **몸통·갑옷·방패는 전부 동일**하고 외곽선만 밝게 뜬다. 5°는 이 해상도에서 인지 문턱 아래다.
- **비용은 싸다:** 무기 8종 × 9포즈 = 72장, 16프레임 시트 1장당 52초(실측) → 한 각도 약 **1시간 · 11 MB**. 두 각도라도 2시간 · 22 MB.
- **그런데 진짜 비용은 따로다:** ① 영구적 2배 유지비(캐릭터·포즈·룩 변경이 전부 두 벌, 동기 안 맞으면 각도 섞임) ② 줌 전환 시 **스프라이트만 툭 바뀌는 팝**(카메라는 lerp로 미끄러지는데 시트는 순간 교체). 크로스페이드로 막으면 픽셀아트 외곽선이 뭉갠다.
- **결론:** "카메라가 낮아졌다"는 인상은 **캐릭터가 아니라 바닥이 만든다**(`RY` 136→104, 24% 납작). 보이는 쪽은 공짜고 돈 드는 쪽은 안 보인다.

> 이 판단의 뒷면: "5°가 안 보인다"면 **바닥과 캐릭터가 5° 어긋나는 것도 안 보인다.** 두 주장은 같은 근거를 쓴다. 나중에 해상도를 올리거나(144 px 이상) 줌인 연출을 더 크게 쓰면 문턱을 넘을 수 있다 — 그때 재검토한다. 베이크가 1시간짜리라 **미룰 수 있는 결정**이다.

#### 2.4.4 불변 규칙 — 어기면 어긋난다

1. **`CamTilt`를 바꾸면 스프라이트를 다시 굽는다.** `elevation = asin(CamTilt)` (0.34→20° · 0.26→15° · 0.20→11.5°). 바닥만 눕히고 스프라이트를 두면 캐릭터만 내려다본 각도로 남는다.
2. **프레이밍 오프셋은 아레나 클램프 *뒤*에 준다.** 클램프 안에서 `tgt.y`를 밀면 아레나가 화면보다 작을 때(줌+눕힌 각도 → `mgnY = 0`) 통째로 먹힌다. 클램프는 추적용, 프레이밍은 변환 단계.
3. **`ctx.setTransform` 후에는 `CANVAS_S`로 되돌린다.** `fitCanvas()`가 걸어둔 DPR 배율이 날아간다.
4. **완전한 픽셀 퍼펙트는 창 크기에도 걸린다.** 캔버스가 무대 상자에 임의 배율로 표시되므로 마지막 단계에서 한 번 더 리샘플링된다. 없애려면 무대 크기를 `W`의 정수배로 스냅해야 한다 — **미구현**.

#### 2.4.5 뷰어 노브 (URL 파라미터)

```
?px=2        저해상 월드 배율 (1 = 픽셀화 없음, 옛 동작)
?figh=120    화면상 검투사 키
?zoom=1.6    줌 추적 배율        ※ 1.6에서 벗어나면 1:1 매핑이 깨진다
?lift=46     상단 여백(px)
?tilt=0.34   기본 부각
?tiltzoom=0.26  줌 부각
?view=zoom   기본 모드 (basic | zoom)
?sheet=sprites.json  스프라이트 시트 교체(룩 비교용)
?venue=arena     경기장 (arena | spar | street | bar) — 팔레트 + 광원 프리셋([15]§3.5)
?weather=night   [14] 세계 이벤트 연출 (night | rain | sand). 조명·색보정만, Sim 무접촉
```

---

## 3. 층 ③-a: 광원 리그 (노멀맵의 전제)

노멀맵은 **L 벡터가 있어야** 존재한다. 여기서 확정한다.

### 3.1 좌표 규약

접선공간(tangent space) 화면 기준: **+X = 화면 오른쪽, +Y = 화면 아래, +Z = 화면 밖(관중 쪽)**. 노멀맵 RGB = (X, Y, Z)를 [0,1]로 인코딩(일반적인 파란 노멀맵). 조명은 `I = ambient + diffuse·max(0, N·L)`.

### 3.2 현재 코드에 암묵적으로 박힌 광원

- 바닥 방사 그라디언트 중심이 `(_CX, _CY − _RY·0.3)` → **밝은 지점이 위쪽**.
- 벽 그림자 그라디언트가 상단에서 아래로 → **북쪽 벽이 안쪽으로 그림자를 드리움**.

두 개를 합치면 "빛이 관중석 뒤(남쪽) 높은 곳에서 온다"는 뜻인데, 그러면 밝은 지점은 아래쪽이어야 한다. **현재 음영은 서로 모순이다.** 손으로 그럴듯하게 칠한 결과 — 원칙 3이 필요한 이유.

### 3.3 확정안 — **안 B「늦은 오후」 (라니스타 확정 2026-07-31)**

> **L = (−0.45, −0.60, 0.66)** · 방위 = 화면 좌상단 · 고도 ≈ 41° · 그림자 우하향.
> 이 벡터가 이 문서 전체의 단일 광원이다. **모든 그림자·하이라이트는 여기서 유도한다** — §3.2의 기존 음영 2건은 폐기하고 다시 계산한다.

| | 안 A — 정오의 태양 (기각) | **안 B — 늦은 오후 ✅ 확정** |
|---|---|---|
| L (화면 접선공간) | (0, −0.20, 0.98) | **(−0.45, −0.60, 0.66)** |
| 방위 | 거의 정수직 | 화면 **좌상단**에서 |
| 고도 | ≈ 78° | ≈ **41°** |
| 그림자 방향 | 발밑에 뭉침 | **우하향으로 길게** |
| 노멀맵 체감 | 약함(요철이 안 드러남) | **강함 — 갈퀴 자국·벽 줄눈이 살아난다** |
| 기존 음영 재작업 | 거의 없음 | 바닥 그라디언트·벽 그림자 **전부 다시** |
| 분위기 | 평평하고 뜨겁다 | 길어진 그림자, 콜로세움의 오후 |

B로 확정. 노멀맵을 하겠다는 목적 자체가 A에서는 거의 무의미하다(부각 20° + 고도 78°면 바닥 요철이 안 보인다). 재작업량은 어차피 §2.3에서 배경을 손대야 하므로 같이 처리된다.

### 3.4 보조 광원

- **횃불(선택):** 벽면 등거리 8개. 이건 **동적**이라 웹 베이크로는 못 한다 → Godot 이관 시 `PointLight2D` 8개로 구현. 웹 단계에서는 **위치만 배경에 그려두고 조명 효과 없음**(껍데기). 미리 자리를 잡아 둬야 Godot에서 좌표가 그대로 넘어간다.
- **환경광(ambient):** 하늘 반사. 위에서 오는 약한 파란빛 — 그림자 안이 새까매지지 않게. `ambient = 0.35`, 색 `#6b7a96` 권고.

### 3.5 venue별 프리셋 (기존 4종 + [14] 연동)

| venue | 광원 | ambient | 비고 |
|---|---|---|---|
| `arena` 콜로세움 | §3.3 확정안 | 0.35 하늘색 | 기준 |
| `spar` 수련장 | 동일 L, 강도 0.8 | 0.45 | 반그늘 마당 |
| `street` 뒷골목 | L 고도 15°(낮은 틈새광) | 0.25 | 좁고 어둡다 |
| `bar` 선술집 | **실내** — L 무의미, 횃불 2개 지배 | 0.20 주황 | 웹 단계는 베이크 정지, 평면 유지 |
| [14] 야간 경기 | 태양 제거, 횃불만 | 0.15 | Godot 단계 기능 |
| [14] 폭우 | L 강도 0.5, 스펙큘러 ↑(젖은 모래) | 0.40 회색 | |
| [14] 모래 폭풍 | L 강도 0.6, 대비 ↓ | 0.50 황갈 | |

> [14] 연동은 **연출 변조만** — Sim def에 손대는 건 [14]의 몫이고 이 문서는 그 그림만 그린다.

---

## 4. 층 ③-b: 재질과 높이 소스

**핵심:** 아래 표의 "높이 소스"가 이미 코드에 있는 절차 생성이면 → **노멀맵 에셋 0장**. 같은 루프에서 height 버퍼에 한 줄 더 쓰면 된다.

| 재질 | 위치 | 높이 소스 | 상태 | 노멀 강도 | 부각 20°에서 |
|---|---|---|---|---|---|
| **모래 — 갈퀴 자국** | 바닥 | 동심 호 5개 (viewer.html:236) | **있음** | **강 (1.0)** | 잘 보임 — 주력 |
| **모래 — 스펙클** | 바닥 | 랜덤 점 950개 (viewer.html:229) | **있음** | 약 (0.2) | 거의 안 보임 |
| **모래 — 발자국/끌린 자국** | 바닥 | 없음 | 신규 | 중 (0.6) | 경기 중 누적 = 살아있는 무대 |
| **혈흔 데칼** | 바닥 | `stains`(viewer.html:1238) | **있음**(색만) | 젖음(스펙큘러) | 높이 0, **광택**으로 처리 |
| **벽 — 석재 줄눈** | 벽면 | 없음 (단색 타원) | **신규 — 최대 수확** | **강 (1.0)** | 벽은 정면에 가까워 노멀 효과 최대 |
| **벽 — 이 빠진 자국·균열** | 벽면 | 없음 | 신규 | 중 (0.5) | 콜로세움의 나이 |
| **경기 라인** | 바닥 0.88r | 있음 | 유지 | 미세 (0.1) | 살짝 파인 백묵선 |
| **`CornerZone` 표시** | 바닥 r=10.5m | 없음 | **신규 — 게임플레이 정보** | 미세 | §1의 유일한 내부 경계 |
| **관중석** | 원 밖 | 점묘 (viewer.html:203) | 있음 | **대상 아님 (확정)** | §9 결정 2 — 배경 유지 |
| **철문 (포르타)** | 벽면 2곳 | 없음 | 신규 | 강 | 선수 입·퇴장 방향의 근거 |

> **부각 20°의 결론:** 예산을 **벽에 몰아라.** 바닥은 갈퀴 자국 하나만 제대로 살리면 되고, 모래알 노멀은 이 카메라에서 픽셀 낭비다.

---

## 5. 노멀맵 파이프라인 (단계별 · 무엇이 남고 무엇이 버려지나)

### N1. 배경 베이크 — **런타임 비용 0** ✅ 웹에서 지금 가능

`buildArenaBg()`가 이미 하는 일에 추가:

```
1) 알베도 캔버스 + 높이 캔버스(그레이스케일)를 나란히 그린다
   — 갈퀴 자국·줄눈을 칠할 때 색 대신 밝기로 한 번 더
2) 로드 시 1회: 높이 → Sobel → 노멀 (N)
3) 로드 시 1회: I = ambient + max(0, N·L)  → 알베도에 곱해 bgcv에 굽는다
4) 프레임 루프는 지금과 동일하게 drawImage(bgcv) 한 장
```

- **프레임당 추가 비용: 0.** 렌더 루프 무변경.
- 베이크 비용: 940×440×BGS² ≈ 1.65 M px 한 패스 → **목표 200 ms 이내**, 로드 시 1회.
- 광원이 바뀌는 때(venue 전환, [14] 날씨)만 다시 굽는다 — 경기당 최대 1회.
- **Godot 이관 시:** 이 코드는 버려지고, 높이맵 생성 규칙(§4 표)만 셰이더/텍스처로 옮긴다.

### N2. 스프라이트 조명 — **보류 (§9 결정 4)** ◐ 웹에서 반쪽만 가능

광원이 고정이고 스프라이트는 **좌우 반전만** 하므로, 로드 시 **포즈 9 × 방향 2 = 18장**을 미리 조명해 캐시하면 된다. 프레임당 비용은 여전히 `drawImage` 1장.

- 전제: **스프라이트 노멀맵이 필요하다.** 현재 파이프라인(제미나이→나노바나나→remove.bg)은 노멀맵을 못 만든다. 대안 — Laigter(무료, 2D 스프라이트 노멀맵 생성기) 또는 SpriteIlluminator.
- **냉정한 평가:** 화면 키 78 px 캐릭터에 노멀맵은 투자 대비 회수가 낮다. → **확정 대체안: 접지 그림자를 L 방향으로 눕히고 림라이트 한 줄.** 이게 A4에 붙는다. 노멀맵 텍스처 제작은 Godot 이관(N3) 시점에 재검토.
- 진짜 동적 광(횃불 깜빡임, KO 순간 붉은 조명)은 웹에서 불가 → Godot.

### N3. Godot 4 이관 — 전부 ✅

- `CanvasTexture.normal_texture` + `DirectionalLight2D`/`PointLight2D` = 네이티브. 셰이더 자작 불필요.
- **넘어가는 것:** 노멀맵 텍스처, §3 광원 리그 수치, §4 재질 표, §3.4 횃불 좌표.
- **버려지는 것:** N1·N2의 JS 베이크 코드 전부.
- **그래서 웹에 WebGL 파이프라인을 새로 짜지 않는다** — 그건 버려지는 쪽에 큰돈을 넣는 짓이다.

---

## 6. 구현 순서

```
A0. §9 결재 4건                          ✅ 완료 (2026-07-31, 전건 권고안)
A1. 투영 단일화 — buildArenaBg가 CX/CY/RX/RY 참조 ✅ 완료 (2026-07-31, 8방위 5/8→8/8)
A2. 높이 버퍼 + Sobel + N·L 베이크 (바닥)  ✅ 완료 (2026-07-31) → §6.1
A3. 벽 줄눈·균열 절차 생성 + 베이크 (최대 수확)  ✅ 완료 (2026-07-31) → §6.2
A4. 철문·CornerZone·발자국 + 그림자 L 방향화·림라이트  ✅ 완료 (2026-07-31) → §6.3
A5. venue 4종 프리셋 + [14] 훅  ✅ 완료 (2026-08-01) → §6.5
A6. 스프라이트 18장 베이크                 ⏸ 보류 (§9 결정 4)
A7. Godot 이관 시 에셋 인계                        → [12] 로드맵
```

**착수 범위 = A1 ~ A5.** A1은 광원과 무관한 좌표 버그 수정이라 단독으로 먼저 끝내고 스샷으로 확인하는 게 안전하다.

### 6.1 A2 구현·검증 기록 (2026-07-31)

**들어간 것** (전부 `buildArenaBg` 안 + 그 위 상수 블록 — `render()` 무접촉):

- `LIGHT`(§3.3 확정 L) · `LAMB`=0.35 · `RELIEF`=1/300 상수화. 광원을 바꾸려면 여기 한 곳만 고친다.
- 높이 버퍼 `hcv`(128=평면). **알베도와 같은 루프에서** 쌓는다 — 갈퀴 자국은 홈(`#5c5c5c`, 강), 모래알은 ±5(약). 난수열이 어긋나면 알베도와 높이가 다른 그림이 되므로 `rnd()` 호출 순서는 그대로 두고 결과만 두 캔버스에 나눠 썼다.
- `bakeRelief()` — Sobel → N → `I = (ambient + (1−ambient)·max(0, N·L)) / (ambient + (1−ambient)·Lz)`. **분모가 핵심**: 평면이 정확히 I=1이라 팔레트·venue 색이 그대로 보존되고 바뀌는 건 요철뿐이다. 높이를 안 쌓은 곳(벽·관중석)은 기울기 0이라 루프에서 즉시 건너뛴다.
- **§3.2 모순 음영 2건 폐기** — 바닥 방사 하이라이트를 좌상(`−0.22RX, −0.45RY`)으로, 벽 그림자를 L의 방위각에서 유도한 좌상 림→안쪽 그라디언트로. 이제 두 음영과 요철 조명이 같은 L 하나를 쓴다.
- 배경 프리렌더가 부각별 2벌(§2.4.3)이므로 **두 벌 모두** 구워진다.

**검증:**

| 항목 | 결과 |
|---|---|
| 베이크 시간 (2벌 합) | **181 ms** (기본 101.7 + 줌 79.7) — 예산 200 ms 이내지만 여유 없음 |
| 프레임 루프 | `render()` diff 0줄 · 배경 `drawImage` 여전히 1~2장 — **런타임 비용 증가 0** |
| 방향성 (국소 대비, 9×9 평균 제거로 광역 음영 상쇄) | ĝ·L = −0.88 → **+26.7** · 0 부근 → **≈0** · +0.88 → **−31.4**. 코사인 단조 + 수직에서 영교차 = N·L 서명 |
| A1 회귀 (8방위) | 8/8 |
| 콘솔 · designlint | 오류 0 · 위반 0 |

> 국소 대비를 쓴 이유: 원시 밝기로 재면 방사 그라디언트·벽 그림자가 섞여 수직 방향에서도 31이 나온다(광역 음영 누출). 9×9 평균을 빼면 요철 성분만 남아 **수직에서 정확히 0**이 나온다 — 이게 조명이 L을 따른다는 증명이다.

**미룬 것:** §3.4의 **환경광 색**(`#6b7a96`). 지금은 스칼라 환경광이라 그림자가 색을 안 띤다. 모래 단독으로는 차이가 미미하고, 색이 실제로 필요해지는 건 벽·venue 프리셋이라 **A3/A5에서 함께** 넣는 게 맞다. → **A3에서 처리됨(§6.2).**

### 6.2 A3 구현·검증 기록 (2026-07-31)

**들어간 것** (전부 `buildArenaBg`/`bakeRelief` 안 — `render()` 여전히 무접촉):

- **벽 링 클립**: 바깥 타원 + 바닥 타원을 한 `Path2D`에 담고 `clip(ring,'evenodd')` → 9 px 띠에만 그린다.
- **베벨**: 안쪽 118 → 바깥 152로 높아지는 동심 타원 10줄. 링 전체가 L에 대해 한 방향으로 기울어 **두께가 생긴다** — 줄눈보다 이쪽이 "벽이 벽으로 보이는" 데 더 크게 기여한다.
- **줄눈**: 48칸 2단 엇갈림 쌓기(위단을 반 칸 밀어 벽돌 패턴) + 단 사이 수평 줄눈. 알베도는 `rgba(0,0,0,.13)`으로 은은하게, 높이는 `#4e4e4e`로 강하게 — **읽히는 건 조명이지 선이 아니다.**
- **균열 9줄** (중간 강도).
- **환경광 색**(A2에서 미뤄둔 것): 그늘 정도 `s`에 비례해서만 `#6b7a96` 쪽으로 민다. 평면은 `s=0`이라 **팔레트가 그대로** — A2의 정규화 원칙을 그대로 지킨다.

**검증:**

| 항목 | 결과 |
|---|---|
| 벽 높이 적재 | 링 표본 2000점 중 **94.3%** 비평면 (A2까지는 0%) |
| 벽 방향성 (국소 대비) | ĝ·L = −0.88 → **+12.4** · 0 → **≈0** · +0.88 → **−15.1** — 바닥과 같은 N·L 서명 |
| 바닥 회귀 | +22.9 / −28.4 (A2와 동일) — 벽 작업이 바닥을 안 건드렸다 |
| 환경광 색 | 파랑/빨강 비 **0.453(양지) → 0.498(그늘)** = 그늘이 9.9% 더 푸르다 |
| A1 회귀 | 8/8 |
| 프레임 루프 | `render()` diff 0줄 · 배경 `drawImage` 1~2장 |
| 콘솔 · designlint | 오류 0 · 위반 0 |

**⚠ 베이크 예산 초과 → §7 기준 개정.** 벽을 얹자 **253 ms**로 200 ms를 넘겼다. 두 가지를 고쳐 되돌렸다:

1. **높이를 1채널 `Uint8Array`로 압축 후 Sobel.** RGBA 스트라이드(4)로 이웃 8칸을 찌르면 캐시가 계속 빗나간다. 이 한 줄이 가장 컸다.
2. **행별 x범위 클리핑.** 바깥 타원 밖은 전부 평면인데 스캔하고 있었다 — bbox 모서리 4곳(약 21%)을 통째로 건너뛴다.

결과: **웜 172~193 ms(중앙값 193) · 콜드 첫 빌드 ≈ 296 ms.** 실제 로드 경로는 콜드다.

> ⚠ 이 개정은 **A4에서 다시 깨진다**(§6.3). 근본 원인은 최적화가 아니라 **프리렌더 해상도 과잉**이었다.

> **기준 개정:** §7의 "베이크 200 ms 이내"는 **콜드 300 ms · 웜 200 ms 이내**로 고친다. 근거 — ⓐ 로드 1회뿐이고 프레임 루프에서 완전히 벗어나 있다, ⓑ 그 시점엔 `viewer.json` fetch와 스프라이트 디코드가 동시에 도는 구간이라 체감 경로가 아니다, ⓒ 200 ms는 애초에 이 문서가 스스로 세운 눈금이지 외부 요구가 아니다. **A5에서 venue별로 다시 구우면 이 예산을 또 건드린다** — 그때는 광원이 바뀐 벌만 다시 굽는 부분 재베이크가 필요하다.

---

### 6.3 A4 구현·검증 기록 (2026-07-31)

**들어간 것 5종.** 방향이 필요한 것은 전부 `LDIR`(§3.3 L의 화면 방위) **하나**만 읽는다.

| 항목 | 구현 | 검증 |
|---|---|---|
| **접지 그림자 L 방향화** | 발밑 정원 → 빛 반대쪽(우하)으로 5 px 밀고, 장축을 방위각에 맞춰 10→13 px로 늘림 | 격리 테스트: 그림자 무게중심 (2.43, 3.40) → **빛 반대 방향과 cos = 1.000** |
| **림라이트** (결정 4 대체안) | 실루엣을 빛 반대쪽으로 2 px 밀어 `destination-out` → 광원 쪽 초승달만 남김. 전용 오프스크린(`_rcv`) | `_rcv` 직접 판독: 오른쪽 볼 때 cos **0.959**, 왼쪽 볼 때 cos **0.975** (둘 다 화면 좌상단) |
| **철문 2곳(포르타)** | 벽 링 클립 안 환형 섹터 + 쇠창살 6줄. 높이는 문틀 깊게(`#3c3c3c`)·창살 높게(`#9a9a9a`) | 문 밝기 **35.1 / 29.1** vs 일반 벽 68.7 · **바닥 침범 0/800**(§1 계약) |
| **CornerZone 표시** | 선을 새로 긋지 않았다 — 기존 경기 라인 `0.88`이 우연히 `(12−1.5)/12 = 0.875`였다. 눈금을 정확히 맞추고 **의미를 붙였다** | 상수 대조 |
| **발자국 누적** | 프레임에서 파생 → `t` 필터(혈흔과 같은 스크럽 규약). **월드 좌표로 저장**해 그릴 때 투영 | 150개 · 말미 137개 노출 |

**⚠ 림라이트 오판 정정.** 첫 두 측정에서 "왼쪽을 볼 때 림이 반대편에 붙는다"는 결과가 나왔으나, **측정이 틀린 것이었다** — 스프라이트 자체의 밝은 픽셀을 림으로 세고 있었다. 림만 담긴 `_rcv`를 직접 읽자 양쪽 다 정상. 반전 보정 코드는 처음부터 옳았고 **코드는 고치지 않았다.**

**⚠ 실제 버그 1건 (고침).** 철문이 안 그려졌다. `buildArenaBg`에는 `c.restore()`가 **두 개**(벽 링 클립 / 바닥 타원 클립) 있는데 철문을 뒤쪽 블록에 넣어 **바닥 클립에 잘려 나갔다**. 벽 블록 안으로 이동. → 교훈: 이 함수에서 무언가를 추가할 때는 **어느 클립 안인지 먼저 확인**할 것.

**프레임 비용** (A4는 배경이 아니라 프레임 루프를 건드리는 첫 단계다):

| | 경기 말미 render() |
|---|---|
| 발자국 포함 | **5.0 ms** (60 fps 예산 16.7 ms의 30%) |
| 발자국 비용 | **1.08 ms** |

> 초판은 보폭 0.55 m·상한 240이라 자국이 **카펫**이 되고 프레임의 55%(2.91 ms)를 먹었다. 보폭 0.9 m·상한 150으로 성기게 — 성능만이 아니라 **보기에도 낫다**(밟힌 자리가 읽혀야지 바닥이 갈려선 안 된다).

**⚠ 베이크 예산 재초과 — 그리고 진짜 원인.** 철문까지 얹자 **웜 240~300 ms · 콜드 361 ms**로 §6.2의 개정 기준마저 넘겼다. GC 스파이크(1065 ms)는 높이 스크래치 버퍼 재사용으로 제거했지만 중앙값은 263 ms다. 계측 중 **근본 원인**이 드러났다:

```
배경 프리렌더 폭 : 1880 px  (BGS = 2)
월드 캔버스 폭   :  470 px  (PX = 2, §2.4.2 2층 렌더)
줌 1.6배 환산    :  752 px
```

**즉 표시될 수 있는 것보다 2.5~4배 높은 해상도를 굽고 있다.** `BGS`를 2→1로 내리면 베이크가 약 1/4(≈65 ms)로 떨어지고 예산 문제가 통째로 사라진다. 다만 `BGS`/`PX`는 §2.4의 **라니스타 결재 계약**(픽셀 균일성)에 얽혀 있고, 패자 줌인 2.9배에서 배경이 뭉갤 수 있다 — **내 판단으로 바꾸지 않았다. §9 결정 5로 올린다.**

**기타:** A1 회귀 8/8 · 요철 방향성 +14.1/−20.4 유지 · 콘솔 0 · designlint 0.

> **손대지 않은 인접 결함(보고만):** 혈흔 데칼 `stains`는 로드 시점의 **화면 좌표**를 굳혀 담는다. `RY`는 줌 모드에서 부각에 따라 변하므로(§2.4.3) 줌 상태에서 혈흔이 어긋난다. A4의 발자국은 월드 좌표로 담아 이 문제가 없다. 기존 코드의 문제라 이번 범위에서 건드리지 않았다.

---

### 6.4 A3·A4 결함 정정 — **화면을 안 보고 있었다** (2026-07-31)

라니스타 지적("제대로 안 되고 있는 것 같다")으로 실물을 처음 봤다. **A3의 벽 디테일이 화면에 전혀 도달하지 않고 있었다** — 줄눈도 철문도 없이 뭉개진 갈색 띠였다.

**원인은 해상도 사슬이다.**

```
논리 좌표계      940 px
배경 프리렌더    940 × BGS = 1880 px      ← A2·A3 검증이 잰 곳
월드 캔버스      940 / PX  =  470 px      ← 실제로 화면에 가는 곳 (§2.4.2)
따라서 축소율     1880 → 470 = 4배
```

| 그린 것 | 논리 px | 월드 px | 결과 |
|---|---|---|---|
| 줄눈 | 1 | **0.5** | 사라짐 |
| 철문 창살 | 1 | **0.5** | 사라짐 |
| 베벨 10단 | 각 0.9 | 각 0.45 | 뭉개짐 |

**내 검증이 전부 프리렌더 캔버스를 쟀다.** 거기서는 줄눈이 선명하니 "94.3% 비평면", "방향성 cos 단조" 같은 수치가 전부 통과했다. 화면에 없는 것을 검증하고 있었다.

**⚠ 그리고 §9 결정 5의 전제가 틀렸다.** `BGS`는 이 문제와 **무관**하다. 선 굵기 L 논리px는 화면에서 `L·BGS / (2·BGS) = L/2` 월드px가 되어 **`BGS`가 약분된다**. 가시성 한계를 정하는 것은 오직 `PX`다. (`BGS`는 카메라 줌 2.9배에서 프리렌더를 확대 표본할 때만 값을 한다 — 그래서 낮추면 안 된다.)

**고친 것:**

| | 전 | 후 |
|---|---|---|
| 줄눈 | 48칸 · 1 px · alpha .13 | **24칸 · `PX` px · alpha .30** |
| 철문 창살 | 6개 · 1 px · 어두운 색 | **4개 · `PX` px · 밝은 색**(어두운 통로 위라 밝아야 선다) |
| 균열 | 1 px · alpha .10 | `PX` px · alpha .22 |
| 갈퀴 자국 | 5줄 · 각 2.2~4.2 rad 대형 스윕 | **9묶음 × 3갈래 · 0.5~1.2 rad · 더 얕게** |

> 갈퀴 자국은 가시성 문제가 아니라 **모양** 문제였다. 아레나를 가로지르는 거대한 호 5개는 모래 고른 자국이 아니라 **긁힌 상처**로 보였다. 고르는 도구는 여러 갈래이므로 짧은 호를 나란히 3줄씩.

**새 규약 2개:**

1. **배경 디테일의 최소 단위는 `PX` 논리 px다.** 그보다 얇은 선은 그리나 마나다. 저해상 월드에 그린다는 것을 그릴 때부터 계산에 넣을 것.
2. **검증은 화면 캔버스에서 한다.** 프리렌더 픽셀을 재는 것은 증명이 아니다. 이번엔 캡처 수신용 로컬 싱크(`scratchpad/shotsink.py`)를 띄워 `cv`를 JPEG로 POST 받아 **눈으로 확인**했다. Browser pane이 프레임을 합성하지 않아 스크린샷 도구가 막히는 이 프로젝트에서는 이 경로가 필요하다.

**검증(화면 실물):** 좌·우 철문이 어두운 통로 + 밝은 창살로 읽힘 · 벽 줄눈이 리듬으로 보임 · 갈퀴 자국이 고른 모래로 보임 · A1 회귀 8/8 · designlint 0 · 말미 render 5.4 ms.

**⚠ 미해결 — 베이크 시간.** 선이 굵어지며 비평면 픽셀이 늘어 **542 ms**까지 뛰었다. 음영 계산을 2 px 격자로 내려(앨비도는 `BGS`=2 유지, 음영만 1/4 표본 — 표시 해상도 아래라 육안 손실 없음) 낮췄으나, 이 환경의 측정 편차가 **295~960 ms로 3배**라 신뢰할 수 없다. 실제 체감은 5173에서 라니스타가 직접 볼 것. → §9 결정 5를 이 사실에 맞춰 다시 씀.

---

### 6.5 A5 구현·검증 기록 (2026-08-01)

**광원을 상수에서 프리셋으로.** `LIGHTS`(venue 4종) + `WEATHERS`([14] 3종)를 로드 시 1회 합성해 `LIGHT`/`LSUN`/`LAMB`/`LAMBC`/`LDIR`/`RIMCOL`을 유도한다. 벡터가 아니라 **방위·고도(도)로 적는다** — 사람이 읽고 고칠 수 있어야 한다. `az 233 · el 41`이 §3.3 확정치이고, 기본값(arena)이 A4까지의 벡터를 **정확히 재현**하는 것을 회귀로 확인했다.

| venue | az/el | sun | amb | 요철 |
|---|---|---|---|---|
| `arena` 콜로세움 | 233 / 41 | 1.00 | 0.35 | ○ |
| `spar` 수련장 | 233 / 41 | 0.80 | 0.45 | ○ |
| `street` 뒷골목 | 233 / **15** | 0.70 | 0.25 | ○ |
| `bar` 선술집 | — | 0.30 | 0.20 | **× (베이크 정지)** |

`bar`는 실내라 태양이 없다. 횃불은 동적 광원이라 웹에서 못 굽는다(§3.4) → `bakeRelief` 자체를 건너뛴다. **부수 효과로 이 venue의 베이크가 16.5 ms다**(다른 venue는 270~300 ms) — 베이크 비용이 사실상 전부 요철 패스임이 이걸로 확정됐다.

**[14] 훅 3종** (`?weather=night|rain|sand`) — 조명·색보정만. Sim·매트릭스 무접촉([14] 원칙 1·3).

**⚠ 설계 결함 1건 발견·수정 — `sun`/`amb`만으로는 밤이 안 온다.** 첫 구현에서 `night`가 **낮보다 밝게** 나왔다. 원인은 A2의 정규화다: `I = (amb + D·N·L) / (amb + D·Lz)`는 평면을 **항상 I=1로 고정**하므로, `sun`·`amb`를 낮추면 *요철 대비*만 줄고 전체 밝기는 그대로다. 팔레트 보존을 위해 일부러 넣은 성질이 분위기 연출을 막고 있었다.

→ **노출·색보정 단계를 분리**했다. 각각 `fillRect` 1회, 프리렌더 안:
- `grade` — 곱연산. 어둡게·색 입히기 (`night` `#42506e` · `rain` `#8791a0`)
- `haze` — 반투명 워시. 대비 낮추기 (`sand` `rgba(214,176,116,.30)`)

관중석·무대 밖까지 전부 걸려야 분위기가 산다 — 그래서 팔레트(f0/f1/wall)를 미는 방식이 아니라 캔버스 전체에 건다.

**검증 (전부 화면 캡처를 눈으로 — §6.4 규약):** venue 4종 + 날씨 3종을 각각 실제 로드해 캡처·대조.

| | 바닥 평균 밝기 |
|---|---|
| arena (기준) | ~190 |
| night | **60.4** |
| rain | **110.9** |
| sand | **192.7** (워시로 대비↓) |

기타: 기본값이 §3.3 벡터 재현 ✓ · A1 회귀 8/8 · 철문 18.0 vs 벽 33.2 ✓ · 베이크 266~469 ms(결정 5 ⓐ에 따라 그대로 둠) · designlint 0 · 콘솔 0.

> **미구현(Godot 이관 몫):** `rain`의 스펙큘러(젖은 모래 반사)와 `night`의 실제 횃불 광원. 둘 다 **동적 광원**이라 로드 시 1회 베이크로는 표현할 수 없다 — N3에서 `PointLight2D`로 받는다(§5). 지금은 색보정으로 인상만 만든다.

---

## 7. 검증 기준

- **성능:** 프레임 루프 `drawImage` 호출 수 **증가 0**. 베이크는 **콜드 300 ms · 웜 200 ms** 이내(로드 1회, §6.2에서 개정). 실측 fps A/B.
- **정합:** 모든 그림자·하이라이트 방향이 §3 확정 L **하나**에서 유도됨. 모순 음영 0(§3.2가 지금 위반).
- **좌표:** 원 경계 8방위에 선수를 놓고 발이 전부 모래 위(§2.3 회귀).
- **Sim 무접촉:** `Morituri.Sim` diff 0줄 → **`matrix` CLI 재검증 불필요**. 한 줄이라도 바뀌면 전 무기 A/B 필수.
- **A/B 증명:** CSS·시각 변경은 전후 스샷으로 효과를 증명한 뒤 보고([[client-verify-traps]]).
- **에셋 등록:** 신규 정적 파일은 `csproj` Link 필수([[presentation-design-system]]).

---

## 8. 비용 정직하게

| 단계 | 작업량 | 체감 효과 | 이식 시 잔존 |
|---|---|---|---|
| A1 투영 단일화 | 소 | (버그 수정) | — |
| A2 바닥 노멀 | 중 | 중 | 규칙만 |
| **A3 벽 노멀** | 중 | **대** | 규칙만 |
| A4 소품 + 그림자·림라이트 | 중 | 중 | 좌표 |
| ~~A6 스프라이트 노멀~~ ⏸ | **대** | 소 (78 px) | 텍스처 ✅ |

**요약: A3가 가장 남는 장사, A6가 가장 밑지는 장사** → 그래서 A6는 보류(§9 결정 4).

---

## 9. 라니스타 결재 — **4건 전부 확정 (2026-07-31)**

| # | 안건 | 결정 | 귀결 |
|---|---|---|---|
| 1 | **광원** | **안 B「늦은 오후」** — L = (−0.45, −0.60, 0.66), 고도 41°, 좌상단 | §3.3이 이 문서의 단일 광원. §3.2의 기존 음영 2건(바닥 그라디언트·벽 그림자)은 **폐기 후 재계산** |
| 2 | **관중석** | **현행 점묘 배경 유지** | 조명 대상 아님(§4). 추가 비용 0. 단차 지오메트리는 Godot 이관 후 재검토 |
| 3 | **장애물** | **도입하지 않음** | §1 계약 확정 — 원 안에는 시각 소품도 놓지 않는다. **`Morituri.Sim` diff 0줄 = `matrix` 재검증 불필요** |
| 4 | **스프라이트 노멀맵** | **후순위 — A6 보류** | 대체안: 접지 그림자를 L 방향으로 + 림라이트 한 줄. 노멀맵 텍스처는 Godot 이관(N3) 시점에 재검토 |

### 결정 5 (신규) — **미결: 베이크 시간을 어디까지 감수할 것인가**

> **초판 정정:** 이 항목을 처음엔 "`BGS`를 2→1로 내리자"로 썼으나 **전제가 틀렸다**(§6.4). `BGS`는 화면 디테일과 무관하게 약분되고, 줌 2.9배에서만 값을 한다 — **내리면 안 된다.** 남은 것은 순수한 시간 문제다.

베이크는 로드 1회, 프레임 루프 밖. 현재 측정 **295~960 ms(편차 3배, 환경 신뢰도 낮음)**.

| | ⓐ 그대로 둔다 (권고) | ⓑ 더 깎는다 |
|---|---|---|
| 근거 | 로드 1회 · `viewer.json`(2611프레임) 파싱과 겹치는 구간 · 프레임 루프 영향 0 | 저사양에서 초기 진입이 늘어질 수 있음 |
| 비용 | 없음 | 그리기 단계 분해·부분 재베이크 설계 필요(A5에서 venue별로 다시 구우면 어차피 필요) |

> **확정 ⓐ (라니스타 2026-08-01):** "로드는 안 거슬린다." 그대로 둔다. 부분 재베이크 설계도 불필요 — venue·날씨는 **URL로 정해져 로드 시 1회만** 굽는다(경기 중 전환 없음). A5 구현으로 확인된 사실: 베이크 비용은 사실상 전부 요철 패스다(요철을 끈 `bar`는 16.5 ms). 나중에 정말 깎아야 하면 손댈 곳은 거기다.

**확정에서 따라오는 것:**
- 착수 범위 = **A1 → A5**. A6는 보류, A7은 [12] 로드맵 소관.
- 이 4건이 잠긴 덕에 **베이크가 1회로 끝난다** — 광원이 흔들리면 §4 전 재질을 다시 굽는다. 이후 광원을 바꾸고 싶으면 이 표를 먼저 고치고 A2부터 재실행할 것.
- 결정 3이 이 문서를 **Sim 무접촉 문서**로 만든다. 구현 중 `Morituri.Sim`에 한 줄이라도 손이 가면 그건 설계 이탈이니 멈추고 이 표로 돌아온다.


---

## 10. 배경 아트 방향 (v0.1 초안 · 2026-08-01)

**상태:** 초안 — §10.9 결재 대기. A1~A5는 조명 작업이었고 여기서 끝났다. 이 절은 **조명이 비출 대상**을 다시 만드는 이야기다.

### 10.1 진단 — 조잡함의 정체는 조명이 아니다

라니스타 지적("실제 게임이라고 느껴지지 않는다"). 원인 5가지, 큰 순서대로.

1. **캐릭터와 배경이 다른 어법이다.** 검투사는 Blender Cycles로 구운 픽셀아트다 — Freestyle 실루엣선(순수 검정)·조명비 2.0·림 14. 배경은 `ellipse()`와 `createRadialGradient()`로 그린 부드러운 벡터다. 같은 픽셀 격자에 올려뒀을 뿐(§2.4.2) **그림의 문법이 다르다.** 또렷한 인물이 뿌연 도형 위에 서 있으니 합성처럼 보인다. — **이게 8할이다.**
2. **수직 구조가 없다.** 포디움 벽이 9 px 테두리다. 실물은 사람 키의 두 배이고, 카메라 앞쪽에서는 **벽면이 화면을 가로지르며 아래 관중석을 가린다.** 지금은 바닥 타원이 검은 배경에 떠 있다 — 장소가 아니라 도형이다.
3. **관중이 노이즈다.** 랜덤 점 990개는 군중이 아니라 지지직거림이다. 군중은 **가로 띠의 명암 구조**로 읽힌다(단마다 값이 갈리고, 통로가 검게 끊고, 가끔 색이 튄다).
4. **프레임이 없다.** 하늘도 차양도 건축 실루엣도 없어 "경기장을 본다"가 아니라 "타원을 본다"가 된다.
5. **절차 생성의 규칙성.** 코드가 만든 형태는 눈이 금방 알아챈다. 완성된 배경은 마모가 고르지 않고 얼룩이 이유 있는 자리에 있다.

### 10.2 어법 계약 — 배경도 스프라이트와 같은 파이프라인으로 굽는다

`Morituri/tools/spritebake/bake.py`가 이미 정사영 카메라 · `--elevation` · Freestyle · 슈퍼샘플링을 갖췄고 **부각 20°가 `CamTilt=0.34`와 맞춰져 있다**. 아레나를 같은 도구로 굽는다.

| | 스프라이트(현행) | 아레나(이 설계) |
|---|---|---|
| 렌더러 | Blender Cycles 정사영 | **동일** |
| 부각 | 20° | **20°/15° 두 벌**(§2.4.3) |
| 윤곽선 | Freestyle 실루엣만·순수 검정·크리스 OFF | **동일 규칙** |
| 조명비 | `--contrast 2.0` | 동일 계열 |
| 림 | `--rim 14` | 사용(벽 상단 분리) |

> **크리스 선 금지는 배경에서 더 중요하다.** 8000버텍스 캐릭터에서 형체가 진흙이 됐던 실측(스프라이트 README §6)은 계단·아치가 즐비한 건축에서 훨씬 심하게 재현된다. **실루엣·경계만.**

### 10.3 구도와 값 구조

지금은 화면 전체가 중간 갈색이라 눈이 쉴 곳도 볼 곳도 없다. 값을 3단으로 갈라 **시선을 모래로 몰아넣는다.**

| 층 | 값 | 내용 |
|---|---|---|
| 프레임 (어둡게) | 0.10~0.25 | 차양 그늘 · 상단 관중석 · 화면 네 귀 |
| 중간 | 0.30~0.50 | 포디움 벽 · 하단 관중석 · 통로 |
| **초점 (밝게)** | 0.60~0.85 | **모래 바닥** — 전투가 벌어지는 곳만 밝다 |

- 검투사는 어두운 갑옷이다 → 밝은 모래 위에서 실루엣이 선다. **값 대비가 곧 가독성이다.**
- 광원은 §3.3 `az 233 · el 41` 그대로. Blender 키라이트 각도로 그대로 옮긴다.

### 10.4 세울 것 (건축 요소)

**§1 계약 유지: 충돌은 원 r=12 m, 원 안에는 아무것도 없다.** 아래는 전부 원 바깥 또는 벽면이다.

| # | 요소 | 역할 | 비고 |
|---|---|---|---|
| 1 | **포디움 벽** (높이 3~4 m) | 아레나를 '구덩이'로 만든다 | 앞쪽 가림 정도 = §10.9 결정 3 |
| 2 | **철문 2곳**(포르타) | 입·퇴장의 근거 | A4에서 이미 자리 잡음 — 좌·우 |
| 3 | **관중석 3단** + 통로(vomitoria) | 가로 띠 명암 구조 | 군중 처리 = §10.9 결정 1 |
| 4 | **차양(velarium)** 부분 | 상단 프레임 · 그늘 | 유무 = §10.9 결정 2 |
| 5 | **황제석/귀빈석** 1곳 | 시선 앵커 · [13] 황제 방문 연출 훅 | 관중석 한 구획 |
| 6 | 시체 끄는 문(포르타 리비티나리아) | 서사 소품 | 철문 중 하나를 겸해도 됨 |
| 7 | 벽면 마모·얼룩·낙서 | 규칙성 제거 | 손으로 불균일하게 |

### 10.5 재질 팔레트

지금은 갈색 하나다. 최소 4종으로 가른다 — **재질이 갈려야 형태가 읽힌다.**

| 재질 | 값/색 | 어디 |
|---|---|---|
| 트래버틴 (밝은 회백~미황) | 0.55~0.70 | 포디움 벽 · 관중석 구조 |
| 붉은 모래 | 0.60~0.85 | 바닥 (초점) |
| 검은 공동 | 0.05~0.15 | 통로 · 문 안쪽 · 아치 뒤 |
| 철 / 목재 | 0.20~0.35 | 창살 · 난간 · 도르래 |

### 10.6 카메라·산출물 규격

기존 계약(§2.4)을 **그대로** 쓴다 — 여기서 어긋나면 스프라이트와 바닥이 따로 논다.

```
부각        20°(기본) · 15°(줌)        → --elevation 20 / 15, 두 벌
산출 해상도  1880 × 880 (BGS=2 유지)
            근거: 줌 2.9배에서 필요한 표본 = 1.45 × 940 = 1363 px < 1880
산출물      albedo.png · normal.png (· ao.png 선택), 부각별로 각 2장
선 굵기     화면 1 px 기준 역산 → 프리렌더 약 2.5 px (--lines 2.5)
            (줌 1.6에서 1 논리px = 0.8 월드px, 1 프리렌더px = 0.4 월드px)
디테일 하한  §6.4 규약 유지 — 화면 1 px 아래로 내려가는 요소는 그리지 않는다
등록        csproj Link 필수
```

**노멀맵이 진짜가 된다.** A2·A3은 절차 텍스처에서 Sobel로 높이를 *추정*했다. Blender는 지오메트리에서 노멀을 바로 굽는다 — 코드는 줄고 결과는 좋아진다.

### 10.7 A1~A5 정산 — 무엇이 남고 무엇이 버려지나

| | 처리 |
|---|---|
| §3.3 광원 리그 (`az 233·el 41`) | **남는다** — Blender 라이트 각도로 이관 |
| §3.5 venue·날씨 프리셋 · `grade`/`haze` | **남는다** — 색보정은 베이크 위에 그대로 |
| `LDIR` 규약(그림자·발자국·림라이트 방향) | **남는다** — 프레임 루프 코드 |
| A1 투영 단일화 · §2.4 카메라 계약 | **남는다** |
| §4 재질·높이 소스 표 | 대체 — §10.5가 이어받는다 |
| A2·A3 절차 텍스처 + `bakeRelief` | **버린다** — 베이크 이미지가 대신한다 |
| §6.4 디테일 하한 규약 | **남는다** — 3D 베이크에도 그대로 적용 |

> 즉 **조명 설계는 살아남고 조명 구현만 교체된다.** 이 문서 §0~§9가 헛일이 아니라 §10의 전제다.

### 10.8 작업 순서

```
B0. 이 절 결재 (§10.9 4건)               ✅ 완료 (2026-08-01)
B1. 블록아웃 — 구도·값 3단 확인           ✅ 완료 (2026-08-01) → §10.10
B2. 재질·조명 (§10.5 + §3.3 각도)         ◐ 1차 완료 (2026-08-01) → §10.11
B3. 베이크 2벌 (20°/15°) + 노멀           ✅ 완료 (2026-08-01) → §10.16
B4. 뷰어 교체 — buildArenaBg → drawImage  ✅ 완료 (2026-08-01) · 군중 띠 포함
B5. venue 4종 (결정 4에 따라 3D or 색 변주)  ✅ 완료 (2026-08-04) → §10.23
    └ 남은 결재 1건: 비-arena의 **건축**을 어떻게 할 것인가(§10.23 A/B/C)
```

**B1이 관문이다.** 회색 상자 단계에서 축소판을 봤을 때 "구덩이에서 둘이 싸운다"가 안 읽히면, 재질과 조명을 아무리 얹어도 안 읽힌다.

### 10.9 라니스타 결재 — **4건 확정 (2026-08-01)**

| # | 안건 | 결정 |
|---|---|---|
| 1 | 군중 | **관중석 건축만 3D + 군중은 2D 띠 오버레이** — 싸고, 나중에 움직일 여지가 남는다 |
| 2 | 차양(velarium) | **넣되 화면에는 안 보인다 — 그림자만.** 프레임 밖 상공에 두어 시야를 잠식하지 않으면서 바닥에 줄무늬만 떨어뜨린다 |
| 3 | 앞벽 가림 상한 | **원 최남단 발밑까지 침범 금지** |
| 4 | venue 범위 | **arena만 3D**, 나머지 3종은 색·소품 변주 |

> 결정 2는 라니스타 안이다. 차양을 세우면 완성도는 오르지만 상단 시야를 먹는 문제가 있었는데, **보이는 것과 그림자를 분리**해 둘 다 얻었다. 정사영 카메라라 프레임 밖 물체는 렌더에 안 잡히면서 그림자는 그대로 진다 — 원근 카메라였다면 이 수가 안 통한다.

### 10.10 B1 블록아웃 결과 (2026-08-01)

`Morituri/tools/arenabake/blockout.py` — 파라메트릭 회색 블록아웃. Blender 5.2, `bpy`로 형상을 코드 생성한다(수동 모델링 없음 → 반복이 싸다).

**카메라 계약 검증 (핵심).** 뷰어의 피트 투영에서 역산한 정사영 카메라가 실제로 일치하는지 실측:

```
px/m     = RX / ArenaRadius = 400/12 = 33.333
ortho 폭 = W / (px/m) = 940 / 33.333 = 28.2 m
부각 20° → 반지름 12 m 원의 세로 = 12·sin20° = 4.10 m = 136.8 px  (뷰어 RY 136) ✓
```
렌더 실측: 바닥 **중심 x = 469** (기대 470) · 폭 820 px (기대 800 — 포디움 벽 두께가 같이 잡힌 분). **투영 일치.**

**⚠ 최대 발견 — 부각 20°에서는 앞쪽에 아무것도 못 세운다.**

높이 `h`인 앞벽은 그 뒤 `h / tan20° = 2.75h` 만큼의 바닥을 가린다.

| 앞벽 높이 | 가리는 바닥 깊이 | 지름 24 m 대비 |
|---|---|---|
| 3.5 m (설계안) | **9.6 m** | **40%** |
| 1.5 m | 4.1 m | 17% |
| 0.35 m (채택) | 0.96 m | 4% |

첫 렌더가 이걸 그대로 보여줬다 — 앞쪽 구조물이 아레나를 통째로 덮어 바닥이 안 보였다. **결정 3은 취향이 아니라 기하학이 강제하는 제약이다.** 처방은 연출 관례인 **앞쪽 컷어웨이**: 카메라 쪽 ±65°는 0.35 m 턱만 남기고(35° 페이드) 관중석은 통째로 걷어낸다. 실물 경기장을 자르는 게 아니라 **카메라 쪽 벽만 걷어내는** 것 — 2.5D 게임이 늘 하는 일이다.

**차양 그림자 전용 — 작동 확인.** 프레임 밖(z=20 m)에 슬랫을 깔면 렌더에는 안 잡히고 그림자만 진다. 두 가지가 필요했다:

1. **위치 보정** — 높이 `h`의 그림자는 `(L_xy/L_z)·h` 만큼 밀린다. 아레나에 떨어뜨리려면 차양을 그 **반대로** 미리 옮겨야 한다(h=20 m에서 약 `(+12.3, −16.4) m`).
2. **천은 반투명이어야 한다** — 불투명하면 그림자가 **검은 막대**가 되어 페인트칠처럼 보인다(첫 렌더에서 확인). 알파 0.58로 낮추자 '눌린 띠'가 됐다.

**컬러 설정은 스프라이트 베이크와 같아야 한다** — `view_transform = Standard`(AgX/Filmic 아님, `spritebake/bake.py:225`). 배경만 다른 변환을 쓰면 팔레트가 어긋난다(§10.2 어법 계약). Standard는 하이라이트가 바로 타므로 노출 −0.35.

**B1 합격 판정:** 25% 축소판에서 "구덩이에서 둘이 싸운다"가 읽힌다 — 밝은 타원 바닥 / 중간 값 벽 링 / 어두운 상단 관중석의 3단이 축소해도 유지된다(§10.3 값 계획 유효).

### 10.11 ⚠ 가시성 상한 — 세워도 안 보이는 것들 (2026-08-01)

B2 착수 직후 발견. **정사영 + 부각 20° + 프레임 세로 13.2 m**가 합쳐지면, 깊이가 깊어질수록 보이는 높이가 급격히 줄어든다.

```
화면 세로 = 0.342·y + 0.940·z          (y = 깊이, 뒤가 +)
프레임 절반 = ±6.60 m
→ 보이는 높이 상한  z ≤ (6.60 − 0.342·y) / 0.940
```

| 위치 | 깊이 y | 보이는 높이 상한 | 실제로 세운 높이 | 결과 |
|---|---|---|---|---|
| 아레나 남단(앞) | −12.0 | 11.39 m | — | 앞쪽은 여유가 많다 |
| 포디움 벽(뒤) | +12.6 | **2.44 m** | 3.5 m | 위 1.1 m는 프레임 밖 |
| 관중석 1단(뒤) | +17.6 | **0.62 m** | 4.7 m | **전부 밖** |
| 아케이드(뒤) | +30.2 | 음수 | 15.7 m | **전부 밖** |

**결론: 관중석은 좌·우 가장자리에서만 보인다.** 옆쪽은 `y ≈ 0`이라 상한이 7.0 m로 넉넉하지만, 아레나 **뒤쪽에는 포디움 벽 아래 2.4 m 말고 아무것도 안 나온다.** 아케이드는 그림자조차 아레나에 안 닿는다(h=15.7의 그림자는 14 m 밖으로 떨어진다) → `--attic` 플래그로 빼고 기본 OFF.

> 이건 2D 뷰어의 관중 점묘가 왜 어색했는지도 설명한다. 현재 배경은 군중을 **온전한 링**으로 그리는데, 실제 카메라에서 보일 자리는 좌·우 구획뿐이다. **결정 1(2D 띠 오버레이)을 그릴 때 링이 아니라 좌·우 두 덩어리로 그려야 한다.**

**B2 1차 결과 (§10.5 팔레트 적용):**

- 붉은 모래(초점) · 트래버틴 벽(중간) · 검은 공동 · 관중석 구조(프레임) — 값 3단 유지한 채 색만 입힘
- **앞쪽 관중석 실루엣** — 결정대로 하단에 걸었다. 위치는 취향이 아니라 계산이다: 반경 20 m에서 높이 2.60 m가 남단 발밑(화면 −4.10 m)을 안 넘는 한계이고, 단이 반경당 0.35씩 오르면 화면 높이가 그대로 유지되어 **윗선이 남단과 나란히 눕는다.**
- **윗선은 톱니여야 한다** — 매끈한 곡선은 '검은 막대'로 읽혔다. 좌석 등받이 46개(0.37 m ≈ 화면 12 px, 남단까지 5 px 여유)로 썰자 관중석으로 읽힌다.
- **서쪽 철문 광정(光庭)** — 왼쪽 바닥의 밝은 사다리꼴은 아티팩트가 아니라 **문으로 들어온 햇빛**이다. 광원이 좌상단(월드 −x·+y)이라 왼쪽 문을 통과한 빛이 오른쪽·앞쪽으로 떨어진다. 계산과 일치하고 보기에도 좋아 **살린다.**

**B2 2차 — 디테일 (`--detail`, 2026-08-01):**

| 항목 | 방법 | 메모 |
|---|---|---|
| **포디움 벽 석재** | 돌(앞)/줄눈(뒤) **2겹 래스** — 앞 겹에 구멍을 내면 뒤의 어둠이 줄눈으로 보인다. 6단 × 80칸, 단마다 반 칸씩 밀어 엇갈림 쌓기 | 화면 상단의 유일한 요소라 예산을 여기 몰았다. **이것 하나로 상단이 '덩어리'에서 '건축'이 됐다** |
| **모래 요철** | 절차 텍스처(Wave RINGS = 갈퀴 자국 + Noise = 모래알) → Bump | 지오메트리로 깎으면 폴리곤이 폭발하고, 어차피 B3에서 노멀맵으로 구워질 것이라 셰이더가 맞다 |
| **앞쪽 실루엣 변주** | 톱니 46개의 높이를 결정론적으로 흔들고 13% 확률로 난간 기둥 | 균일한 톱니는 그 자체로 규칙성이 보였다 |

> 조정 기록: 줄눈을 처음 0.07 m 깊이·0.45 배 어둡기로 팠더니 석조가 아니라 **울타리**로 보였다 → 0.035 m·0.62 배. 블록도 44칸(폭 1.7 m)은 너무 넓어 80칸(0.94 m)으로. **줄눈은 보이되 도드라지면 안 된다.**

**B2 3차 — 라니스타 피드백 반영 (2026-08-01):** *"모래 요철 세기 늘리고, 벽 디자인 너무 단순해."*

- **모래 요철 ↑** — Bump 거리 2.2 → 5.0배, 갈퀴 링 배율 3.4. 바닥이 평면에서 표면이 됐다.
- **벽 디자인** — 균일 격자는 콜로세움이 아니라 **벽돌담**이다. 세 가지로 깼다:
  ① **굽도리(plinth)·처마(cornice)** — 위아래를 매듭지어 벽면에 시작과 끝을 준다. 본체를 바깥으로 6 cm 물리고 이 둘만 `r=12`에 둬서 도드라지게 했다(안쪽으로는 여전히 한 톨도 안 나온다 — §1)
  ② **칸 너비 흔들기** — 같은 폭 80개가 늘어서면 눈이 돌보다 격자를 먼저 본다. 0.72~1.42배로 흔든다
  ③ **빠진 돌** — 5% 확률로 한 칸을 8.5 cm 뒤로 물려 세월을 넣는다(§10.1 진단 5)
- **좌·우 관중석을 좌석 열로** — 큰 단 3개는 '회색 쐐기'로 보였다. 라이저 0.42 / 트레드 0.78의 실제 카베아 비율로 14열. §10.11에 따라 어차피 좌·우 가장자리에서만 보이지만, **거기서만이라도 관중석으로 읽혀야** 한다.

> **좌·우 관중석을 왜 남기나** (라니스타 질문): §10.11로 관중이 화면에 나올 수 있는 자리가 거기뿐이기 때문이다. 뒤쪽은 프레임 밖, 앞쪽은 컷어웨이. 그리고 **[10] 군중게이지가 경기를 흔드는 실제 메커닉**이라 관중이 화면 어디에도 없으면 그 시스템이 붕 뜬다. 대안(잘라내고 네 귀를 어둠으로)도 성립하지만, 게이지가 있는 한 살리는 쪽이 맞다.

### 10.12 ⚠ 참고 이미지의 룩은 지금 카메라로 도달 불가 (2026-08-01)

라니스타가 로마 원형극장(풀라 아레나) 사진 2장을 참고로 제시. *"좌석이 잘 안 보여. 벽돌도 더 섬세하고 불규칙하게."*

벽돌은 해결했다(아래). **좌석은 기하학적 상한에 걸린다.**

```
화면 세로 = 0.342·y + 0.940·z          프레임 위 끝 = +6.60 m
아레나 뒤 가장자리(y=12)가 이미 4.10 m를 먹는다
→ 뒤쪽 구조물이 쓸 수 있는 월드 높이 총량 = (6.60 − 4.10) / 0.940 = 2.66 m
```

| 포디움 벽 | 화면 도달 | 좌석용 잔여 |
|---|---|---|
| 3.5 m (설계값) | 7.60 m | **프레임 초과** — 좌석 자리 0 |
| 2.4 m | 6.56 m | 0.04 m |
| 1.4 m | 5.63 m | 0.97 m |

**즉 벽 하나가 예산을 다 먹고 있었다.** 참고 이미지는 카메라가 훨씬 높고(30~40°) 화면비가 정사각에 가까워 **관중석 사발 전체**가 들어간다. 우리 프레임은 940×440 레터박스(세로 13.2 m)라 24 m 아레나를 담고 나면 위로 2.5 m밖에 안 남는다.

**이번에 한 것 — 무대 세트 수법(`backsquash`).** 카메라는 스프라이트 베이크에 묶여 못 올리므로(§2.4.4 규칙 1), **뒤쪽 구조물만 0.44배로 눌렀다.** 한 각도에서만 보는 그림이라 가능한 수다 — 옆쪽(y≈0)은 화면 여유가 7 m라 그대로 두고 뒤쪽만 납작하게. 그 결과 좌석 열이 벽 위로 올라와 **상단에 보이기 시작했다.** 다만 화면 40 px 남짓에 눌려 있어 참고 이미지의 사발과는 거리가 있다.

**참고 룩에 실제로 도달하려면 — 라니스타 결재 필요:**

| | 방법 | 대가 |
|---|---|---|
| ⓐ | **현행 유지** — '구덩이 뷰'로 간다 | 참고 이미지 룩은 포기. 비용 0 |
| ⓑ | **카메라 부각 ↑** (20° → 30~35°) | 스프라이트 **전량 재베이크**(무기 8 × 9포즈, 각도당 약 1시간). 바닥이 덜 눕고 게임 가독성도 바뀐다 |
| ⓒ | **무대 세로 ↑** (440 → 560 px 등) | 뷰어 레이아웃 전반 재조정. 스프라이트는 무사 |

> ⓑ와 ⓒ는 **§2.4 계약을 여는 일**이라 임의로 못 한다. ⓒ가 스프라이트를 안 건드려 싸지만 UI 배치가 전부 흔들린다.

**벽돌 개선 (요청 반영):** 8단 × 150칸(이전 5단 × 80칸) · 칸 너비 0.55~1.60배 흔들기(이전 0.72~1.42) · 빠진 돌 16%로 상향하고 물러나는 깊이도 2~9 cm로 흔든다. 좌석은 참고 이미지처럼 **밝은 석회석**(0.42)으로 올려 열이 읽히게 했다.

### 10.13 사진에 근접하는 방법 (2026-08-01)

라니스타 지시: *"텍스처·형태 등 사진에 근접하게 만들 방법을 고안해."*

**먼저 경제성부터.** 이 모든 것은 B3에서 **이미지 한 장으로 구워진다.** 런타임에 남는 건 `drawImage` 하나뿐이다. 즉 **셰이더가 아무리 복잡해도 게임 성능에는 0원**이다. 실시간 3D였다면 못 할 것들을 여기서는 다 해도 된다 — 이게 베이크 파이프라인을 고른 이유의 절반이다.

**원리: 블록아웃과 사진을 가르는 것은 색이 아니다.** 세 축이다.

| 축 | 블록아웃 | 사진 |
|---|---|---|
| **모서리** | 면도날처럼 각지다 | 깨지고 둥글다 |
| **때** | 없거나 균일하다 | **오목한 곳에만** 쌓인다 |
| **불규칙성** | 완벽한 원·같은 크기 | 무너지고 어긋난다 |

#### 이번에 넣어 효과를 확인한 것

| # | 수단 | 원리 | 비용 |
|---|---|---|---|
| 1 | **`Bevel` 노드** | 지오메트리 없이 셰이딩 단계에서 모서리를 굴린다. 각진 상자는 그 자체로 '3D 블록아웃'이라는 신호다 | 폴리곤 0 |
| 2 | **AO 마스크 때** | 때는 오목한 곳에 쌓인다. AO를 마스크로 쓰면 '더러워 보이는' 게 아니라 **'오래돼 보인다'** — 이 한 수가 가장 크다 | 노드 3개 |
| 3 | **Voronoi 블록별 톤** | 균일한 색은 페인트지 석재가 아니다. 셀 난수로 돌마다 톤을 흔든다 | 노드 2개 |
| 4 | **방사형 계단(vomitoria) 8곳** | 열이 통째로 이어지면 골판지다. 쐐기로 끊어야 '구역'이 생긴다 — 사진에서 관중석을 가장 알아보게 만드는 요소 | 각도 배열 |
| 5 | **무대 세로 560** | 관중석이 들어올 자리 자체를 만든다(§10.12) | 계약 개정 |

#### 다음 (효과 큰 순)

| # | 수단 | 왜 |
|---|---|---|
| 6 | **무너진 실루엣** — 관중석 상단·벽 윗선을 불규칙하게 깨기 | 사진은 **폐허**다. 완벽한 수평선이 남아 있으면 도면으로 보인다. 가장 값싸고 효과 큰 다음 수 |
| 7 | **깊은 아치 통로** — 철문을 납작한 홈이 아니라 실제로 뚫어 안쪽 어둠에 깊이를 준다 | 사진의 아케이드가 주는 인상의 대부분은 **구멍의 깊이**다 |
| 8 | **색 범위 확대** — 크림·회색·황토가 섞이게 ColorRamp를 3단 이상으로 | 지금은 한 색의 명암 변주라 석회석이 아니라 회벽으로 보인다 |
| 9 | **식생 몇 포기** — 계단 틈·벽 아래 | 사진에 풀이 있다. 폐허감을 크게 올리는데 비용은 작다 |
| 10 | **버트레스·계단참 등 2차 형태** | 실루엣에 리듬을 준다 |

> **하지 말 것:** 폴리곤을 늘려 해결하려는 시도. 표시 해상도가 월드 470 px(§6.4)이라 형태를 잘게 나눠도 화면에 안 남는다. **읽히는 것은 실루엣과 값 대비**이고, 그 둘은 위 수단으로 만든다.

#### 6~9 적용 결과 (2026-08-01)

| # | 결과 |
|---|---|
| 6 **무너진 실루엣** | 서로 안 맞아떨어지는 배음(3·5·8·13·21)의 합으로 각도별 높이를 흔든다. 난수를 그냥 뿌리면 지글거리고, 배음 합은 **연속이면서 주기가 안 읽힌다.** 관중석 윗선에만 걸었다 — 벽에 걸면 아래 문제가 재발한다 |
| 7 **아치 통로** | 개구부 윗선을 반원으로 깎고 그 위를 벽으로 채워 아치를 만든 뒤, 뒤로 물러나는 통로를 어둠으로 깔았다. **다만 철문이 az 0·180°라 화면 좌·우 끝에 걸려 대부분 잘린다** — 위치를 옮길지는 §10.14 |
| 8 **색 범위** | ColorRamp를 5단으로: 때(어두움) → 회색기 → 기본 → 황토기 → 밝은 석회석. 한 색의 명암 변주에서 벗어났다 |
| 9 ~~**식생**~~ | **제거(라니스타 지시).** 화면에서 몇 픽셀짜리 검은 점으로만 남아 폐허감이 아니라 **얼룩**으로 읽혔다. `--veg` 플래그로만 남긴다 |

#### 벽돌 재작업 — "너무 부자연스러워" (2026-08-01)

부자연스러움의 원인은 **돌 크기가 아니라 기계로 쌓은 듯한 균일함**이었다. 로마 오푸스 콰드라툼(다듬은 큰 돌 쌓기)으로 다시 짰다.

| | 이전 | 지금 | 이유 |
|---|---|---|---|
| 단 높이 | 8단 전부 동일 | **6단, 단마다 다름**(0.80~1.35배 후 정규화) | 같은 높이가 정확히 반복되면 눈이 돌보다 **격자**를 먼저 본다 |
| 빠진 돌 | 16% | **4%** | 16%는 세월이 아니라 **이가 빠진 것**으로 보인다 |
| 면 요철 | 없음(전부 평면) | **모든 돌에 ±1.5 cm** | 면이 고르지 않은 것이 진짜 석재의 신호다. 빠진 돌을 줄인 만큼 여기서 불규칙성을 얻는다 |
| 줄눈 | 두께 6%·밝기 0.62배 | **두께 6%→얇게·밝기 0.80배** | 두껍고 검으면 석조가 아니라 **격자무늬**가 된다 |

**⚠ `backsquash` 폐기 — 라니스타 지적 "가운데 벽만 낮은 거 고쳐".** 맞는 지적이었고, **세로 560 개정으로 애초에 불필요해졌다:**

| 무대 세로 | 프레임 위 끝 | 뒤쪽 높이 예산 | 벽 3.5 m 도달 |
|---|---|---|---|
| 440 | 6.60 m | 2.66 m | 7.60 m → **넘침**(그래서 눌렀다) |
| **560** | **8.40 m** | **4.57 m** | 7.60 m → **여유 0.80 m** |

즉 `backsquash`는 440 시절의 응급처치였는데 560으로 바꾸고도 그대로 켜둬서 **가운데만 낮은 벽**이 남아 있었다. 기본값 1.00(압축 없음)으로 되돌렸다. → **교훈: 제약이 풀리면 그 제약 때문에 넣었던 수법도 같이 걷어내야 한다.**

#### 차양 그림자 형상 교정 (2026-08-01)

라니스타 지시로 형상 확인. **틀려 있었다 — 평행 슬랫이라 그림자가 곧은 줄무늬였다. 그건 차양이 아니라 블라인드다.**

실제 벨라리움은 **중앙 고리에서 방사로 뻗은 삼각 돛**이고 가운데는 뚫려 있다(oculus). 방사 24돛(듀티 0.62, `r` 4.5~26 m)으로 바꾸자 **쐐기 그림자 + 가운데 밝은 원**이 나온다. 부수 효과가 크다 — **아레나 중앙이 밝아져 전투가 벌어지는 자리로 시선이 모인다.**

**2차 교정 — 그림자를 물리에 맡긴다 (라니스타: "광원 위치랑 잘 조절해서 현실적으로").** 방사로 바꾼 뒤에도 문제가 남아 있었다: **차양을 억지로 옮겨 그림자를 화면 한가운데 맞추고 있었다.** 고도 41°의 비스듬한 빛인데 그림자만 정중앙에 동그랗게 앉아 있으면 **빛과 그림자가 서로 다른 말을 한다.**

- 인위 오프셋 폐기. 차양을 **제자리**(관중석 위 `r` 9~34 m)에 건다 — 실제 벨라리움은 아레나가 아니라 **관중을 그늘지게** 하는 물건이다.
- 안쪽이 처진 원뿔(`z` 11.5 → 14.5 m). 평평한 판은 그림자가 원본과 똑같이 평행이동만 하지만, 원뿔은 **그림자가 눕는다.**
- 결과: 그림자 중심이 `(+8.0, −10.6) m`로 밀린다. 태양 쪽(좌상)에서 쐐기가 뻗어 아레나를 가로지르고 **근우측은 열린 하늘로 남는다.** 실물 원형극장이 그렇다.

> **⚠ 부호 버그가 여기서 드러났다.** 높이 `H`의 점 `p`가 만드는 그림자는 `p − L·(H/Lz)`에 떨어지므로, 그림자를 원점에 놓으려면 `p = +L_xy·(H/Lz)`여야 한다. 초판은 **부호가 반대**였는데, 평행 슬랫이 x −34~34 · y −40~62로 워낙 넓어 **우연히 덮고 있어서 안 드러났다.** 방사 돛(`r ≤ 26`)으로 바꾸자 그림자가 통째로 아레나 밖(27.7, −36.8)으로 나가며 발각됐다. 검산으로 확정: 차양 (−13.85, 18.37) → 그림자 (0.00, 0.00). — **넓은 것으로 덮으면 틀린 계산이 안 보인다.**

### 10.16 B3 베이크 — 결함 2건을 검산이 잡았다 (2026-08-01)

산출물: `arena_{basic,zoom}.png`(알베도) + `arena_{basic,zoom}_n.png`(노멀), 각 **1880×1120**(940×560 논리 × BGS 2, §10.6 계약).

**노멀맵 출력 경로 — 컴포지터를 못 쓴다.** Blender 5.x는 `scene.node_tree`가 사라졌고(→`compositing_node_group`), 새 컴포지터에는 `MixRGB`·`Math`·`VecMath`가 **등록조차 안 돼 있다**(실측). 인코딩(×0.5+0.5)을 할 노드가 없다. 대신 **각 머티리얼의 BSDF Normal 입력에 물린 소켓**을 그대로 이미션으로 내보낸다 — 거기엔 Bump와 Bevel이 이미 합쳐져 있어 **모래 요철·모서리 마모까지 포함된** 노멀이 나온다. (`Geometry` 노드의 Normal을 쓰면 그 둘이 빠진다.)

| # | 결함 | 증상 | 원인 | 처방 |
|---|---|---|---|---|
| 1 | **sRGB를 먹은 노멀** | x=0이어야 할 바닥이 **+0.468** (벽도 +0.469 — 상수 오프셋) | `view_transform = Standard`가 sRGB 전달함수를 적용. 선형 0.5 → sRGB 0.735 → 187 | **`Raw`**. 노멀은 색이 아니라 데이터다 |
| 2 | **축 부호 반대** | z가 **−0.349** (기대 +0.342 — 크기는 맞고 부호만) | Blender 카메라 공간은 +y 위·+z 화면 안쪽. §3.1은 +y 아래·+z 화면 밖 | 인코딩 배율을 `(0.5, −0.5, −0.5)`로 |

**검증 결과 (`verify_bake.py`, 실패 0):**

| | 부각 20° | 부각 15° | 기대 |
|---|---|---|---|
| 해상도 | 1880×1120 | 1880×1120 | 계약치 |
| 바닥 N.x | −0.000 | −0.001 | 0 |
| 바닥 N.y | −0.928 | −0.957 | −cos(부각) |
| 바닥 N.z | +0.346 | +0.259 | +sin(부각) |
| 법선 길이 | 0.991 | 0.992 | 1.0 |

**B4로 넘길 자산 등록 건:** 베이크 산출물은 지금 `tools/arenabake/`에 있고 그 폴더의 `*.png`는 `.gitignore` 대상이다(블록아웃 스크래치용). **알베도 2장은 실제 배포 에셋**이므로 B4에서 `Morituri.Headless/`로 옮기고 **`csproj` Link를 걸어야 한다**(§10.6). 노멀 2장은 웹에서 안 쓰고 Godot 이관용이라 위치는 N3에서 정한다.

> **단일 픽셀만 봤으면 둘 다 놓쳤다.** 1번은 "모래 요철이겠지"로 넘어갈 값이었다 — **영역 평균**이 상수 오프셋을 드러냈고, 2번은 크기가 맞아 더 교묘했다. 그래서 검증을 `tools/arenabake/verify_bake.py`로 **고정**했다. 다시 구울 때마다 눈이 아니라 수로 판정한다.

### 10.17 돌리 시퀀스 — "드론처럼 들어간다" (2026-08-04)

라니스타: *"줌 했을 때 진짜 카메라 위상이 낮아지며 가까이 간 것처럼, 베이크가 안 끊기고 쭉 따라 들어갔으면."*

**지금 왜 끊기나 — 원인이 둘이다.**

1. **크로스페이드** — 20°/15° 두 장을 알파로 겹치니 전환 중 디테일이 이중으로 보인다.
2. **시차(parallax) 0** — 정사영 스틸이라 카메라가 다가가도 앞뒤가 상대적으로 안 움직인다. "들어간다"는 느낌은 **가까운 것이 먼 것보다 빨리 움직일 때** 생기는데, 스틸 한 장은 통째로 확대될 뿐이다.

| 안 | 부각 연속 | 시차 | 이중상 | 용량 | 뷰어 수정 |
|---|---|---|---|---|---|
| 크로스페이드(기존) | ✗ 두 각만 | ✗ | 있음 | 4.6 MB | — |
| **돌리 시퀀스(채택)** | **✓** | ✗ (1단계) | **없음** | 약 5~9 MB | 프레임 선택 |
| 각도 스택 | 부분 | ✗ | 있음 | 11.5 MB | — |
| 실시간 3D | ✓ | ✓ | 없음 | 0 | **투영 전면 재작성** |

**핵심 착상: 줌은 정해진 1차원 경로다.** `zoomFrac` 0→1 위의 프레임을 미리 구워 두면 각 장이 **진짜 3D 렌더**라 부각이 연속으로 낮아지고 이중상이 없다. 런타임 3D는 0 — 프레임 하나 골라 `drawImage` 할 뿐이다.

#### 1단계 (구현) — 정사영 돌리

프레임 `f`의 값은 **뷰어가 매 프레임 계산하는 것과 같은 식**이다(§2.4.3):

```
frac    = f / (N−1)
CamTilt = TiltBasic + (TiltZoom − TiltBasic)·frac     → elevation = asin(CamTilt)
camZoom = 1 + (ZoomFollow − 1)·frac                   → ortho 폭 = ORTHO_W / camZoom
```
정사영에서 '가까이 감'은 **ortho 폭을 줄이는 것**이다. 프레이밍 이동도 `1/zoom`으로 나눠 화면 픽셀 기준을 맞춘다.

**⚠ 통합의 함정 — 줌을 두 번 걸면 안 된다.** 돌리 프레임에는 줌이 **이미 구워져 있다.** 기존 배경은 카메라 변환 *안에서* 그려 `scale(camZoom)`을 받았는데, 돌리 프레임을 거기 그대로 넣으면 이중 확대된다. 변환을 직접 조립하되 **배율만 빼고 팬은 줌만큼 곱해** 넣는다:

```
translate(shx, shy) · translate(CX, CY) · rotate(tilt)
· translate(camZoom·(CX−camX), camZoom·(CY−camY) + camLift)
· drawImage(frame, −CX, −CY, W, H)
```

**자산이 없으면 조용히 폴백한다** — `dolly.ready`가 false면 기존 2장 크로스페이드로 돌아간다. 자산 없이도 뷰어는 돌아가야 한다.

#### 1단계 실행 결과 (2026-08-04)

24장 · 940×528 · 부각 0.340 → 0.270 연속. **실물 확인: 아레나가 커지면서 타원이 실제로 납작해진다** — 크로스페이드가 아니라 진짜 카메라 이동이다.

| 항목 | 값 | 메모 |
|---|---|---|
| 용량 | PNG 13.7 MB → **JPEG(q90) 1.3 MB** | 90% 절감. 전환 중엔 화면이 움직여 압축 손실이 안 보인다 |
| 뷰어 메모리 | **45 MB** | 프레임 캔버스 24장(940×528) |
| 프레임 비용 | 2.97 → **4.1 ms** | 60 fps 예산 16.7 ms의 25% |
| 베이크 시간 | 약 60분 (장당 2.5분) | 한 번만 |

**함정 2건 (둘 다 실측으로 잡았다):**

1. **캔버스 크기를 `mkbg()`로 잡으면 메모리가 터진다.** 1880×1056 × 4 byte × 24장 = **190 MB**. 돌리 프레임은 줌이 이미 구워져 있어 `BGS` 배율이 필요 없다 — 원본 크기(940×528)로 잡으면 45 MB.
2. **`paintCrowd`가 `BGS`를 상수로 썼다.** 940×528 캔버스에서는 2배로 그려진다. 배율을 **캔버스에서 유도**하도록 고쳤다(`c.canvas.width / W`).

> **측정 오염 주의:** 폴백 검증 중 프레임 비용이 **237 ms**로 찍혀 회귀를 의심했으나, 원인은 코드가 아니라 **Blender가 전 코어로 베이크 중이던 CPU 경합**이었다. 베이크 종료 후 재측정 4.1 ms. **다른 무거운 작업이 도는 중에 낸 성능 수치는 믿으면 안 된다.**

#### 2단계 (미착수) — 원근 돌리

진짜 시차를 얻으려면 원근 투영으로 구워야 한다. **대가가 크다:** 뷰어의 `proj()`가 정사영 타원을 전제로 짜여 있고 사거리 원·발자국·혈흔이 전부 그 타원을 쓴다. 원근으로 가면 **그 수식을 전부 다시 써야 한다.** B5 이후 별도 항목으로 잡는다 — 또는 Godot 이관(N3)에서 실시간 3D로 받으면 공짜다.

### 10.18 시네마 모드 + 돌리 결함 2건 (2026-08-04)

#### 결함 ① 줌하면 관중이 카메라를 따라온다

`paintCrowd`가 **기본 배율**(`RX/12`)로 그렸는데 돌리 프레임은 **이미 확대된 렌더**다. 관중석은 커지는데 사람만 제자리라, 관중이 화면에 붙어 따라오는 것처럼 보였다.

→ `paintCrowd(c, tilt, zoom)`으로 배율을 넘긴다. 사람 크기(`PH`·`PW`)도 같이 곱한다. **프레임에 구워진 줌을 그리는 쪽이 모르면 반드시 어긋난다.**

#### 결함 ② 확대하면 화면 가장자리가 잘린다

카메라가 선수를 추적하며 팬하는데 돌리 프레임은 1:1이라 **프레임 밖이 드러난다.**

| camZoom | 팬 최대(가로) | 세로 |
|---|---|---|
| 1.0 | 0 px | 0 |
| 1.6 | **218 px** | 0 |
| 2.9(패자 줌인) | **777 px** | 107 px |

→ **가로 오버스캔 1.5배**로 다시 굽는다(`940 + 2·218 = 1376`). 세로는 팬이 0이라 불필요하다. `ortho_scale`이 긴 변에 걸리므로 폭만 키우면 세로 화각은 그대로다.

**실행 결과:** 24장 · **1410×528** · JPEG 합계 **2.1 MB**(장당 90 KB).
검증은 **팬을 최대로 밀어** 했다 — `camZoom 1.6`에서 `camX = CX ± mgnX`(±240 px). 좌·우 가장자리 세로줄의 최대 밝기가 **115~140**으로 나왔다. 무대 밖 어둠이면 20~46이므로 **프레임 밖이 드러나지 않는다.** 팬을 0으로 두고 잰 값은 이 결함을 못 잡는다.

→ 함께 넣은 **잔여 배율 보정**(`camZoom / frameZoom`)이 두 가지를 푼다: 프레임 24단계 양자화 오차, 그리고 **`ZoomFollow`를 넘는 구간**(패자 줌인 2.9배 — 보정이 없으면 배경만 1.6에서 멈춘다).

> 오버스캔이 들어가면 `paintCrowd`의 배율 계산이 또 깨진다. 캔버스 **가로**로 배율을 재면 오버스캔을 배율로 오인한다 → **세로 기준**(`canvas.height / H`)으로 잡고, 아레나 중심도 `CX`가 아니라 캔버스 한가운데로 잡아야 한다.

#### 관중 디테일 — "멀리서 카메라로 찍힌 관중"

| | 초판 | 지금 |
|---|---|---|
| 색 | 단일 어두운 갈색 | **탁한 흙·아마포·모직 10색**(대기 원근으로 채도가 낮다). 원색을 쓰면 사탕처럼 보인다 |
| 머리 | 몸통과 같은 색 | **살빛이 옷보다 밝고 붉다** — 이 대비가 '사람'을 만든다 |
| 바닥 | 없음 | **앉은 자리의 그늘** — 밑이 어두워야 좌석에 얹힌 것으로 보인다 |
| 윗선 | 균일 | **10%는 서 있다** — 윗선이 고르지 않아야 군중이다 |
| 밀도 | 균일 | **뭉침**(사인파 × 난수) — 균일하면 격자로 보인다 |

#### 시네마 모드

`?cinema=1` 또는 **C 키**로 토글, `Esc`로 나감. `header`·`#hud`·`#ctrl`을 걷고 무대를 뷰포트 전체로 늘린다.

**실측: 1280×718 / 뷰포트 1280×720 — 가로·세로 100% 채움, 비율 1.783.** 무대를 16:9로 맞춰둔 덕에 레터박스가 사실상 없다.

> HP·이름표는 캔버스 위에 그려지므로(`unitTag`) UI를 걷어도 정보가 다 사라지진 않는다. 나가는 버튼은 평소 투명도 0.12로 숨고 마우스를 올리면 나타난다.

### 10.19 관중을 배경에서 떼어내다 + 모니터 전체화면 (2026-08-04)

라니스타 지적 4건.

#### ①·② 옷 색이 튀고, 관중이 가만히 있다 — 뿌리가 같다

둘 다 **관중을 배경에 구웠기 때문**이다.

- **색 튐:** 난수를 `rnd()`(호출 순서)에서 뽑았는데, 프레임마다 건너뛴 자리 수가 달라 **같은 관객이 프레임마다 다른 색**을 받았다. 줌하며 프레임이 넘어갈 때마다 옷 색이 튀었다.
- **정지:** 구운 그림이라 움직일 방법이 없다.

→ **난수를 자리(열 `k`, 자리 `i`)의 해시에서 뽑는다.** 어느 프레임에서 그리든 같은 사람은 같은 색이다. 그리고 **매 프레임 그리도록** 배경 굽기에서 떼어냈다 — §10.9 결정 1이 "2D 띠 오버레이"를 고른 이유가 이거였다(나중에 움직일 여지).

**움직임:** 멀리서 본 관중은 개인이 아니라 **웅성거림**으로 읽힌다. 개체마다 다른 위상·주기로 아주 작게 흔든다(진폭 = 사람 키의 3~7%, 좌우 흔들림은 어깨 너비의 10%). **위상이 자리에서 나오므로 파도처럼 줄지어 움직이지 않고 제각각 꿈틀댄다.** 앉은 자리의 그늘은 안 흔든다 — 그늘까지 움직이면 사람이 떠 보인다.

> **구조 변경:** `paintCrowd`가 이제 **변환을 건드리지 않고 아레나 중심(원점) 기준**으로 그린다. 렌더 루프에서 카메라 변환 위에 올려야 하므로 `setTransform`을 쓸 수 없다. 배율도 주의가 필요하다 — 돌리 경로는 이미 `scale(ex)`가 걸려 있어 **`frameZoom`을 넘겨야** 한다(`camZoom`을 넘기면 두 번 곱해진다).

#### ③ 줌하면 상단에 여백이 남는다

뷰어가 `camLift`만큼 배경을 **아래로** 민다(줌 46 px · 인트로 100 px). 그만큼 위가 비는데 돌리 프레임에 세로 여유가 없었다.

→ **세로 오버스캔 1.42배**(`(528+2·100)/528`)로 재베이크. `ortho_scale`은 긴 변에만 걸리므로 세로 화각을 넓히려면 **`resolution_y`를 늘려야** 한다(가로 오버스캔과 방식이 다르다).

**실행 결과 (2026-08-04):** 24장 · **1410×750** · JPEG 3.0 MB(장당 127 KB).
검증은 **`camLift`를 실제로 걸고** 상단 20 px 띠의 최대 밝기를 쟀다 — 두 줌 단계 모두 **231**(무대 밖 어둠이면 20~46). 위쪽이 채워진다.

| | 값 | 메모 |
|---|---|---|
| 프레임 비용 | **6.4~6.7 ms** | 4.1 → 6.5. 관중이 매 프레임으로 옮겨간 값이다. 60 fps 예산의 40% |
| 돌리 메모리 | **97 MB** | 1410×750×4×24. 부담되면 **프레임 수를 16으로 줄이면 65 MB**(부각 간격 0.005 → 0.0053이라 체감 차이는 거의 없다) |

> **⚠ 작업 함정 — 이 세션에서 두 번 물렸다.** 여러 치환을 한 스크립트에 넣고 `assert s != b` 하나로 검증하면, 그건 **어느 하나라도** 바뀌면 통과한다. 실제로는 뒤쪽 치환이 공백 불일치로 조용히 실패했고 **그 상태로 60분짜리 베이크를 돌렸다**(세로 오버스캔이 안 들어간 프레임 24장). 앞서 관중 밀도 변경도 같은 이유로 커밋에 안 들어갔다.
> **처방: 치환마다 개수를 세어 검증한다**(`scratchpad/patch.py`). 아무것도 안 바뀌는 것이 절반만 바뀌는 것보다 낫다. 그리고 **검증 출력에 고정 문자열을 찍지 말 것** — 치환이 실패해도 성공한 것처럼 보인다.

#### ④ 모니터 전체화면

시네마 모드가 브라우저 뷰포트만 채웠다 — 브라우저 크롬이 남았다. `requestFullscreen`을 붙였다.

> **제스처 제약:** `requestFullscreen`은 사용자 제스처 안에서만 허용된다. 그래서 `?cinema=1` 자동 진입은 UI만 걷고, **C 키/버튼으로 부를 때만** 실제 전체화면이 걸린다. `fullscreenchange`를 듣고 F11·Esc로 빠져나가면 시네마도 함께 푼다 — 상태가 어긋나면 안 된다.
>
> (§10.21에서 뒤집힌다 — 시네마가 기본이 되면 Esc 한 번에 기본값이 풀려 버린다.)

### 10.20 관중이 줌에서 튀던 진짜 원인 — 축척에 딸린 솎아내기 (2026-08-04)

§10.19에서 색 튐은 잡았는데 **위치와 형태가 여전히 튀었다.** 남은 원인은 색과 무관한 곳에 있었다 — 누구를 그릴지 고르는 규칙이 카메라에 딸려 있었다.

```js
if (Math.hypot(sx - lastX, sy - lastY) < PW * 1.25 + q0 * 5) continue;
lastX = sx; lastY = sy;
```

두 결함이 겹쳐 있다.

1. **축척불변이 아니다.** `PW = 9 * zoom`은 줌에 비례하는데 `q0 * 5`는 상수다. 합이 배율에 비례하지 않으니 줌마다 다른 밀도가 나온다.
2. **사슬이다.** `lastX/lastY`는 직전 채택자를 가리킨다. 앞사람 하나가 채택↔탈락으로 뒤집히면 그 뒤 전원의 기준점이 밀려 **연쇄로** 바뀐다. 돌리 24프레임이 이산적인 배율로 넘어갈 때마다 채택 집합이 통째로 갈렸다.

→ **자리를 월드 좌석 간격으로 고정한다.** 실제 관중석은 좌석이 돌에 새겨져 있다. 자리는 카메라와 무관한 상수다.

```js
const SEAT = 0.42;                                  // 좌석 간격(m)
const seats = Math.round(2 * Math.PI * r / SEAT);   // 열마다 개수 고정
const aDeg  = i * (360 / seats);
if (q0 < 0.12) continue;                            // 빈 자리도 자리에서 뽑는다
```

`zoom`이 선택식에서 완전히 사라졌다. 벽 가림 판정의 여유값도 `wallY - 1.5` → `wallY - 1.5 * zoom`으로 축척을 맞췄다(px 상수였다).

**검증 (2026-08-04):** 스텁 컨텍스트로 머리 좌표를 받아 `zoom`으로 나눈 정규 좌표를 비교했다.

| zoom | 인원 | 정규 좌표 일치 |
|---|---|---|
| 1.0 | 437 | (기준) |
| 1.2137 | 437 | **완전 일치** |
| 1.6 | 437 | **완전 일치** |

밀도도 확인했다 — 940×528 프레임 안 184명, **최근접 이웃 중앙값 10 px**(어깨 너비 `PW` 9 px). 어깨가 거의 닿는 만석이고, 옛 문턱 `PW·1.25 ≈ 11 px`와 같은 수준이다.

### 10.21 시네마를 기본으로 — 컨트롤을 화면 위로 띄운다 (2026-08-04)

라니스타 지시: 전체화면을 디폴트로, 그 상태에서 기존 버튼을 쓸 수 있게.

**충돌:** 시네마 모드는 `#ctrl`을 `display:none`으로 걷어냈다. 그게 기본이 되면 **조작할 방법이 사라진다.**

→ 컨트롤을 문서 흐름에서 떼어내 `position:fixed`로 화면 하단에 띄운다. 영상 플레이어 관례 그대로: 입력이 2.6초 멎으면 물러나고(`body.idle`), 움직이면 돌아온다. 커서가 컨트롤 위에 있는 동안은 타이머를 멈춘다 — 조작 중에 버튼이 사라지면 안 된다. **무대는 그동안 한 픽셀도 안 줄어든다**(`fixed`라 레이아웃에 영향이 없다).

> `opacity:0`만으로는 부족하다. **`pointer-events:none`을 같이 걸어야** 안 보이는 버튼이 안 눌린다.

**제스처 제약을 두 단계로 나눈다.** `requestFullscreen`은 여전히 사용자 제스처 안에서만 된다.

1. 로드 즉시 **시네마 레이아웃** — UI를 걷고 무대를 뷰포트에 꽉 채운다(제스처 불필요).
2. **첫 클릭/키 입력**에 모니터 전체화면을 붙인다. 한 번만 시도하고, 막히면 조용히 넘긴다.

Photino 셸에는 브라우저 크롬이 없으므로 ①만으로도 이미 전체화면이다.

**뒤집은 규칙:** `fullscreenchange`에서 시네마를 끄던 것을 **없앴다.** 시네마가 기본인데 Esc 한 번에 기본값이 풀리면 안 된다. 끄는 길은 '시네마 종료' 버튼 하나로 명시한다. 되돌리려면 `?cinema=0`.

**검증 (2026-08-04):** 뷰포트 1280×720에서 `#stagebox` 1280×720(무대 무손실) · `#ctrl` `fixed` 940×58, 하단 여백 18 px · `idle` 토글 시 opacity 1↔0 · `pointer-events` auto↔none · `?cinema=0`이면 `position:static`·헤더 복귀.

> **검증 함정:** Browser 창이 안 떠 있으면 `document.visibilityState === 'hidden'`이라 **CSS 트랜지션이 `currentTime 0`에서 얼어붙는다.** 그 상태로 `getComputedStyle(...).opacity`를 읽으면 언제나 시작값(1)이 나온다 — 규칙이 안 먹는 것처럼 보인다. 트랜지션을 잠깐 끄고 재면 1↔0이 정상으로 나온다.

### 10.22 전면부를 더 걷어낸다 — 컷어웨이 82° · 실루엣 0.55배 (2026-08-04)

라니스타 지시: "전면부 벽이나 관중석은 더 컷오프해서 화면에 안 거슬리게."

앞쪽 실루엣(`fg_cavea` + 이빨)이 화면 하단 5분의 1을 검게 덮고 있었다. 두 인자를 함께 움직였다.

| 인자 | 이전 | 이번 | 뜻 |
|---|---|---|---|
| `--cut-keep` | 65° | **82°** | 앞쪽에서 통째로 걷어내는 각반경 |
| `--cut-fade` | 35° | **30°** | 걷어낸 곳에서 온전한 높이로 올라오는 폭 |
| `--fg-h` | (없음) | **0.55** | 앞쪽 실루엣 높이 배율 — 새로 뺀 인자 |

**결과 (보이는 창 940×528 기준, `dolly_00`):**

| 하단 18% | 이전 | 이번 |
|---|---|---|
| 평균 밝기 | 27.8 | **52.4** |
| 어두운 픽셀(<45) | 69.1% | **22.7%** |

프레임이 뒤로 갈수록(줌이 들어갈수록) 더 열린다 — 어두운 픽셀 22.9% → 16.1% → 11.9% → 14.3%(00·08·16·23).

> **딸린 변경 — 뷰어의 관중 컷 램프도 같이 움직여야 한다.** `paintCrowd`의 `cut()`은 `blockout.py`의 `cutaway()`와 **같은 램프**를 써야 한다. 어긋나면 걷어낸 자리에 관중만 남아 허공에 뜬다. 65/35 → 82/30으로 맞추고 상수(`CUT_KEEP`/`CUT_FADE`)로 빼 대응 관계를 명시했다.
> 관중 손실은 미미했다 — 프레임 안 **184 → 180명**. 지배적인 필터는 컷 램프가 아니라 벽 가림이었다.

**배포·검산 (2026-08-04):** 24장 · 1410×750 · JPEG 품질 88 → **2.9 MB(장당 121 KB)**.

| 항목 | 값 | 판정 |
|---|---|---|
| 상단 20 px 밴드 최대 | **228** | 세로 오버스캔 유지(무대 밖 어둠이면 20~46) ✓ |
| 하단 90 px 밴드 평균 | **91.7** (최대 137) | 모래다. 검은 실루엣이 물러났다 ✓ |
| 프레임 비용 | **3.7 ms** (줌 1.6) | 60 fps 예산의 22% |
| 콘솔 오류 · designlint | 0 · 0 | ✓ |

> **작업 메모:** PNG→JPEG 배포를 세 번째 손으로 하고 있었다 → `tools/arenabake/deploy_dolly.py`로 고정했다. 새 시퀀스가 더 짧을 때 **옛 프레임이 섞이지 않도록 먼저 지운다**(24장 → 16장으로 줄이면 17~24가 남는다).
>
> **검증 함정(§10.21에 이어):** Browser 창이 안 떠 있으면 `requestAnimationFrame`도 얼어붙는다. rAF를 기다리는 측정 코드는 그대로 30초 타임아웃이 난다 — **`tick()`을 직접 부르면** 된다.

### 10.23 B5 venue 4종 — 색은 됐고, 관중이 문제였다 (2026-08-04)

검증 항목은 "venue별 캡처 대조"였다. 먼저 **재고 그대로 재봤다.**

| venue | RGB 평균 | 휘도 | 채도 | arena 대비 거리 |
|---|---|---|---|---|
| arena | 60.1 / 55.9 / 50.9 | 56.6 | 15.3 | — |
| spar `#c9d2b6` | 47.5 / 46.1 / 36.5 | 45.4 | 23.2 | 21.5 |
| street `#a6adbb` | 39.2 / 38.0 / 37.4 | 38.3 | 4.6 | 30.7 |
| bar `#bc9670` | 44.2 / 33.0 / 22.4 | 35.1 | 49.3 | 39.9 |

**색은 통과다.** 넷이 색상·채도 모두 갈린다(수련장=탁한 초록 / 뒷골목=무채색 / 선술집=호박색). 곱연산이라 전부 어두워지는데, 밤골목·실내 술집에는 오히려 맞다.

**그런데 색을 아무리 맞춰도 안 되는 것이 있었다 — 네 venue 모두 관중 180명이 앉은 콜로세움을 그린다.** 술집 난투가 원형경기장에서 벌어진다. 이건 색보정으로 못 고친다. §10.9 결정 4의 "색·**소품** 변주"에서 소품 쪽이 비어 있었던 것이다.

→ **관중을 venue별로 게이트한다.** 빈 자리 문턱을 `EMPTY = 1 − 0.88·crowd`로 두면 arena에서 정확히 옛 값 0.12가 나온다(회귀 없음). 자리에서 뽑으므로 §10.20 원칙이 유지된다 — 누가 남는지가 카메라·시간과 무관하게 고정된다.

| venue | `crowd` | 프레임 안 관중 | 뜻 |
|---|---|---|---|
| arena | 1.00 | **180명** | 만석 (이전과 동일 = 회귀 없음) |
| spar | 0.18 | **30명** | 훈련장 — 구경꾼 몇뿐 |
| street | 0 | **0명** | 관중석이 없다 |
| bar | 0 | **0명** | 〃 |

`bar`의 배경 실측 RGB **64.1 / 43.7 / 25.8** — 횃불빛 호박색이 실제로 들어간다.

> **딸린 정리:** `VENUES` 표의 `wall`/`f0`/`f1`/`ring`은 B4에서 절차 배경(`buildArenaBg`)이 사라지며 **전부 죽은 값**이 됐고, `VENUE`는 대입만 되고 아무도 안 읽고 있었다. 거짓말하는 표를 남기느니 지금 쓰는 것(`label`·`crowd`)만 남겼다. `melee.html`은 자체 절차 배경을 쓰므로 무관하다.

**남은 정직한 한계 — 라니스타 결재가 필요하다.** 뒷골목·선술집에서 관중은 사라졌지만 **건축은 여전히 원형경기장**이다(포디움 벽·관중석 계단·아치문). 색만으로는 이걸 못 지운다. 선택지:

| 안 | 내용 | 비용 |
|---|---|---|
| **A** | 지금 상태 유지 — 관중 없는 어두운 경기장으로 읽는다 | 0 |
| **B** | venue별 3D 베이크 추가(뒷골목=담벼락, 선술집=실내) | 베이크 1벌당 약 2시간 + 블록아웃 작성 |
| **C** | 비-arena는 `melee.html`의 절차 배경을 뷰어에도 되살려 쓴다 | 중간 — B4에서 지운 코드를 부분 복구 |

**검증 (2026-08-04):** 4 venue 실제 로드 · 콘솔 오류 0 · designlint 0 · arena 관중 수 회귀 없음(180).

### 10.15 B4 교체 계획 — 뷰어에서 무엇을 지우고 무엇을 넣나

베이크가 끝나면 뷰어 교체는 **삭제가 대부분**이다. 대상 지점을 미리 짚어둔다(`viewer.html`).

| 행 | 지금 | B4 |
|---|---|---|
| 144 | `H = 440` | **`H = 560`** (§2.4 개정) |
| 240 | `mkbg()` — 940×440 하드코딩 캔버스 2장 | **삭제** → `Image` 2장(`arena_basic.png` · `arena_zoom.png`) |
| 338~476 | `buildArenaBg()` 전체 + `bakeRelief()` | **삭제.** 절차 텍스처·Sobel 베이크·요철 조명이 전부 이미지 한 장으로 대체된다 |
| 1529·1531 | `drawImage(bgBasic/bgZoom, 0,0,W,H)` | **그대로** — 소스만 캔버스에서 이미지로 바뀐다 |

**남는 것:** `LIGHT`/`LDIR`/`RIMCOL`(선수 그림자·발자국·림라이트가 읽는다) · `grade`/`haze`(venue·날씨 색보정) · A1 투영 단일화 · §2.4 카메라 계약.

**노멀맵은 웹 런타임에서 안 쓴다.** 알베도에 이미 조명이 구워져 있고, venue·날씨는 결정 4에 따라 **색보정으로만** 변주한다(arena만 3D). 노멀맵은 §5 N3의 **Godot 이관 에셋**이다 — 거기서 `PointLight2D`가 실제로 쓴다.

> **B4에서 주의:** 940/440이 **하드코딩된 곳이 6군데**다(위 표의 240·338·340·347·475·476행). 세로만 바꾸고 이걸 놓치면 A1과 똑같은 어긋남이 재발한다 — 다만 그 코드가 전부 삭제 대상이라 실수 여지는 오히려 작다.

#### B4 실행 결과 (2026-08-01)

계획대로 **삭제가 대부분이었다.** `viewer.html`에서 약 200줄이 빠지고 25줄이 들어왔다.

| 항목 | 결과 |
|---|---|
| `H` | 440 → **560** |
| `buildArenaBg` · `bakeRelief` · `hcv` · `RELIEF` · `LAMB`/`LAMBC`/`LSUN` | **전부 삭제** (478행 이후 참조 0건을 확인하고 잘랐다) |
| 신규 | `paintArena()` — 로드 시 1회 색보정만 입힌다 |
| 에셋 | `arena_basic.png` · `arena_zoom.png` → `Morituri.Headless/` + **csproj Link 2줄** |
| 프레임 비용(말미) | 5.0 ms → **2.97 ms** |
| 로드 비용 | 절차 생성 + 요철 베이크 **295~960 ms → 0** (이미지 디코드로 대체) |
| designlint · 콘솔 | 0 · 0 |

**정렬 검증 — 눈으로 봐야 했다.** 자동 판정식(A1의 8방위 재질 검사)이 **베이크 이미지에서는 못 쓴다.** 옛 절차 팔레트를 전제로 "모래인가"를 색으로 판정했는데, 베이크 이미지는 명암 범위가 훨씬 넓어 그늘진 모래가 무대 밖보다 어둡다(실측: 아레나 안 lum 29~33 vs 무대 밖 46.6). 밝기 임계로는 안팎을 못 가른다.

→ **플레이 타원(r=12 m)을 붉은 점선으로 겹쳐 캡처**해 확인했다. 점선이 베이크된 모래 가장자리와 일치한다. 카메라 계약(§10.6)이 실제로 성립함을 이걸로 확인한 것이다.
→ 자동 회귀로는 **에셋 크기 일치**(`bgBasic.width === W*BGS && height === H*BGS`)를 남긴다. 부각이나 `H`를 바꾸고 다시 안 구우면 여기서 걸린다.

**venue 색보정 완화.** 결정 4대로 arena만 3D고 나머지는 색 변주인데, 처음 넣은 곱연산이 너무 셌다(뒷골목이 밤이 됐다). **베이크 이미지에는 이미 명암이 들어 있으므로 색보정은 '기울이는' 정도여야 한다** — 0.43배에서 0.65배 수준으로 낮췄다.

**2D 군중 띠(결정 1) — 완료.** 베이크된 관중석은 **빈 좌석**이라 사람은 뷰어가 얹는다.

- **좌석 자리를 눈대중하지 않는다.** `blockout.py`와 **같은 식**으로 계산한다 — `화면 x = CX + X·(RX/12)`, `화면 y = CY − Y·(RY/12) − Z·(RX/12)·cos(부각)`, 카베아는 `r = 14.4 + 0.55k` · `z = 3.5 + 0.30(k+1)`. 배경이 다시 구워져도 관중이 같이 따라간다.
- **앞쪽 컷어웨이 램프도 그대로 복제**(`keep 65 · fade 35`). 초판은 여기를 100°로 잘랐다가 **좌·우(각거리 90°)가 통째로 제외돼 관중이 하나도 안 찍혔다.**
- **벽에 가리는 자리는 건너뛴다** — 각도마다 포디움 벽 top의 화면 y를 구해 그보다 아래면 버린다. 안 그러면 관중이 벽 위에 떠 버린다.
- **윗열 16개만 쓴다.** 베이크에서 `ruin()`으로 깎인 곡선을 JS에서 재현할 수 없어(파이썬 난수) 영향이 적은 아래쪽만. 어차피 윗열은 대부분 프레임 밖이다.
- **배경 캔버스에 구워 넣는다**(색보정 *전에* 얹어야 관중도 같은 빛을 받는다) → **프레임 비용 0**. 나중에 군중게이지에 반응시키려면 이 지점을 매 프레임 패스로 옮기면 된다.

> 초판은 343점뿐이라 돌 배경에 묻혔다. 밀도를 열당 1400으로 올리고 **머리 그림자를 먼저 깔고 옷을 얹는 2겹**으로 바꿔서야 읽혔다 — 밝은 석재 위에서는 어두운 쪽이 먼저 형태를 만든다.

**B4 최종 프레임 비용: 2.38 ms** (교체 전 5.0 ms).

### 10.14 B2 잔여 (다음 작업)

- ~~철문 위치~~ → **해결 (라니스타 지시 "좌우에서 살짝만 뒤로"):** `az 0·180°` → **`35·145°`**. 화면 x 798 / 142 px로 프레임 안에 들어오면서 좌우 감은 유지된다. **덤으로 앞쪽 컷어웨이 페이드 구간(170°~10°)에서도 벗어나** 문이 온전한 높이로 선다 — 원래 위치는 페이드에 걸려 있었다.
  아치 비율도 조정: 폭 14°→10°(2.1 m) · 스프링라인 1.45 · 라이즈 1.05(높이 2.5 m). **로마 아치는 폭보다 높다** — 정사각 구멍은 아치로 안 읽힌다.
- 모래 요철 세기 최종 확정 — B3 노멀맵 베이크에서 실제 기여도를 보고
- ~~벽 핏자국 등 서사적 얼룩~~ → **완료:** 노이즈를 세로로 눌러(`Scale z = 0.22`) **아래로 흘러내린 자국**을 만든다. 규칙적인 무늬가 아니라 "무언가 흘렀다"는 흔적이라 서사가 붙는다(핏자국·빗물)
- 결정 1의 2D 군중 띠 — B4에서, 링이 아니라 좌·우 두 덩어리(§10.11)
- 벽 마모·핏자국 — 규칙성을 깨는 불균일 요소(§10.1 진단 5)
- 좌·우 관중석 구획 세부(§10.11로 범위가 좁아졌다)
- **결정 1의 2D 군중 띠는 B4(뷰어 교체)에서** — 3D 모델이 아니라 뷰어가 그리는 오버레이다. §10.11에 따라 **링이 아니라 좌·우 두 덩어리**로

---
