# MORITURI — [6] 스킬·계급 시스템 (설계 초안 0.1)
**상태:** 라니스타 발의 → Claude 피드백·디벨롭. **미확정.** Phase 2(행동 다양성 #2)의 대안/보강으로 도입 예정.
**한 줄:** 모리투리는 계급에 따라 넓어지는 스킬풀에서 N개를 장착한다. 액티브=무기 결합, 패시브=성격 결합, 모든 스킬은 능력치 계수로 스케일.

---

## 1. 원안 (라니스타)
- 각 모리투리는 N개 스킬 장착.
- 계급(rank) 존재. 높을수록 접근 가능한 스킬풀이 넓어짐.
- **단, 비례적 파워업 금지** — 낮은 계급 스킬 중에도 강한 게 있고, 낮은 계급이 가질 수 있는 스킬에도 유용한 게 있어야.
- 풀 구조: **액티브 = 무기 의존 / 패시브 = 성격 의존.**
- 스킬은 능력치 계수(stat coefficient)를 가짐.

---

## 2. 아키텍처 적합성 — 새 엔진이 필요 없다 ★

이 구조가 좋은 이유: **기존 데이터 주도 엔진(원칙 C)을 그대로 확장**한다.

| 스킬 종류 | 결합 대상 | 재사용하는 기존 시스템 |
|---|---|---|
| 액티브 | 무기 | T02 모션(`MotionDef`) + 실행 FSM. 스킬 = 추가 모션 or 모션 수정자. |
| 패시브 | 성격 | **T04 트리거(`TriggerRule`: 조건→Override/Interrupt)**. 패시브 스킬 = 트리거 규칙 1개. |

→ 패시브 스킬은 **이미 있는 트리거 엔진과 문자 그대로 동일**하다(잔혹함의 "취약상태→공격성↑"이 곧 패시브 스킬의 형태). 액티브 스킬은 무기별 모션 테이블에 행을 추가하는 것. **문서[1] §5 검증 질문("이 기능에 Sim 기존 파일을 고쳐야 하나?") → 답: No. 데이터 행 + 장착 레이어만.**

---

## 3. 핵심 긴장점 3개와 해결 제안

### 3.1 ⚠ 스킬 vs 특성(T09) — 중복을 정리하라
기획시안/문서[5]에 **이미 특성 시스템(T09: 불굴/천재/노련함…)** 이 있고, 그 훅 타입이 `StatMod / TriggerAdd / AgingMod / GrowthMod`다. **패시브 스킬 = TriggerAdd 특성**과 기계적으로 거의 같다. 두 시스템을 따로 만들면 엔진이 두 벌이 된다.

**제안 — 엔진은 하나, 경제는 둘:**
- **공용 기층:** 특성·스킬 모두 T04 트리거규칙 / StatMod / 모션수정자로 컴파일. 실행 엔진 1벌.
- **의미·획득으로 구분:**
  - **특성 = 타고난 정체성** (출생/성장/노화로 획득, 1~2개, 교체 불가, 서사). "이 선수는 누구인가."
  - **스킬 = 획득한 빌드** (계급/커리어로 획득, N슬롯 장착·교체, 매니지먼트 선택). "이 선수를 어떻게 짤 것인가."
- 같은 엔진, 다른 획득·슬롯 규칙. 이러면 시스템 2벌 만들 필요 없음.

### 3.2 "계급은 폭을 넓히되 파워가 비례하지 않는다" — 슬롯 경제로 해결
라니스타의 핵심 요구를 기계적으로 보장하는 방법:

- 스킬마다 **슬롯 코스트(1~3)**. 강한 스킬일수록 코스트↑.
- 모리투리는 **슬롯 예산**(계급에 따라 *느리게* 증가) + **풀 접근권**(계급에 따라 넓게 증가)을 가짐.
- **핵심: 예산은 풀보다 느리게 큰다.** → 고계급 = 선택지는 많지만 여전히 다 못 가짐 = 빌드 정체성 강제.
- 스킬의 진짜 강함은 **절대 파워가 아니라 가치/슬롯**. 낮은 계급의 1슬롯 효율 스킬이 3슬롯 화려한 스킬을 이긴다 → **"낮은 계급 스킬에도 강한 게 있어야"가 자동 성립.** 고계급 빌드도 효율 때문에 저계급 스킬을 넣게 됨.
- 보강: **상황 특화 스킬**(특정 무기/매치업에서만 강함) — 저계급·니치지만 그 매치업에선 메타 결정. "유용한 것이 낮은 계급에도" 요구 충족.

### 3.3 능력치 계수 — 기존 공식 슬롯에 그대로
- `스킬효과 = base × (1 + 능력치×계수)`. 데미지 공식(`RawDamage = base×(1+ATK/100)`)과 동형.
- 같은 스킬이 선수마다 다르게 스케일 → **빌드 시너지**(고ATK 선수에게 카운터데미지 스킬). 기획시안 "냉철함엔 카운터형을" 식 매니지먼트 재미의 확장.
- 계수는 코드 아닌 T06/데이터(원칙 C).

---

## 4. 가독성 제약 (양보 불가)
관전 게임이므로 **모든 스킬 발동은 SimEvent(reasonTag)를 발행**해 UI 아이콘/오라/컷인으로 보여야 한다(문서[3] §8). 조용히 발동하는 스킬 = "AI 고장" 인상 = 설계 원칙 위반. **스킬 설계 제약: 발동이 눈에 보일 것.** (T08 ReasonTags에 스킬 태그 추가.)

---

## 5. #2(행동 다양성)와의 관계 — 정직한 한계
스킬은 #2를 **부분적으로** 푼다:
- ✅ **빌드 다양성**(선수마다 다른 스킬셋 = 다른 경기) + **상황 트리거**(상태별 다른 행동)를 더한다.
- ⚠ 그러나 바탕의 Utility AI가 여전히 "최선 행동 1개 스팸"으로 붕괴하면, 스킬은 스팸에 분칠만 한다.
- **결론:** 스킬은 #2의 강력한 *기여*이되 *완전 대체*는 아님. 스킬 + Utility의 반복 페널티/콤보 보너스(경기 중 변주 유인)를 **함께** 가야 #2가 풀린다. 스킬 설계 시 "새 행동 선택지를 여는 액티브"를 우선하면 둘이 겹쳐 효율적.

---

## 6. 계급 ↔ 영속 세계
계급 = 커리어 성취(Phase 3 리그 랭킹/전적). 스킬 = 계급 상승으로 해금 = **장기 성장·매니지먼트 루프**. 즉 스킬 시스템은 Phase 2(전투 메커니즘) + Phase 3(획득=리그)에 걸친다. 액티브 스킬은 해당 무기 장착을 요구하게 게이트 → 무기 정체성 강화, #1(무기 밸런스)와 직접 연동.

---

## 7. 미해결 질문 (확정 전 결정)
1. 슬롯 예산 곡선(계급별 N)과 풀 접근권 곡선의 구체값.
2. 스킬 획득 방식: 생성 시 드래프트 / 리그 보상 / 훈련 / 영입.
3. 액티브 스킬 = 새 모션인가, 모션 수정자인가 (아마 둘 다 타입).
4. 액티브 스킬의 무기 게이트 강도(엄격: 해당 무기만 / 느슨: 계수 페널티).
5. 특성과 스킬의 슬롯 상호작용(특성이 스킬 슬롯을 먹나, 별도 트랙인가).

## 8. Phase 배치
- **Phase 2:** 엔진(트리거/모션 확장은 이미 존재) + 슬롯 경제 + 대표 스킬 10~15종 + 가독성 이벤트.
- **Phase 3:** 계급·획득(리그 연동).
- 예약 스키마: T12_Skills(id, kind=Active/Passive, gateWeapon|gatePersonality, slotCost, rankTier, triggerRuleId|motionMod, statCoef), T13_RankTiers(tier, slotBudget, poolAccess).
