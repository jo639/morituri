# MORITURI 짜침 감사표 (R0 — 2026-07-17)

> **목적:** "프로토타입 흔적"을 감이 아니라 목록으로 관리한다. 점수 높은 것부터 제거.
> **척도(짜침 1~5):** 5=몰입 파괴(브라우저 UI·의미불명 무반응), 4=명백한 웹 느낌, 3=게임답지 않음, 2=아쉬움, 1=완성.
> **계측 도구:** `dotnet run -- designlint` (금지 목록 스캐너, R1 완료 후 `designlint gate`를 테스트에 편입).
> **계측 기준:** 뷰포트 651px(1280×720 브라우저) 기준 스크롤 배율. 시즌 8 세이브(슬롯1).

## 0. 전역 이슈 (화면 불문 — R1~R3의 몸통)

| # | 이슈 | 실측 | 짜침 | 라운드 |
|---|---|---|---|---|
| G1 | 유니코드 이모지 | ~~789건~~ → **0** ✅ (R1 2026-07-17: 서버 `{토큰}` + ui.js 관문, icons.svg 32→60종) | ~~5~~ 1 | R1 ✅ |
| G2 | `alert()`/`confirm()` | ~~8곳~~ → **0** ✅ (오류=toast, 확인=askConfirm — 비가역은 hold-to-confirm) | ~~5~~ 1 | R1 ✅ |
| G3 | 기본 폼 요소 | ~~15곳~~ → **0** ✅ (.seg 세그먼트·.sw 스위치 — .value/.checked 호환 API) | ~~4~~ 1 | R1 ✅ |
| G4 | 페이지 리플로우 레이아웃 | → **0** ✅ (R2 2026-07-17: `#stage` 고정 무대 1280×800 + zoom fit, 오버레이 무대 좌표계, UI 배율=가상 해상도) | ~~4~~ 1 | R2 ✅ |
| G5 | 버튼 진행중 상태 부재 | → ✅ (R3: POST 중 `#stage.busy` — 상단 금빛 진행선 + 전 버튼 이중클릭 차단, api() 단일 관문) | ~~4~~ 1 | R3 ✅ |
| G6 | 클릭 SFX/FX 공용 레이어 없음 | → ✅ (R3: ui.js 전역 pointerdown — 클릭음+금빛 스파크(4화면 공유), 확정=낙인음, 토스트=전갈음) | ~~3~~ 1 | R3 ✅ |
| G7 | 클릭 타겟이 버튼이 아님 | → ◐ (R3: `[onclick]` 전역 호버 어포던스(골드 아웃라인)+클릭 스파크. 시맨틱 버튼화는 후속) | ~~3~~ 2 | R3 ◐ |
| G11 | 모달 닫기 비일관 | → ✅ (2026-07-18: 열람용 모달 `.dismissable` 클래스+통합 배경클릭 핸들러 — profModal·cfgModal·kpModal·ludusModal. 결정·입력 모달은 제외) | 1 | ✅ |
| G12 | 소형 아바타 초상 잘림 | → ✅ (코치마크: 정사각 cover가 전신을 작게 담던 것 → `.avatar` 1.7배 얼굴 확대. 대화 초상은 G13이 원인) | 1 | ✅ |
| G13 | 가로스크롤 전역 규칙이 초상 클립 | → ✅ (`#stage *{overflow-x}` 전역이 `left:-200px` 대화 초상까지 클립+세로축 auto 부작용 → 스크롤 컨테이너 한정 `.screen/.pad/[overflow:auto]`) | 1 | ✅ |
| G8 | 모션 토큰 부재 | → ✅ (R3: theme.css `--dur-fast/mid/slow`·`--ease-out/snap` — 새 전환은 토큰만) | ~~3~~ 1 | R3 ✅ |
| G9 | 오류·불가 사유 미표시 | → ✅ (R3 2026-07-21: `[data-why]` 사유 툴팁 — theme.css 공용 CSS만, JS 핸들러 0. 비활성 버튼은 클릭 이벤트를 못 받지만 `:hover`는 받는다는 비대칭을 이용. 헬퍼 `lock(ok,why)`/`lockEl(el,ok,why)`가 사유 문자열을 발행: *"영광이 모자란다 — 12 필요, 지금 4"* · *"숙소가 찼다 — 5명이 한계다"*. **말풍선은 버튼 아래·왼쪽 모서리 기준**(조상이 `overflow-x:hidden`+`overflow-y:auto` 스크롤 컨테이너라 위/중앙 정렬은 잘린다 — 중앙 정렬 시 −70px 잘림 실측 후 교정) | ~~3~~ 1 | R3 ✅ |
| G10 | 텍스트 톤 혼재 | ~~시스템체와 세계관체 혼용~~ → **0** ✅ (R5 2026-07-30: 한 게임에 사극 말투가 다섯 종류 섞여 있던 것을 인월드 구어 하나로. 실측 — 하오/하게체 0건, 시스템 안내체(~하세요·~해주세요) 0건, 「~됩니다」 잔여 12건은 전부 카토·테아·무레나가 존댓말로 말하는 자리. 오류 문구도 「갚을 빚이 없다」식 인월드 평서) | ~~3~~ 1 | R5 ✅ |

## 1. 화면별 감사 (짜침 내림차순)

| 화면 | 스크롤 | 이모지 | 짜침 | 주요 문제 → 처방 |
|---|---|---|---|---|
| **리그** | ~~6.2×~~ → **0** ✅ | 0 | ~~5~~ 1 | R2: iframe 해체 — 네이티브 3분할(일정=이번 달 그리드 주인공·순위=부별 표·기록=경기/서사/관계그래프). league.html은 headless 대시보드 전용으로 존치 |
| **루두스** | ~~2.6×~~ → **0** ✅ | 0 | ~~5~~ 1 | R2: 지휘실 서브탭 4분할(지휘실=등급·목표보드 / 모리튜리=선수단 관리 / 시설·특전·업적 / 시즌=컵·대항전) — 기존 렌더 로직 무수정(마크업 재배치) |
| **모달·다이얼로그** | — | — | **5** | confirm() 4곳(방출·자유·인수·매각 = 하필 가장 극적인 순간이 브라우저 팝업), 오류 alert 4곳 → R1 hold-to-confirm·인월드 오류 배너 |
| **로스터** | ~~2.4×~~ → **0** ✅ | 0 | ~~4~~ 2 | R2: 좌 레일(루두스 순위·통산·전설) + 우 명부(내부 스크롤 서가). 클릭 타겟 div → R3에서 버튼화 |
| **관전(기록실)** | 1.0×+내부 스크롤러 | **307** | **4** | 이모지 밀도 1위(목록 행마다 🎬). 웹 게시판 감. 검색 input+체크박스. → R1 이모지, R2 목록 재설계 |
| **신문** | 3.1× | 25 | 3 | 콘셉트(세피아 신문)는 세계관에 맞음 — 스크롤도 신문은 변명 가능. 다만 지면 넘김(페이지제)이 더 게임답다. → R2 후순위 |
| **도박장** | ~~1.5×~~ → **0** ✅ | 0 | ~~3~~ 2 | R2: 좌=베팅 빌더(주인공) / 우=장부·이력 2컬럼 |
| **설정 모달** | — | — | 3 | checkbox 6·select 2 = 가장 "웹 설정창" 같은 곳. → R1 토글/세그먼트 교체 |
| **타이틀/메뉴** | — | — | 3 | 각본 스킵이 기본 checkbox. 슬롯 카드는 양호. → R1 |
| **뷰어(경기)** | 1.0 | 47 | 2 | 게임화면 자체는 최상. 배속 `<select>`·지시 태그 이모지(😡🔥 등)·⏸ 텍스트 버튼이 옥에 티. → R1 |
| **패싸움(melee)** | 1.0 | 10 | 2 | 뷰어와 동급. select 1. → R1 |
| **보관함** | 1.3× | 13 | 2 | 최근 리스킨— 양피지 카드 양호. → R1 이모지만 |
| **기록실/후원자/도감/시장** | ~1.0 | 1~20 | 2 | 한 화면 안. 이모지 헤더만. → R1 |

## 2. 이모지 → 아이콘 치환 우선순위 (빈도순, 서버 토큰 (a)안)

`💰43+18` `🏛22+8` `🔥20+10` `📜18+8` `🎭17+11` `⚔16+12` `✨14+11` `👑12+8` `🎲10+11` (index+Game.cs) — 이 9종이 전체의 절반.
서버는 `{gold}` `{ludus}` `{streak}` `{scroll}` `{fest}` `{duel}` `{glory}` `{crown}` `{dice}` 토큰 발행, 클라이언트 렌더 단일 관문에서 `icons.svg` `<use>` 치환. 감정 이모지(😡😨 등 뷰어 지시 태그)는 아이콘이 아니라 **텍스트 라벨+색**으로.

## 3. 라운드 매핑 요약

- **R2-fix2 ✅ (2026-07-17)** = 라니스타 피드백 8건: ①시즌 중 영입 합류전을 **잔여 일정에 균등 분산**(데뷔 21경기 뒤→3경기 뒤. 라운드 번호로 자리를 찾으면 구버전이 꼬리에 몰아둔 합류전에 걸려 또 끝으로 간다 — 인덱스 균등 배치로 해결, 베팅·배당 커서 보정) ②로스터 = 개별 스크롤 폐지·화면 전체 스크롤 ③**가로 스크롤 전역 금지**(`#stage *{overflow-x:hidden}` + theme.css html/body + designlint `scroll-x` 규칙) ④리그 진입 시 항상 일정 뷰 + 오늘의 달 ⑤**관전 화면 고정**(viewer/melee = `#stagebox` contain 배치 — 리플레이 스크롤 207px→0, 라이브 0) ⑥전술창 하단 잘림 = **뷰포트 단위 11곳**이 무대 좌표계와 어긋난 것(`92vh`가 배율 1.13에서 828px>무대 800px) → 전부 무대 기준으로 교체 + designlint `viewport-unit` 규칙(셸 한정) ⑦도박장 = 장부·이력 레일 좌 / 베팅 카드 우·상단(넘침 0) ⑧「각본 없이 시작」→ 메인메뉴에서 제거, **[새 커리어] 확인창의 「캠페인 생략」**으로 이동.
- **R2-fix ✅ (2026-07-17)** = 라니스타 실플레이 피드백 수정: ①서브뷰 겹침 — `hidden` 속성은 인라인 `display:flex`를 못 이긴다(리그 3뷰 동시 표시·지휘실 상시 켜짐 원인) → `style.display` 직접 토글 ②상단바 짤림 — **2단 재설계**(1단=로고·탭·설정 / 2단=재화·상태·다음 경기), 다음 경기 버튼 ellipsis(max 340px)·전 요소 nowrap·등급/상태 tcut ③**무대 맞춤 옵션**(설정): 비율 적응(기본 — 모니터 비율대로 무대 폭 1160~1600 확장, 레터박스 없음) / 고정 16:10, UI 배율과 조합 가능 ④resize 신호 누락 웹뷰 대비 400ms 폴링 폴백 ⑤로스터 재배치(루두스 순위=전폭 상단 밴드·명부=주인공·기록실=우측 레일). 검증: 900×700·1280×720·1600×900 × 적응/고정 × 배율 125 — 전 탭 오버플로 0·상단바 오버플로 0.
- **R2 ✅ (2026-07-17)** = G4 + 리그·루두스·로스터·도박장 화면화. 고정 무대 `#stage`(1280×800 기준, zoom fit — `fitStage()`, UI 배율=가상 해상도 조절), 페이지 스크롤 0 (신문=지면 스크롤 유지·관전/명부=내부 서가 스크롤). 리그 iframe 제거 — `renderLeague()/lgPaint()` 네이티브(`season.json` fetch), 서브탭 `.seg`.
- **R1 ✅ (2026-07-17)** = G1·G2·G3 완료 — lint 789 → 0. **커밋 전 `dotnet run -- designlint gate` 필수** (위반 시 exit 1).
  - 아키텍처: 서버는 `{coin}` 등 60종 토큰 발행 → 클라 `ui.js` 단일 관문이 SVG 치환(MutationObserver 텍스트 노드). 구세이브 이모지는 `normLegacy`(api 응답 정규화)+DOM 관문 이중 호환. **이모지는 ui.js 레거시 맵에만 존재 가능.**
  - 규약: 새 UI 코드는 `{토큰}` 또는 `IC('이름')`만. 속성(title 등)은 `plainTip()`으로 토큰 제거. 캔버스 fillText엔 토큰 금지(순수 텍스트).
- **R2** = G4·G7 + 리그 iframe 해체·루두스 재설계·로스터/관전 화면화 (전면 개편 확정)
- **R3 ✅ (2026-07-21)** = G5·G6·G8·G9 완료 (반응성 공용 레이어). G7은 어포던스까지(◐ — 시맨틱 버튼화는 후속).
  - 규약: **비활성 버튼은 이유를 말한다.** 새 잠금 버튼은 `${lock(조건, '사유')}`(마크업) 또는 `lockEl(el, 조건, '사유')`(속성 대입)로 발행할 것 — `disabled`만 달면 침묵한다.
  - 사유 문구는 **무엇이 얼마나 모자란지**까지: *"영광이 모자란다 — 12 필요, 지금 4"*. "불가"·"조건 미달" 같은 결과만 쓰지 않는다.
  - 함정: 이 버튼들의 조상은 `overflow-x:hidden` 스크롤 컨테이너다. 말풍선은 **아래·왼쪽 모서리 기준**으로만 띄운다(중앙 정렬은 좌측 −70px 잘림 실측).
- **R4 ✅ (2026-07-18)** = 모멘트 연출 + **장례 시퀀스** — `showMoment()` 전화면 정지(우승·컵·대항전=금빛 「월계관이 내려온다」, 타이틀 인라인 등장), `showFuneral()` 사망 시 다단계(어둠 1s 하강 → 묘비 SVG 떠오름 → 이름+심장북 → 「이명·통산·모래가 삼켰다」 비문, 내 선수면 루두스 그림자 문구). 서버 데이터(부고 문자열+로스터/명전 전적·이명)만 사용. **핵심 교훈: 프리뷰 웹뷰가 CSS 애니메이션을 0%에 얼려 실행중 애니메이션이 인라인을 이김 → 모멘트 가시성·단계 등장을 전부 JS 인라인 opacity로 구동**(momentIn 키프레임 제거). 톤: 저장 「장부에 기록했다」(대부분 문구는 이미 교관·의사 등 인월드 화자).

- **R5 ✅ (2026-07-30)** = **대사·스토리 검수** — G10 종결. 커밋 5건.
  - **표기:** 「이(가)/을(를)/은(는)/과(와)」 병기 48곳 제거 — 이름은 런타임에 아는데 병기할 이유가 없다. 서버 `Josa/Ga/Reul/Neun/Wa`, 클라 `jo()`(마크업이 이름과 조사 사이에 끼는 자리가 있어 **조사만** 반환). 용어 일괄치환이 깨뜨린 조사 4곳(「라니스타을 노려본다」·「출전자는 라니스타이 고른다」·빅매치 제안창).
  - **번역체:** 전투 직전 대사 27줄(호격 「관중이여」·「그대」·하오체·"네 비명이 벌써 들리는군") · 랜덤 이벤트 화자 14곳(하게체 4인·하오체 전령·호격 사제·속담 격언) · 튜토리얼 목소리 14곳 — **안내 투어 12칸이 전부 반말인데 화자 라벨·초상·서명은 카토였다**, 루킬리우스 레슨만 하게체였다.
  - **문형:** 신문 산문 20개가 예외 없이 「사실 + 격언」 2절 대구 → 보도문으로("월계관은 무겁고 지키기란 더 무겁다" → "결승 관중은 올해 최다였다"). 「모래는 정직하다」 5곳 → 4곳(유서·S0·A0·종막만 — 매 경기·매 호에서 되풀이되며 회수의 힘을 빼앗고 있었다). 「…나중에 알게 된다」 2회 → 0(한 번은 연출, 두 번은 장치가 보인다).
  - **개연성 6건:** 테아의 시간선이 카토의 자백과 충돌(19해 재직인데 20년 전을 목격 → 「저만 알았습니다」 붕괴) · 후일담 E1~E3에 **내친 카토가 등장**(C0·C1·종막만 분기를 갖고 있었다) · 조기 종막이 자백(B7)을 건너뛰어 후일담이 듣지도 못한 「반쪽」을 아는 척 · A6가 조작한 선수가 아니라 `MyFirst`를 호명 · 「진상의 반쪽」이 스폰되지 않아 E아크의 유일한 복선이 죽어 있음 · 「나가라」가 약속한 훈련 페널티 미구현.
  - **[13a] 부록 B·C 구현:** 리액티브 비트(**화자가 트리거마다 다르다 — 카토가 있어도 몸 이야기는 테아가 한다**) + 막간 대화 25줄(라운드당 1회, 다이제스트 상단·없으면 토스트).
  - **함정:** 파이썬 일괄치환 스크립트는 **Bash 히어독에 한글을 넣으면 깨진다**(스크래치패드에 파일로 쓸 것) · `newline=''`로 읽고 써도 여러 줄 매칭은 CRLF로 맞춰야 한다(안 그러면 파일 전체가 LF로 평탄화되는데 git diff는 정규화 때문에 조용하다) · **esprima는 파일 앞쪽 옵셔널 체이닝에서 멈춰** 뒤쪽 편집을 검증하지 못한다 → 브라우저 재적재로 확인할 것.
  - **실플레이가 잡은 것 2건:** 보고만 하고 안 고친 조사 1곳(하필 빅매치 제안창) · 라운드가 내 경기에서 끊겼다 재개되면 막간이 같은 라운드로 두 번 발화(시드가 라운드 기반). **둘 다 테스트는 통과한 상태였다.**

> 갱신 규칙: 라운드 완료 시 해당 행 점수 재평가. designlint 수치는 커밋 전 재실행 값으로.
