# MORITURI — [5] 데이터 테이블 정의서
**버전:** 0.1 (Phase 1 범위 ★ / Phase 2+ 예약 스키마 포함)
**원칙:** 모든 밸런스 수치는 테이블에 있고, 코드에는 단 하나의 매직 넘버도 없다.

---

## 1. 파이프라인

```
Google Sheets (기획/튜닝 작업 공간)
   │  CSV export
   ▼
Tools/DataImporter  ── 검증(범위, 참조 무결성, ID 중복) ──→ 실패 시 리포트
   │
   ▼
ScriptableObject (Unity 에디터에서 열람/오버라이드 가능)
   │  Bake
   ▼
Sim POD 구조체 (런타임은 이것만 읽음, 불변)
```

- **왜 SO 직접 편집이 아니라 시트 경유인가:** 상성 튜닝은 표 전체를 보며 수십 셀을 한 번에 만지는 작업이다. 시트가 작업대, SO는 산출물.
- ID 규칙: `<도메인 약어>_<영문 슬러그>` — 예: `WPN_AXE`, `TAC_PRESSURE`, `PER_COWARD`. 정수 ID 금지(머지 충돌·가독성 문제).
- 모든 테이블 공통 컬럼: `id`, `nameKey`(로컬라이즈 키), `schemaVer`, `enabled`(밸런스 테스트에서 임시 제외용).

---

## 2. ★ T01_Weapons (무기)

문서[4] 7장의 정규화 버전.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | string | WPN_* |
| baseDamage | float | 기본 데미지 |
| range | float | 사거리 (m) |
| motionSpeed | float | 모션 속도 계수 |
| recoverySec | float | 후딜 (초) |
| poiseDamage | float | 경직 피해 |
| poiseMax | float | 사용자 Poise 최대치 (M1 추가 — "도끼 적중→창 Stagger" 시나리오 성립 조건, 문서[4] 7장) |
| guardCrush | float | 가드 게이지 깎기 계수 |
| innerRangePenalty | float | 사거리 안쪽 침투 시 데미지 배율 (전역 상수 InnerRangePenalty=0.6) |
| guardGaugeBonus | float | 방패 전용 +0.6, 그 외 0 |
| guardBypass | float | 채찍 전용 0.1, 그 외 0 |
| motionSetId | string | → T02 참조 |
| **hitCount** | int | 타수 (쌍검 2, 그 외 1) — *동기화 2026-06-26* |
| **hyperArmor** | bool | 대검·망치: 강공 선딜 중 약공 관철 (M3-A2) |
| **heavyBias** | float | 중량무기 강공 선호 가중 (1.5) |
| **moveSpeedMult** | float | 무기 중량 이속 배율 (쌍검 1.15~망치 0.82) |
| **bleedDps** | float | 출혈: 도끼 전용, 클린히트당 1스택(최대 BleedMaxStacks), 스택당 초당 피해 |
| **parryWindowSec** | float | 방패 전용: 가드 진입 후 패링 자격창 (0.15) |
| **engageRangeRatio** | float | AI 교전 유지거리 = min(선호거리, 사거리×이값). 카이터 0.92~0.95 |
| **postAttackRetreatSec** | float | 공격 후 후퇴 강제 시간 (카이터 창·채찍) |

## 3. ★ T02_WeaponMotions (무기 모션)

무기당 약공/강공(+무기 고유 모션)의 프레임 데이터. **상성의 실체가 이 테이블에 있다.**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | string | MOT_AXE_HEAVY 등 |
| weaponId | string | → T01 |
| kind | enum | Light / Heavy / Special |
| damageMult | float | 약공 0.7 / 강공 1.5 기본 |
| windupSec | float | 선딜 |
| activeSec | float | 히트박스 활성 |
| hitboxShape | enum | Thrust(직선) / Arc(호) / Point |
| hitboxLength / arcDeg | float | 형태별 치수 |
| staminaCost | float | |
| cancelWindow | float | 선딜 중 캔슬 가능 비율 (기본 0.7) |

## 4. ★ T03_Tactics (전술 프로파일)

문서[3] 4.2 테이블 그대로. 컬럼: `preferredDistance, distanceTolerance, aggression, commitThreshold, counterWindow, guardBias, feintRate, riskTolerance, staminaReserve` + `uniqueRuleId`(전술 고유 조건, 프로파일당 최대 1개, → T04 참조).

## 5. ★ T04_TriggerRules (트리거 규칙)

성격 트리거와 전술 고유 조건이 **같은 스키마**를 쓴다 (엔진 단일화).

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | string | TRG_* |
| ownerId | string | PER_* 또는 TAC_* |
| conditionType | enum | (현 코드 `TriggerCondition`) SelfHpBelowPct / SelfHpAbovePctAndWinning / OppHpBelowPct / ConsecHitsTaken / OppHeavyWindup / OppDown / CritTakenWithinSec / TimeRemainPctBelowAndLosing / OppGuardGaugeBelowPct / **OppExhausted** / **OppStaggeredWhileAhead**(M3-B 도발) / **WasTaunted**(피도발 반응) / StaminaNearReserve / SameAttackWhiffedTwice / HpDeficitPct / OppIsNemesis(Phase 2 예약, 현재 false) |
| conditionValue | float | 조건 임계값 |
| effectType | enum | **Override** / **Interrupt** |
| effectPayload | json | Override: `{param: value, ...}` / Interrupt: `{action, duration}` |
| probability | float | 0~1 |
| cooldownSec | float | |
| durationSec | float | Override 지속 시간 |
| reasonTag | string | UI 아이콘 키 (→ T08) |

> conditionType의 enum 목록이 곧 "성격 시스템의 표현력"이다. Phase 2 감정/관계는 여기 enum 추가로 들어온다.

## 6. ★ T05_Personalities (성격)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | string | PER_* |
| triggerIds | string[] | → T04 (성격당 1~3개) |
| globalProbMod | float | 냉철함 -0.5, 그 외 0 |
| globalParamMod | json | 상시 파라미터 보정 (예: 신중함 commit+0.2) |

## 7. ★ T06_BalanceConstants (전역 상수)

문서[4]에 흩어진 계수의 단일 출처. 예시 행:

| key | value | 출처 |
|---|---|---|
| DEF_CURVE | 0.8 | [4] 2장 |
| CRIT_MULT | 1.6 | [4] 2장 |
| COUNTER_MULT | 1.35 | [4] 2장 |
| GUARD_DMG_MULT | 0.25 | [4] 4장 |
| UTILITY_NOISE | 0.10 | [3] 6.2 |
| DECISION_TICK_SEC | 0.2 | [3] 1장 |
| MATCH_TIME_SEC | 180 | [4] 10장 |
| STAM_COST_WHIFF | 12 | [4] 6장 (M3-A: 16→12) |
| EXHAUST_DURATION | 3.0 | [4] 6장 (M3-A: 원값 복귀) |
| EXHAUST_DMG_TAKEN_MULT | 1.3 | [4] 6장 (M3-A: 2.2→1.3, 신규 처벌경로와 중첩 완화) |
| GUARD_STAMINA_COST_RATIO | 0.06 | [4] 4장 (M3-A: 0.15→0.06) |
| GUARDED_RECOVERY_MULT | 1.65 | [4] 4장 (가드시킨 공격 후딜 — 프레임 불리) |
| ATTACK_GATE_SCALE | 0.9 | [3] 6.2 (M2: 1.6→0.9) |

**동기화(2026-06-26) — `BalanceConstants.cs` 신규 상수군** (코드가 단일 출처, 상세값은 [현황 §8~9](MORITURI_현황.md)):

| 상수군 | 키 (예) | 용도 |
|---|---|---|
| 전역 데미지 | `DamageGlobalMult`=1.0 | TTK 단축 노브(전 무기 비율 보존) |
| 출혈 | `BleedDurationSec`=4, `BleedMaxStacks`=3 | 도끼 출혈 트랙 |
| 패링(방패) | `ParryChance`=0.4, `ParryRefundStamina`=12, `ParryStunStacksMax`=3, `ParryStunDecaySec`=3, `ShieldGuardBreakStaggerSec`=0.5 | 방어형 메커니즘 |
| 선취점 쉴드 | `FirstBloodShield`=50, `FirstBloodShieldSec`=8 | 선취점 특성·향후 방패 액티브 |
| 카이팅 | `KiteStamCostPerSec`=1.5, `KiteCostMinRange`=3.0 | 카이터 가스아웃 비용 |
| 스티어링(안1) | `SteerArriveBand`=0.4, `SteerOrbitGain`=0.35, `SteerMaxAccel`=10 | 라벨→위치 번역 매끄럽게(연출) |
| 스페이싱(안2) | `SpacingDwellSec`=0.35, `SpacingHoldReleaseRatio`=0.4 | 거리 댄스 억제 히스테리시스 |
| 판단주기 지터 | `DecisionJitterMinMult`=0.4, `DecisionJitterMaxMult`=1.6, `TauntProbMult`=0.2 | 불규칙 반응 리듬 + 도발 보정 |
| 인내심 | `PatienceMax`=100, `PatienceDrainBase`=10, `PatienceImpulseScale`=2.0 | 영원 대치 해소 |
| 아레나(2D) | `ArenaRadius`=12, `StartGap`=4, `CornerZone`=1.5, `CollisionRadius`=0.6 | 원형 핏·disc 충돌 |
| 거리 판정 | `InnerRangeRatio`=0.4, `MinLongRange`=3.0 | 침투 패널티 게이트 |
| 도발 분노 | `TauntRageAggression`=0.25, `TauntRageCommitAdd`=−0.10, `TauntRageDurationSec`=5.0 | 도발당한 상대 반응 |
| 관중게이지 | (코드 내장) `Crowd*` — 적립/감쇠/데드존/버프 | → [10] (T14_CrowdTuning으로 외부화 예정) |

## 8. ★ T07_FighterTemplates (테스트용 선수 원형)

검증용 고정 선수 셋(`FighterDef`). 컬럼: 6대 스탯(`FighterStats`), `tacticsId`, `personalityId`, `weaponId`, 선택적 `traitIds[]`. 기준점은 스탯 70 균일(`FighterStats.Baseline`) × (전술 10 × 성격 10) 조합 — 상성 매트릭스 배치 테스트의 입력. *(실제 게임 선수는 `StatGen`/`TraitGen`이 생성.)*

## 9. ★ T08_ReasonTags (UI 아이콘 매핑)

| 컬럼 | 설명 |
|---|---|
| tag | RECKLESS, FEAR, TAUNT ... |
| iconKey | 아틀라스 스프라이트 키 |
| auraColor | 캐릭터 오라 색 (없으면 null) |
| speechPoolId | 컷인 대사 풀 (Phase 1은 null 허용) |

문서[3] 8장 DecisionEvent의 reasonTag가 이 테이블로 해석된다 — **AI 내부 상태와 UI의 유일한 접점.**

## 10. 예약 스키마 (Phase 2+, 컬럼만 확정)

- **T09_Traits (특성) — ✅ 구현됨 (`TraitData.cs`):** 14종 + 생성 추첨(`TraitGen`: 1개 75%/2개 20%/3개 5% + 상반 배타).
  현 구현은 hookType 분류 대신 `TraitDef`의 파생스탯 배율 필드(받피·스태미나·이속·포이즈·사거리·크기·가드받피 등)와
  고유행동 Id(좀비·숨고르기·초상비·선취점)로 표현. 카탈로그·효과는 [7]§6 / [현황 §6]. *AgingMod/GrowthMod(천재·저속노화)는 나이 시스템 도입 시.*
- **T10_Emotions (감정) — ✅ 구현됨 (`EmotionData.cs`, decision-only 재정의):** 8종 + 생성기(`EmotionGen`).
  **원안 `atkMod`/`defMod` 삭제** — 라니스타 확정: 감정은 의사선택에만 영향, 데미지·스탯 배율 무관. 현 스키마 = `(id, name, triggerProbMod, ParamMod[] mods, genChance, decaySec)`.
  생성 2단계: `Classify`(결과×성격 → 감정 유형) + `Roll`(발생률 `genChance` 게이트 — 감정은 매 경기가 아니라 **가끔 생기는 이벤트성 변화구**, 실측 ~30%만 발생). 8종(자신감/자만/부담감/열등감/동기부여/좌절/트라우마/원한). decaySec는 Phase 3(영속)까지 미사용. 영속 이월·성격 변화 입력은 Phase 3~4.
- **T11_Relations (관계):** typeId(친구/라이벌/원수/존경/질투/공포/집착), thresholdMin/Max(-100~100 구간), paramMod, eventHooks. **미구현**(OppIsNemesis 항상 false).
- **T12_Skills / T13_RankTiers (스킬·계급):** → [6][7]. **미구현.** 패시브=트리거 엔진 재사용, 액티브=모션 추가, 슬롯경제.
- **T14_CrowdTuning (관중게이지):** 적립 델타·감쇠·데드존·버프계수. 현재 코드 내장, 외부화 예정. → [10].

---

## 11. 검증 규칙 (임포터가 강제)

1. 참조 무결성: 모든 `*Id` 컬럼은 대상 테이블에 존재해야 함.
2. 범위 검사: 확률 0~1, 스탯 1~150, 거리 0~3.2.
3. **전술 고유 조건 ≤ 1개/프로파일** (문서[3] 4.2 규칙의 기계적 강제).
4. reasonTag 사용처 ↔ T08 등록 여부 교차 검사 (아이콘 없는 트리거 = 가독성 원칙 위반으로 빌드 경고).
