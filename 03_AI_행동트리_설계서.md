# MORITURI — [3] AI 행동 트리 / 상태 머신 설계서
**버전:** 0.1 (Phase 1 전투 프로토타입용)
**대상 범위:** 1v1 실시간 자동 전투의 AI 의사결정 전체. 감정/관계/기억은 "입력 인터페이스"만 정의하고 구현은 Phase 2로 미룸.

---

## 1. 설계 원칙

1. **전술(Tactics) = 뼈대, 성격(Personality) = 변수.** 120개 조합(전술 10 × 성격 12)을 개별 AI로 만들지 않는다. 전술은 *파라미터 세트*, 성격은 *트리거 규칙 세트*로 분리하여 조합 폭발을 막는다.
2. **3계층 구조.** 의사결정을 전략층 → 전술층 → 실행층으로 분리한다. 디버깅과 시각화(내러티브 가독성)를 위해 각 층의 "현재 결정 이유"를 항상 Blackboard에 남긴다.
3. **모든 결정은 결정 Tick에서만.** 물리/애니메이션 Tick(60Hz)과 의사결정 Tick(5Hz, 0.2초)을 분리한다. 의사결정 빈도 자체도 능력치(반응속도)와 성격의 영향을 받는다.
4. **관전 가독성 우선.** AI가 "왜 저러는지"를 플레이어가 알 수 있도록, 행동 변경 시 반드시 `DecisionReason` 이벤트를 발행한다. UI는 이 이벤트를 구독해 아이콘/말풍선/오라를 출력한다.

---

## 2. 전체 아키텍처 (3계층)

```
┌─────────────────────────────────────────────┐
│ Layer 1. 전략층 (Strategy)        주기: 1~3초 │
│  - 전술 프로파일 적용 (목표 거리, 공격성 등)      │
│  - 성격 트리거 평가 → 전술 파라미터 override     │
│  - 출력: TacticalDirective (지향 상태)         │
├─────────────────────────────────────────────┤
│ Layer 2. 전술층 (Tactical)        주기: 0.2초  │
│  - Utility 기반 행동 후보 평가                  │
│  - 출력: ActionRequest (Approach/Attack/...)  │
├─────────────────────────────────────────────┤
│ Layer 3. 실행층 (Execution FSM)   주기: 60Hz   │
│  - 애니메이션/이동/판정 수행                     │
│  - 인터럽트 처리 (피격 경직, 다운, 성격 인터럽트)  │
└─────────────────────────────────────────────┘
```

- 전략층은 GOAP까지 갈 필요 없음(Phase 1 기준). **"파라미터화된 Utility AI + 실행 FSM"** 이 가장 빠르게 상성 검증이 가능한 구조다.
- Phase 2에서 감정/관계가 들어오면 전략층의 트리거 평가에 입력만 추가하면 된다 (구조 변경 없음).

---

## 3. Blackboard (공유 데이터)

모든 계층이 읽고 쓰는 선수별 런타임 데이터.

```csharp
public class CombatBlackboard
{
    // --- 자기 상태 ---
    public float HP, HPMax;
    public float Stamina, StaminaMax;
    public float Poise;                 // 경직 저항 게이지
    public float GuardGauge;            // 가드 내구도
    public Vector2 Position;
    public FacingDir Facing;

    // --- 상대 인지 (반응속도에 따라 지연된 스냅샷) ---
    public PerceivedOpponent Opponent;  // 위치, 현재 모션, 모션 경과시간, 추정 HP

    // --- 전략층 출력 ---
    public TacticalDirective Directive; // 아래 4장 참조

    // --- 전술층 출력 ---
    public ActionRequest CurrentAction;
    public string DecisionReason;       // UI/로그용. 예: "TRIGGER:충동적/HP30"

    // --- 누적 컨텍스트 (라운드 내) ---
    public float TimeRemaining;
    public int   ConsecutiveHitsTaken;  // 연속 피격 수
    public int   ConsecutiveWhiffs;     // 연속 헛스윙 수
    public float LastBigHitTime;        // 치명타 피격 시각
    public float CornerPressure;        // 0~1, 구석 몰림 정도
}
```

---

## 4. 전략층: 전술 프로파일

### 4.1 TacticalDirective 구조

전술은 코드가 아니라 **데이터(ScriptableObject)** 다.

```csharp
public class TacticsProfile : ScriptableObject
{
    public float PreferredDistance;   // 유지하려는 거리 (m)
    public float DistanceTolerance;   // 허용 오차 (m)
    public float Aggression;          // 0~1, 공격 행동 기본 가중치
    public float CommitThreshold;     // 0~1, 공격 확신도 요구치 (높을수록 신중)
    public float CounterWindow;       // 상대 모션 인지 후 반격 시도 확률 가중
    public float GuardBias;           // 방어 행동 가중치
    public float FeintRate;           // 페인트/견제 공격 비율
    public float RiskTolerance;       // 체력 손실 감수 성향 (도박사형 ↑)
    public float StaminaReserve;      // 이 비율 이하로 스태미나를 쓰지 않음
}
```

### 4.2 전술 10종 기본값 테이블

| 전술 | 목표거리(m) | Aggression | Commit | Counter | Guard | Feint | Risk | StamReserve |
|---|---|---|---|---|---|---|---|---|
| 압박형 | 1.0 | 0.70 | 0.35 | 0.10 | 0.20 | 0.15 | 0.50 | 0.10 |
| 카운터형 | 2.0 | 0.30 | 0.60 | 0.80 | 0.50 | 0.10 | 0.40 | 0.25 |
| 견제형 | 2.8 | 0.45 | 0.50 | 0.30 | 0.30 | 0.50 | 0.30 | 0.30 |
| 난전형 | 0.7 | 0.80 | 0.25 | 0.15 | 0.10 | 0.20 | 0.70 | 0.05 |
| 판정형 | 2.2 | 0.35 | 0.55 | 0.35 | 0.55 | 0.40 | 0.15 | 0.40 |
| 사냥꾼형 | 1.6 | 0.55 | 0.65 | 0.40 | 0.30 | 0.35 | 0.45 | 0.20 |
| 도박사형 | 1.2 | 0.65 | 0.20 | 0.25 | 0.10 | 0.25 | 0.90 | 0.00 |
| 회피형 | 2.5 | 0.25 | 0.55 | 0.45 | 0.20 | 0.30 | 0.20 | 0.35 |
| 방어형 | 1.4 | 0.20 | 0.50 | 0.50 | 0.80 | 0.10 | 0.10 | 0.35 |
| 균형형 | 1.6 | 0.50 | 0.45 | 0.35 | 0.40 | 0.30 | 0.40 | 0.20 |

> **튜닝 메모:** 사냥꾼형은 "상대 HP가 낮거나 상대가 스태미나 고갈 상태일 때 Aggression +0.3"이라는 전술 고유 조건 1개를 가진다(상처 입은 사냥감 추적). 전술 고유 조건은 프로파일당 최대 1개로 제한 — 그 이상의 변주는 전부 성격으로 처리한다.

---

## 5. 전략층: 성격 트리거

### 5.1 트리거 규칙 구조

성격은 **(조건 → 효과)** 규칙의 리스트다. 효과는 두 종류뿐:
- **Override:** 전술 파라미터를 일시 변경 (지속형)
- **Interrupt:** 실행층에 강제 행동 주입 (즉발형, 예: 도발)

```csharp
public class PersonalityTrigger
{
    public TriggerCondition Condition;  // 평가식
    public TriggerEffect    Effect;     // Override or Interrupt
    public float Probability;           // 트리거 발동 확률 (매 평가 시)
    public float CooldownSec;           // 재발동 쿨다운
    public string ReasonTag;            // UI 아이콘 키. 예: "RAGE", "FEAR"
}
```

### 5.2 성격 12종 트리거 정의 테이블

| 성격 | 조건 | 효과 | 확률 | ReasonTag |
|---|---|---|---|---|
| 냉철함 | (상시) | 모든 트리거 발동 확률 -50%, Commit +0.10 | — | CALM |
| 충동적 | 자기 HP ≤ 30% | Override: Aggression=0.9, Guard=0, 거리=0.6m (10초) | 80% | RECKLESS |
| 충동적 | 연속 피격 ≥ 3 | Override: Aggression +0.3 (5초) | 60% | RECKLESS |
| 오만함 | 상대 HP ≤ 10% | **Interrupt: 도발 1.5초** (무방비) | 70% | TAUNT |
| 오만함 | 자기 HP ≥ 80% & 우세 | Override: Commit -0.15 (대충 침) | 40% | TAUNT |
| 명예중시 | 상대 다운 상태 | Interrupt: 거리 벌리고 대기 (추가타 금지) | 95% | HONOR |
| 명예중시 | 관중 호응 高 | Override: Aggression +0.1 | 50% | HONOR |
| 겁쟁이 | 상대 강공 모션 인지 | Interrupt: 후방 회피 (반격 기회 포기) | 65% | FEAR |
| 겁쟁이 | 치명타 피격 후 8초 | Override: 거리 +1.0m, Aggression -0.3 | 90% | FEAR |
| 복수심 | 상대=원수 (Phase 2 입력) | Override: Aggression +0.25, Risk +0.3 (상시) | 100% | VENGE |
| 집착적 | 같은 공격 2회 빗나감 | Override: 동일 공격 반복 가중 +0.4 | 70% | OBSESS |
| 낙천적 | 자기 HP ≤ 20% | Override: 패널티 없음 (위축 면역), 스태미나 회복 +10% | 100% | HOPE |
| 비관적 | 열세 판정 (HP差 ≥ 25%) | Override: Guard +0.3, Aggression -0.2 | 75% | GLOOM |
| 잔혹함 | 상대 **취약 상태** 인지: 가드 게이지 ≤ 20% **또는** HP ≤ 30% **또는** 스태미나 고갈 | Override: Aggression=0.95, 강공 가중 +0.5 | 90% | CRUEL |
| 잔혹함 | 상대 다운 상태 | Interrupt: 즉시 추가타 시도 | 80% | CRUEL |
| 대담함 | 남은 시간 ≤ 20% & 열세 | Override: Risk=1.0, 강공만 선택 | 85% | BOLD |
| 신중함 | (상시) | Commit +0.20, 확신도 미달 시 공격 보류 | — | WARY |
| 신중함 | 스태미나 ≤ Reserve+10% | Override: Aggression -0.3 (즉시) | 100% | WARY |

> **기획 의도 보존:** 기획서의 "압박형+겁쟁이 = 전진은 하는데 반격 오면 구르는 허당"이 이 구조에서 자동으로 나온다. 전술이 거리 1.0m로 끌고 가지만, 상대 강공 모션마다 65% 확률로 Interrupt(후방 회피)가 터지기 때문.

> **개정 (M3):** 잔혹함의 1번 트리거를 "가드 붕괴"에서 **취약 상태 일반**(가드 붕괴/빈사/스태미나 고갈)으로 확장.
> 초판은 가드 게이지 단일 조건이라, 스태미나전 위주로 흐르는 경기(헛스윙 비용 도입 후)에서 가드가 깎이기 전에
> 끝나 트리거가 굶었다. "잔혹함은 특정 약점이 아니라 약함 그 자체를 냄새 맡는다"로 재해석 — 표현력도 더 넓다.
> 신규 트리거 조건 enum `OppExhausted`는 T04(문서[5])에 추가됨.

### 5.3 우선순위 충돌 규칙

1. **Interrupt > Override.** 실행 중 Interrupt가 오면 현재 행동을 캔슬 가능 여부 검사 후 교체.
2. Override 다중 발동 시 **나중 발동이 먼저 종료될 파라미터만 덮어씀** (스택 구조, 종료 시 롤백).
3. `냉철함`의 확률 감산은 다른 모든 트리거 평가 전에 적용 (전처리 modifier).

---

## 6. 전술층: Utility 행동 선택

### 6.1 행동 후보 (Phase 1: 8종)

| ActionRequest | 설명 |
|---|---|
| `Approach` | 목표 거리까지 접근 |
| `Retreat` | 거리 벌리기 |
| `Strafe` | 좌우 각 잡기 |
| `AttackLight` | 약공 (빠름, 저데미지, 저스태미나) |
| `AttackHeavy` | 강공 (느림, 고데미지, 경직 유발) |
| `Guard` | 방어 자세 (가드 게이지 소모로 데미지 경감) |
| `Dodge` | 회피 (무적 프레임, 스태미나 大 소모) |
| `Feint` | 공격 페인트 (상대 반응 유도) |

### 6.2 Utility 점수 산식

매 결정 Tick(0.2s)마다 8개 행동의 점수를 계산하고 최고점을 선택한다.

```
Score(action) = Base(action, Directive)
              × DistFit(action, currentDist, preferredDist)
              × StaminaFit(action, stamina, staminaReserve)
              × OpportunityMod(action, opponentState)
              × Noise(1 ± ε)
```

- **Base:** Directive 파라미터에서 도출. 예) `Score(AttackHeavy).Base = Aggression × (0.4 + 0.6×Risk)`
- **DistFit:** 사거리 밖 공격 = 0점. 목표 거리에서 멀수록 Approach/Retreat 점수 상승.
- **OpportunityMod (수싸움의 핵심):**
  - 상대가 강공 모션 중 & 내 CounterWindow 高 → `AttackLight`(카운터) ×2.5
  - 상대 헛스윙 직후(후딜) → 모든 공격 ×1.8 (CounterWindow가 추가 기여 — 후딜 처벌은 카운터형의 본업)
  - 상대 가드 중 → `AttackHeavy`(가드 깎기) ×1.4, `Feint` ×1.5
  - 상대 다운 → 성격 트리거에 위임 (명예중시/잔혹함 분기)
  - **상대 스태미나 고갈(Exhausted) 인지 → 약공 ×1.8, 강공 ×2.2 + 확정 사거리 취급** (M3 추가.
    지친 적은 반격 불가 = 후딜과 동급의 처벌 창. 문서[4] 6장의 받는 피해 ×2.2와 한 쌍)
- **회피 거리 게이트 (M3 추가):** 회피는 **상대 공격이 실제로 닿는 거리에서만** 가치를 가진다.
  사거리 밖에서 헛스윙할 공격에 반사적으로 구르면 스태미나만 탕진해, 정작 처벌 창에서 자신이
  지쳐 못 때린다 (창 vs 도끼 디버깅으로 발견 — 카운터형 영구 수비 루프의 근본 원인이었음).
- **Noise:** ε = 0.10 기본. **이변의 원천 1.** 능력치 차이가 커도 항상 비최적 행동 가능성을 남긴다.
- **Commit 게이트:** 공격 계열 행동은 `Score ≥ CommitThreshold × ScoreMax`일 때만 채택. 미달 시 차순위 비공격 행동 선택. (신중함이 공격을 "아끼는" 메커니즘)

### 6.3 인지 지연 (반응속도 구현)

상대 상태는 실시간이 아니라 `반응속도` 스탯에 따라 **지연된 스냅샷**으로 들어온다.

```
perceptionDelay = clamp(0.30 - 반응속도×0.002, 0.08, 0.30)  // 초
```

- 반응속도 100 → 0.10초 지연. 반응속도 30 → 0.24초 지연.
- 카운터형 전술의 성립 여부가 이 값에 달려 있다. 노화로 반응속도가 떨어지면 **같은 전술 프로파일인데도 카운터가 늦어 맞는다** — 세대교체 서사의 기계적 기반.

---

## 7. 실행층: FSM

### 7.1 상태 정의

```
            ┌──────────────────────────────────────┐
            ▼                                      │
 [Idle] → [Move] → [AttackWindup] → [AttackActive] → [Recovery]
   │         │           │ (캔슬가능)        │ (캔슬불가)     │
   │         ▼           ▼                ▼              │
   └──→ [Guard] ←── [Dodge]          (히트 판정)  ←───────┘
             │
   피격 시 ──┴──→ [HitStun] → [Stagger] → [Down] → [GetUp]
                                  │
                          성격 Interrupt 진입점
                            [Taunt] (오만함)
```

| 상태 | 캔슬 가능 시점 | 비고 |
|---|---|---|
| AttackWindup | Dodge/Guard로 캔슬 가능 (선딜 70%까지) | 겁쟁이 Interrupt가 여길 노림 |
| AttackActive | 캔슬 불가 | 히트박스 활성 구간 |
| Recovery | 캔슬 불가 | **후딜 = 카운터 기회.** 무기별 길이 상이 |
| HitStun | 불가 | Poise 파괴 시 Stagger로 전이 |
| Stagger | 불가 | 강공 확정타 허용 구간 |
| Taunt | 피격으로만 캔슬 | 역전패 제조기 (의도된 설계) |

### 7.2 인터럽트 처리 우선순위

```
1. 물리 강제 (HitStun, Stagger, Down)        — 무조건
2. 성격 Interrupt (Taunt, 강제회피, 추가타)    — 캔슬 가능 상태에서만
3. 전술층 ActionRequest 갱신                  — 현재 행동 완료 후
```

---

## 8. 내러티브 가독성 이벤트 (UI 연동 계약)

전략층/전술층이 결정을 바꿀 때마다 발행. Phase 1에서는 디버그 오버레이로만 표시하되, **이벤트 스키마는 처음부터 확정**한다 (UI 바인딩 설계서[7]의 입력이 됨).

```csharp
public struct DecisionEvent
{
    public int    FighterId;
    public string ReasonTag;     // "RECKLESS", "TAUNT", "FEAR" ...
    public string SourceLayer;   // Strategy / Tactical / Execution
    public float  Duration;      // 아이콘 점멸 시간
}
```

예: 충동적 트리거 발동 → `{id, "RECKLESS", Strategy, 10.0}` → 머리 위 [폭주] 아이콘 + 빨간 오라.

---

## 9. Phase 1 검증 체크리스트

프로토타입에서 아래가 **육안으로 구분**되면 이 설계는 합격이다.

1. 압박형+잔혹함 vs 압박형+겁쟁이 — 같은 전술인데 다른 선수처럼 보이는가?
2. 버서커(난전형+도끼) vs 전술가(카운터형+창) — 창이 거리를 지키다 도끼 후딜에 카운터를 꽂는 그림이 나오는가? (상성 검증 → 문서[4] 11장)
3. 오만함 도발 중 역전 KO가 100경기 중 2~5회 발생하는가? (너무 잦으면 억까, 없으면 무의미)
4. 한 경기를 10분 관전했을 때 같은 패턴 반복으로 느껴지지 않는가? (Noise/트리거 빈도 튜닝 지표)
